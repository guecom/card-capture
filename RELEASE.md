# Release Evidence Contract — Kairen Card Capture

Kairen-Ref: `TSK-000140` (release baseline). 이 문서는 "무엇이 하나의 release 증거인가"와 현재 확인된 baseline을 소유한다. PR merge, Pages build, GAS ping 중 하나만으로는 release를 증명하지 않는다.

## 하나의 Release Evidence가 담아야 하는 것

| 항목 | 확인 방법 |
| --- | --- |
| Repository 상태 | release 대상 commit SHA(가능하면 tag)가 `main`에 포함 |
| Pages 일치 | `https://guecom.github.io/card-capture/index.html` 콘텐츠 해시 == 해당 SHA의 `docs/index.html` 해시 (`sw.js` 동일) |
| GAS 동작 | `ping` ok · 무효 토큰이 `whoami`/`list`/`persondoc`(·`search`·`researchinstruction`)에서 `invalid_token` · guest 조사 지시 `owner_only` · target mismatch 거부 · unknown action 거부 · (배포 직후) 유효 owner 토큰 1회 실동작은 사람이 폰에서 확인 |
| GAS 배포 | 어떤 deployment version이 어느 Code.gs 상태인지(배포 일시·버전 메모) |
| Watcher | 실행 중 PID·health 파일 최신성(`watcher-health.json`), 워처 스크립트 버전(commit) |
| Web Push | 기본 off 확인 → VAPID public key·private registry·전용 sender/DPAPI 설정 → watcher safe health → 사람 승인 enable → Android opt-in·닫힌 앱 수신·same-origin 열기·해제. machine PASS와 live PASS를 구분 |
| Processing contract | vault `CardCapture_Processing.md`의 당시 상태(vault 이력으로 식별) |
| Human gate | merge·tag·GAS deployment를 승인한 사람·시점 |

release 기록은 이 파일 하단 "Verified Baselines"에 최신이 위로 오게 추가한다.

## Release 절차 (사람 게이트 포함)

1. **릴리즈 PR 안에서, merge 전에** 버전을 확정한다 — (a) `CHANGELOG.md`의 `[Unreleased]` 아래에 `## vX.Y.Z — 날짜 — 커밋` heading을 만들고, (b) `frontend/package.json`의 `version`을 `X.Y.Z`로 올리고, (c) 빈 `[Unreleased]`를 위에 남긴다. **tag 뒤로 미루지 마라** — 미루면 tag가 붙는 커밋에 옛 버전이 담겨 배포된 앱이 자기 버전을 틀리게 말한다. v2.13.0에서 실제로 일어났다(앱이 `버전 2.12.0`을 표시했고, `package.json`과 CHANGELOG가 **함께** 뒤처져 서로 일치했기 때문에 두 곳을 대조하는 검사로는 보이지도 않았다). `eval/version-sync.test.js`가 이 셋(heading·`package.json`·**실제 tag**)을 CI에서 강제한다.
2. branch → draft PR → `scripts/validate.ps1` PASS → 사람 review·merge (human gate).
3. 사람: release tag `vX.Y.Z` 생성 (human gate). 1단계에서 확정한 값과 **정확히 같은** 버전이어야 한다.
4. `Code.gs` 변경이 있으면 사람: GAS 재배포 — vault `CardCapture_Setup.md`의 클릭 단위 절차 (human gate).
5. Web Push 변경이 있으면 `PUSH_NOTIFICATIONS_ENABLED=false`로 Pages·GAS·watcher를 먼저 맞추고, key·registry·sender·safe health probe를 확인한 뒤 사람 승인으로만 enable한다.
6. 검증: 위 표의 각 항목 확인(Pages 해시 비교, GAS probe는 무효 토큰 거부까지 스크립트로, 유효 토큰·Push opt-in/receive/open/revoke는 폰에서). **배포된 앱의 설정 화면이 이 tag와 같은 버전을 말하는지 확인한다.**
7. 이 파일에 baseline 기록 + vault Task에 exact SHA·결과 회수.

조사 지시를 되돌릴 때는 code rollback 전에도 Script Property `RESEARCH_INSTRUCTION_ENABLED=false`로 새 접수를 닫을 수 있다. 이 변경과 재활성화는 사람 운영 게이트다.

Web Push를 되돌릴 때는 code rollback 전에도 `PUSH_NOTIFICATIONS_ENABLED=false`로 새 구독·상태 조회·발송을 닫는다. private registry와 durable outbox는 감사·복구 증거이므로 원인 확인 전에 삭제하지 않는다. 다시 enable하려면 `Initialize-CardCapturePush.ps1 -Force`로 VAPID key epoch를 회전하고 새 public key·sender token을 GAS에 반영한 뒤 Android에서 다시 opt-in한다. 이전 epoch event·subscription을 재사용하지 않는다.

## Rollback

- 코드: `git revert` 후 동일 절차(사람 merge). Pages는 main 반영 후 수 분 내 재빌드.
- GAS: 배포 관리에서 이전 version으로 전환(사람). 전환 후 behavior probe 재실행.
- 워처: 이전 commit의 `CardCapture_Watcher.ps1`로 교체 후 재시작(사람; BOM 유지 필수).

## Known Limitations (정직한 경계)

- GitHub Actions CI가 `scripts/validate.ps1`과 frontend unit·build·Playwright gate를 실행하지만, 실제 Android/iOS camera와 사용자 계정 상태는 합성 CI가 대체하지 못한다.
- 처리 agent의 write allowlist는 계약+회귀(eval) 수준이며 OS 수준 강제가 아니다(`SECURITY.md`).
- GAS 배포 version과 Code.gs 상태의 연결은 수동 기록에 의존한다(자동 검증 endpoint 없음).
- v2.0.0 배포 직후 유효 owner token을 쓰는 실동작은 token 노출과 운영 데이터 쓰기를 피하기 위해 agent가 자동 실행하지 않았다. founder는 배포 승인 전 앱과 검색이 정상 동작함을 확인했고, 다음 실제 폰 사용이 배포 후 owner-token 재확인 역할을 한다.

## Verified Baselines

### v2.26.0 @ `e490b19` — 검증 2026-08-04 (agent:kairen.claude, Kairen-Ref: TSK-000561 / INT-000036) — **INT-000036 네 표면 · Code.gs 무변경 / GAS 재배포 불필요 · actual phone NOT RUN**

- repository: annotated release tag `v2.26.0`(tag object `2cff678ea846b3e354f70af00d39976f8b0c0a2c`) → squash merge commit `e490b194a891828e12a672601b95ebbc50db5042`(main). 구현 PR은 [#86](https://github.com/guecom/card-capture/pull/86)이고 승인·검증된 exact PR head는 `b5830170b89369298270e97bb50c2f4df47b64a2`다. base는 `13f6f85`(PR #85의 v2.25.0 GAS 재배포 기록 merge)이고 직전 release tag `v2.25.0`은 `1235078`을 가리킨다 — **base commit과 직전 release tag는 같은 commit이 아니다.** 기존 tag는 움직이지 않았다.
  - GitHub Release는 2026-08-04T15:16:34Z에 `v2.26.0 — 개발 인터뷰 008의 네 표면`으로 발행됐다. `draft: false`, `prerelease: false`, 목록의 Latest가 `v2.26.0`이다.
- human gate: founder가 2026-08-04 세션에서 `이 개발 인터뷰 승인안 실행해줘`와 `PR, Merge, Release까지 해줘`로 구현·PR·exact-head merge·새 불변 tag·GitHub Release·Pages publication을 승인했다. **merge·tag·release를 실행한 것은 agent다** — GitHub이 기록하는 author는 founder 계정(`guecom`)이지만 그것은 이 기계의 인증된 `gh` CLI를 agent가 사용했기 때문이고, 사람이 버튼을 누른 것이 아니다. v2.25.0에서 founder가 직접 merge한 것과 다르므로 구분해 적는다. 이 승인은 actual-phone PASS, GAS 재배포, Script Property 변경, credential·production data 변경 승인이 **아니다.**
- CI: exact PR head `b583017`의 required `validate` run `30922073526` **SUCCESS**(11m57s). 통합 tree 로컬에서 Vitest **45 files / 776 tests PASS**, Playwright **296/296**, `scripts/validate.ps1` **fail=0 warn=0**, `eval/version-sync` 6/6(`package.json=2.26.0`, 최신 heading `2.26.0`, headings 28).
- **CI가 두 번 실패했고 둘 다 게이트 결함이었다. 지나가기를 기다린 판이 하나도 없다.**
  - 1차 `cab1fbd` run `30918890069`(attempt 1) FAILURE — `status-truth.spec.ts:245`에서 `.section-toggle` "명함 기록"을 5초 안에 못 찾았다. 2차(같은 run의 rerun) FAILURE — 이번에는 **다른** 시험(`int29-manual.spec.ts:428`)이 `입력 칸에 접근 이름이 없다`로 걸렸다. 판마다 걸리는 자리가 달랐다는 것이 제품 회귀가 아니라는 신호였다.
  - **원인을 확정한 것은 두 번째다.** 그 게이트는 시트가 보인 뒤 고정 `waitForTimeout(400)`으로 재고 있었다. 50ms 간격으로 실측하니 접근 이름이 붙는 시점이 그 경계 **바로 앞**이다 — `t=0~150ms`는 `aria-label`이 `null`, `t=200ms`에 `이 사람에 대해 아는 내용`이 붙고 그 뒤로는 안 바뀐다. 이 PC는 400ms 안에 그 200ms를 넘겼고, CI는 같은 스위트를 도는 데 **약 3.3배**(2.9분 → 9.7분) 걸려 못 넘겼다. **기계 속도가 판정을 정하고 있었다.**
  - 이 저장소는 같은 모양을 이미 한 번 원인으로 확정했다 — v2.25.0 항목의 작성 box 게이트가 정확히 `waitForTimeout(400)`으로 재다가 중간 프레임을 잡았다. **그때 그 게이트만 고치고 이 게이트는 남겨 뒀다.** 이제 시간이 아니라 상태로 기다린다.
  - 약해지지 않았음을 확인했다: `ManualPersonSheet`에서 `aria-label`을 지우고 다시 빌드하니 `Received: 0`으로 FAIL하고, 되돌리니 PASS한다. 고친 뒤 단독 12/12, 전체 296/296.
  - 1차 실패(`status-truth:245`)는 rerun과 최종 run에서 재현되지 않았다. 같은 성질(느린 기계 + 고정 예산)로 보이지만 **원인을 확정하지 않았다** — 확정하지 않은 것을 확정한 척하지 않는다.
- **release evidence PR([#87](https://github.com/guecom/card-capture/pull/87), `RELEASE.md` 한 파일)에서도 CI가 두 번 빨갰다. 이것이 코드와 무관함의 증거다** — 이 저장소는 `v2.14.0` 항목에서 같은 방식으로 판정한 적이 있다. 나온 것 넷 중 둘은 이 release가 새로 들여온 게이트 결함이라 고쳤고, 둘은 이 PR이 손대지 않은 기존 spec이다.
  - **고침 (이 release가 들여온 것)**: `int36-surfaces.spec.ts`의 다크 대비 판정이 `boot()` 직후 아무 대기 없이 곧바로 `page.evaluate`로 재서 `대비를 잴 표면을 0개밖에 못 찾았다`로 걸렸다. **0은 1·2와 다르다** — `querySelector('ion-header')`조차 null이었다는 뜻이고 곧 그 순간 셸이 없었다는 뜻이다. 세 표면이 보이는 것을 각각 기다린 뒤 재도록 고쳤다. 같은 파일의 다른 시험은 전부 대기를 두고 있었고 이 한 줄만 빠져 있었다. 덧붙여 `int30-research.spec.ts:445`도 같은 병이었다 — 목록 요청이 떴는지를 **한 번만** 읽어, 앱이 composer를 먼저 그리고 요청을 나중에 보내는 순서일 때 느린 기계에서 0이다. 응답은 route handler가 붙잡고 있으므로 poll로 바꿔도 판정 구간은 그대로다.
  - **안 고침 (기존·무관)**: `int29-research.spec.ts:421`이 30초 test 예산을 넘겼다. **이 PR은 그 파일을 바꾸지 않았고, 로컬 단독 실행은 3.2초다** — 예산의 1/9이라 3.3배를 곱해도 여유가 크다. 즉 marginal budget이 아니라 runner가 그 순간 멈춘 것이다. 예산을 늘려 덮지 않는다.
  - **새 게이트는 CI 불안정을 함께 들여온다.** 이 release의 신규 spec 하나가 흔들림 원인 둘을 만들었고 하나는 merge 전에, 하나는 merge 뒤에 드러났다. 다음부터 새 게이트는 **단독 반복 실행으로 흔들림을 털고** required check에 넣는다. 고정 대기(`waitForTimeout`)와 선행 조건 one-shot 읽기 둘 다 이 세션에서 실제 원인이었다.
  - **게이트 여섯 종을 고친 뒤 e2e 296건이 CI에서 전부 통과했다.** 고친 것은 전부 "시점을 재던 것을 상태로 바꾼" 것이고 임계값·예산·단언을 느슨하게 한 것은 하나도 없다. 그중 하나는 **오히려 엄격해졌다** — 갱신 박자 게이트가 재는 도중 4초↔20초 regime이 바뀌면 이제 소리 내어 실패한다(예전에는 서로 다른 regime의 값을 조용히 비교했다).
    - 그 박자 게이트의 CI 실패(`실제 간격은 313ms`)는 부하가 아니라 **관측 창을 여는 시점**이 원인이었고, 둘이 동시에 일어나고 있었다. (a) `waitUntil: 'networkidle'`은 *네트워크*가 조용하다는 뜻이지 첫 목록 조회가 시작됐다는 뜻이 아니라, 느린 기계에서는 `briefs`가 빈 채로 화면이 idle regime(`20초마다`)을 말한 뒤 곧 4초로 뒤집힌다. (b) 부팅이 priority read를 **두 번** 띄우고 두 번째가 single-flight의 trailing re-read라 첫 응답이 닿는 순간 시작된다 — 이 기계 실측 34·42·47·59·86·128·414·529ms, throttle에서 2.4s. CI의 313ms가 그 범위 안이다. **폴링 자체는 모든 관측에서 정직했다(3905~4054ms / 약속 4초).** 제품 결함이 아니다.
  - **남은 실패는 이 release가 손대지 않은 파일들의 wall-clock 예산 게이트다.** 판마다 다른 것이 걸린다 — `capture-flow:258`(2초 예산), `camera-detection:64`(8초 엔진 준비), `offline-shell:142`, `card-quad-model:42`(1.5초), `int30-capture:313`(렌더 전 측정), 그리고 watcher의 `APP-AC-239`(Deep slice 종료 wall-clock 상한). **하나도 늘리지 않았다** — 이것들은 사람이 체감하는 시간을 재는 단언이라 느슨하게 만들면 재는 뜻이 사라진다. 부하에 정직하게 민감하다는 사실을 아는 채로 둔다. 이 저장소는 `v2.14.0`에서 같은 이유로 재실행 3회가 필요했다고 기록한 적이 있다.
  - 두 게이트를 고친 뒤의 판에서는 **남은 실패가 하나뿐이었고 그것은 이 파일이 이미 기록해 둔 것**이다 — `capture-flow.spec.ts:258`이 실시간 2초 예산을 단언하는데 `2249ms`로 걸렸다(12% 초과). v2.14.0·v2.15.0·v2.25.0 항목이 같은 성질을 기록했고 **이 release가 만든 것이 아니다.** 예산을 늘리지 않았다 — 이 단언은 사용자가 체감하는 응답성을 재는 것이라 느슨하게 만들면 재는 뜻이 사라진다. 대신 그 판에서 실측값을 남긴다. 이 게이트는 기계 부하에 정직하게 민감하고, 그 사실을 아는 채로 둔다.
- 새 게이트의 수정 전 FAIL 확인: 신규 `frontend/e2e/int36-surfaces.spec.ts`(25 judgment)는 구현 lane과 분리된 context가 승인안을 사용자 문장으로 옮겨 쓴 독립 판정이다. 수정 전 `main`에서 **12건 FAIL**했고 두 판 연속 같은 숫자였다 — 자동 폴링 중 521 표본 가운데 **84건(≈16%)**에서 `aria-busy=true`(자동 폴링 3건 × 각 700ms에 정확히 대응), 진행을 말하는 표면 **4개**, `파일 올리기` 클릭 뒤 중간 면 **6개** 출현, 스위치 히트 영역 **40px**, 브랜드 표식 대비 세로 중심 **8.5px** 어긋남.
  - **게이트가 스스로 틀린 경우를 두 번 잡았다.** 즉답 mock 서버는 폴링이 1ms에 끝나 버튼이 도는 프레임을 아예 없앴고, 그래서 첫 판이 결함이 살아 있는 채로 **통과했다**(응답에 700ms를 줘야 재현된다). 시트의 아래→위 관측은 "칠해진 큰 면"을 쫓다가 매 프레임 backdrop의 `top=0`을 읽어 움직임 0px으로 오판했다(transform이 shadow DOM wrapper에 걸려 host rect로는 안 보인다).
- Pages: source `main:/docs`, build `status=built`, build commit `e490b194a891828e12a672601b95ebbc50db5042`, duration 51.8s(시작 2026-08-04T15:15:18Z). live 파일과 merge commit blob이 **참조된 25개 전부** 일치한다(entry HTML·service worker 2종·manifest·active JS·CSS와 `docs/next/index.html`이 참조하는 모든 vendor chunk). 주요 값 — `docs/index.html` `90AC1AD2077D21EB6B1D5779DC8D7F8AF039AA473B2DD4CAC071404192A5FF1D`(1,232 B), `docs/sw.js` `5FC49D5DC741A2DC50FF33770DD452790578DA1DCAC7731B176C324D32EA3221`(1,989 B), `docs/next/index.html` `50E2B692DB225B5C7645311B9A13E39884332C01734C504CCEFF0B8A5AF5E047`(10,114 B), `docs/next/sw.js` `6ECD1982B6C5F78DC66D4A013F31257B877D2F62E9B2555F3D0A2E292532042A`(9,194 B), active JS `docs/next/assets/index-BDAexAEf.js` `389BF17624FC782CA7EE21D07D18955706195FFB8C2291B4C16F53DF031DE54D`(254,239 B), CSS `docs/next/assets/index-DFAMBut8.css` `24E2FDE510B2335792692AFD206522144767C841B8AD5BAE9A82EEEC54958BD8`(99,464 B). live base는 `https://guecom.github.io/card-capture/next/`다.
  - 측정 주의: **원시 바이트로 내려받아 CRLF만 정규화해** SHA-256으로 쟀다. 문자열을 경유한 비교를 release 판정 근거로 쓰지 않는다. 이번에 그 함정을 직접 밟았다 — `Invoke-WebRequest -UseBasicParsing`의 `.Content`는 text/\*와 `application/javascript`에서 **디코드된 String**이라 `[byte[]]` 캐스트가 `$null`이 되고, 그것이 25개 전부 `MISS`로 보였다. 배포 실패와 구분되지 않는 모양이다. `-OutFile`로 디스크에 받아 바이트로 읽으면 25/25 일치한다.
  - **merge를 release 증거로 계산하지 않고 배포본을 열어 확인했다.** live active JS가 앱 버전 `2.26.0`과 build id `src-58f367575805`를 포함하고, 이번 승인 동작의 흔적이 그 안에 직접 있다 — `지금은 접수 안 돼요`·`다시 시도`·`붙여넣`·`끌어다`·`최신 상태 확인`·`자동 갱신`. **없어져야 할 옛 문구가 실제로 없다는 것도 함께 확인했다** — `지금은 못 골라요`·`새로고침 완료`·`새로고침 중…` 셋 다 0건. 있어야 할 것만 확인하면 지운 것이 지워졌는지는 아무도 안 본다.
- 이번 변경의 machine-observable behavior — 개발 인터뷰 008(`INT-000036`) Approved Proposal v2의 네 표면이다. 사용자 관점 문장은 `CHANGELOG.md`의 v2.26.0 절이 소유한다.
  1. **수동 갱신 진행 상태의 단일 소유권**(`TSK-000559` / `ISS-000050`). spinner·`aria-busy`·`새로고침 중`은 사용자가 누른 요청에만 나타난다. 원인은 `busy`가 *수동 요청*이 아니라 *모든 목록 조회가 공유하는 loading 플래그*에 붙어 있던 것이고, `busy` prop 자체를 없애 그 통로를 막았다. 진행·성공 toast는 0건이고, 실패는 원인 범주와 누를 수 있는 `다시 시도`로 남아 다음 요청이 해소할 때까지 유지되며 반복되면 단계가 올라간다.
  2. **상단 control cluster의 시각 균형**(`TSK-000220` / `ISS-000084`). 스위치가 상단 바에서 가장 두꺼운 요소(40px, 브랜드 34px·제목 34.2px)였고 세로 중심이 8.5px 떠 있었다. 26px·`--cc-r-sm`으로 낮추고 신선도 줄만큼의 spacer로 덩어리를 자기 중심에 대칭시켰다. 보이는 상자는 작아졌지만 실제 히트 영역은 `::after`로 **44px**다(이전 40px — 승인안의 "44px를 유지한다"는 오늘 사실과 달랐고, 유지가 아니라 키우는 일이었다).
  3. **`파일 올리기` 즉시 picker**(`TSK-000220` / `ISS-000084`). `<input type="file">`이 패널 안에 있어 패널이 먼저 렌더돼야 닿을 수 있었다. input을 카드 옆으로 옮겨 `card onClick → open() → input.click()`을 await 없는 동기 사슬로 만들었다(중간에 `await` 하나가 들어가면 브라우저가 신뢰된 제스처를 잃어 조용히 죽는다 — 사슬의 각 마디에 그 사실을 주석으로 박았다). `DesktopIntake`는 남을 이유가 없어 삭제했고 dead CSS 242줄이 함께 나갔다. 촬영의 전체 화면 modal과 직접 입력의 `initialBreakpoint 0.94` 시트는 그대로다. 끌어다 놓기와 클립보드 붙여넣기가 추가됐다.
  4. **조사 깊이 즉시 선택**(`TSK-000560` / `ISS-000082`). `deepAvailable: boolean`이 「아직 서버에게 못 들었다」와 「서버가 껐다고 말했다」를 하나의 `false`로 뭉개서, 첫 화면이 전자를 후자로 그리고 목록 응답이 도착할 때 뒤집혔다 — **그 뒤집힘이 곧 「기다리면 켜진다」였다.** `'unknown' | 'open' | 'closed'` 세 상태로 갈랐다. 세 깊이는 첫 render부터 선택 가능하고, 닫힘은 `지금은 접수 안 돼요`라는 지속 상태로 말하며 지연 낱말(`잠시`·`곧`·`기다`·`확인 중`)은 닫힘 문구에서 게이트로 금지된다. 목적 조건은 제출 validation이 소유하고, 막혔을 때 focus는 **이유별로** 실제 해소 가능한 자리로 간다(범위 미선택 → 범위 영역, 닫힘 → 깊이 선택). 내부 binding은 `RESEARCH_ROUTE_R1`(`quick=luna`·`standard=terra`·`deep=sol`)로 versioned이며 화면·접근성 이름·textual 속성에 0건이고, 그 검사는 오염 노드를 먼저 주입해 자기 유효성을 증명한다.
  5. **접근성 회귀 4건**(`TSK-000562` / `ISS-000244`). `명함 기록` 옆 `.refresh-hint`가 상단 바와 같은 사실을 반복하면서 1초마다 글자가 바뀌는 `role="status"`라 낭독기를 **1초에 한 번씩** 끊었다 — 삭제했다. 깊은 조사가 선택 가능해진 뒤에도 `data-unavailable="yes"`가 남아 눈과 보조 기술이 서로 다른 말을 했다. 꺼진 항목이 radiogroup 순회에서 통째로 빠져 키보드로는 `깊은 조사`의 존재를 알 수 없었다. 막힌 제출의 focus가 설명 문단으로 가서 키보드로 아무것도 할 수 없었다.
- 안전 경계: `Code.gs`, `watcher/`, `config/`, `.github/workflows/`, dependency lockfile을 **하나도 바꾸지 않았다**(`git diff --name-only`로 확인). `frontend/src/services/research-envelope.test.ts`가 `Code.gs` 원문을 읽어 client mode registry ⊆ 서버 `RESEARCH_MODES_`를 계속 증명하며, client registry를 넓히지 않았다. 버그 리포트에 조사 route receipt를 붙이면서 열거된 field만 넣었고(통째 `JSON.stringify` 금지), token·개인 링크 코드·`apiUrl`·Person 데이터가 도달할 수 없다는 기존 negative test는 그대로 통과한다. 깊은 조사의 fail-closed는 서버(`Code.gs:717`·`:1040`의 `deep_feature_disabled`)가 계속 소유한다 — client는 더 이상 추측하지 않을 뿐이다.
- **GAS deployment: 해당 없음.** `13f6f85..e490b19`에서 `Code.gs`가 바뀌지 않았다. Script Properties, credential, production data도 바꾸지 않았다. **따라서 이 release는 GAS 재배포를 요구하지 않는다.**
- **watcher: 해당 없음.** `watcher/`를 바꾸지 않았다. 다만 `v2.24.1`이 남긴 **운영 watcher 재기동은 여전히 미해소이며 이 release가 그것을 해소하지 않는다.**
- **`DEEP_RESEARCH_ENABLED` Script Property: 값을 여전히 확인하지 못했다.** 이 값은 유효 owner token 뒤에 있고 agent는 credential을 다루지 않는다. **설정돼 있지 않으면 `깊은 조사` 칸은 이제 고를 수는 있되 제출이 `지금은 접수 안 돼요`로 막힌다** — v2.25.0에서는 아예 고를 수 없었다. 그건 결함이 아니라 계약의 fail-closed 동작이고, 이번 변경은 그 사실을 지연이 아니라 지속 상태로 말하게 만든 것이다. 값을 켤지 말지는 사람이 정한다.
- 실기기 판정: **미완료.** 실제 폰에서 카메라·파일 선택창·붙여넣기·체감 성능을 하나도 실행하지 않았다. founder의 exact live build 판정도 없다. 이 release의 네 표면은 전부 사람이 보고 만지는 결과이고 PR lane도 그래서 `HUMAN TEST REQUIRED`였다. 저장소 게이트는 **구현 증거**이며 actual-phone PASS·MVP gate PASS·customer proof가 아니다. MVP build/testability gate comes before customer proof.
- **남은 사람 단계**:
  1. **founder actual-phone 판정** — 네 표면(자동 폴링 중 버튼이 도는지, 파일 올리기 한 번 누르기, 조사 깊이 세 칸, 자동 갱신 스위치의 두께·누르기). 이 판정을 done_criteria에 명시적으로 담고 있는 것은 `TSK-000220`이고(`TST-000018`의 founder exact-build 판정), 그래서 그것만 열려 있다. `TSK-000559`·`560`·`562`는 각자의 done_criteria가 기계 검증과 Result 연결까지이므로 닫혔다 — **그 셋이 닫혔다는 것이 실기기에서 좋았다는 뜻은 아니다.** 이 항목이 그 판정을 소유한다.
  2. **Script Property `DEEP_RESEARCH_ENABLED` 확인·결정.** `true`가 아니면 `깊은 조사`는 고를 수는 있어도 접수되지 않는다.
  3. **(`v2.24.1`에서 넘어온) 운영 watcher 재기동.** 이 release의 산출물이 아니지만 열려 있다.
- rollback: **PR revert 하나로 끝난다.** PR #86 merge commit `e490b194a891828e12a672601b95ebbc50db5042`를 revert하고 같은 Pages 절차로 재배포한다. 기존 tag를 움직이거나 재사용하지 않는다. `Code.gs`·Script Property·credential·production data·watcher를 바꾸지 않았으므로 **서버 rollback이 없다** — v2.25.0과 달리 되돌릴 서버 상태가 존재하지 않는다.
  - 되돌리면 사라지는 것: 네 표면 전부와 접근성 회귀 수정 4건, 그리고 끌어다 놓기·붙여넣기. `깊은 조사`는 다시 **고를 수 없는** 상태(`지금은 못 골라요`)로 돌아가고, 자동 폴링마다 새로고침 버튼이 다시 돈다.

### v2.25.0 @ `1235078` — 검증 2026-08-04 (agent:kairen.claude, Kairen-Ref: TSK-000220 / INT-000030) — **INT-000030 다섯 표면 · Code.gs 변경 / GAS 재배포 완료(버전 9, 2026-08-04) · `quick` 실동작 미검증 · actual phone NOT RUN**

- repository: annotated release tag `v2.25.0`(tag object `917cb40718441babda11cd35e55ca53fb0fbf7d5`) → squash merge commit `123507819cd0245b21a99b5d4ee976f8cd21983b`(main). 구현 PR은 [#83](https://github.com/guecom/card-capture/pull/83)이고 승인·검증된 exact PR head는 `f63c11bae08f170a34d4dd9758f54dc0da799638`다. base는 `05008009d78a37c8acbefc37ab0f324a0bb39869`(PR #82의 v2.24.1 evidence merge)이며 `v2.24.1` tag 자체는 `8b981875d1c190104e956bd8e88b626a44edda71`을 가리킨다 — **base commit과 직전 release tag는 같은 commit이 아니다.** 기존 tag는 움직이지 않았다.
  - **GitHub Release: 이 기록을 처음 적을 때는 미발행이었고, 2026-08-04에 발행됐다.** 처음 적을 때는 tag만 origin에 있고(`refs/tags/v2.25.0` → `917cb407…` → `1235078…`) `gh release view v2.25.0`이 `release not found`였으며 목록의 Latest는 여전히 `v2.24.1`이었다 — v2.19.0부터 v2.24.1까지 이어진 정식 Latest release 연속성에서 이 release만 그 단계가 비어 있었다. **2026-08-04T09:06:43Z에 `v2.25.0 — 개발 인터뷰 007의 다섯 표면`으로 발행됐다** — `draft: false`, `prerelease: false`이고 목록의 Latest가 `v2.25.0`으로 바뀌었다(`gh release view v2.25.0` · `gh release list`로 확인). 아래 "남은 사람 단계" 1번이 이렇게 닫혔다.
- human gate: PR #83은 body에서 lane을 **`HUMAN TEST REQUIRED`**로 선언했고(`docs/` live Pages 표면·user-visible behavior·visual interaction·카메라/모바일 경로 변경), `guecom`이 2026-08-04T08:32:45Z에 exact head `f63c11b…`를 squash merge했으며 31초 뒤(2026-08-04T08:33:16Z = 17:33:16 +0900) 같은 계정이 annotated tag `v2.25.0`을 만들었다. Product 승인은 `DEC-000105`, 인터뷰 승인안은 `INT-000030`이 소유한다(PR body 기준). **founder의 승인 문장 원문은 이 기록의 증거 안에 없다** — 앞선 baseline들처럼 인용하지 않고 확인된 것(선언된 lane, merge·tag의 행위자와 시각)만 남긴다. 이 merge와 tag는 actual-phone PASS, GAS 재배포, Script Property 변경, credential·production data 변경 승인이 **아니다.**
- CI: exact PR head `f63c11b…`의 required `validate` run `30891586279` **SUCCESS** — `run_attempt 1`, `run_duration_ms 636000`(**10m36s**). merge commit의 tree에서 로컬 재실행: Vitest **45 files / 725 tests PASS**(1.74s), Playwright 전체 **239 passed**(2.4m), `eval/pages-bundle-freshness` PASS(`src-d027b28abbb1`), `eval/server-syntax` PASS(Code.gs 구문 + research policy 10 fixture + fail-closed flag 경계), `eval/version-sync` **6/6**(`package.json=2.25.0`, 최신 heading `2.25.0`, headings 27), `scripts/validate.ps1` **fail=0 warn=0**(build reproducibility 27 artifact byte-identical, watcher 5개 스위트, prompt-injection 20/20 포함).
- **CI가 두 번 실패했고 둘 다 flaky가 아니었다.** 지나가기를 기다린 판이 하나도 없다.
  - 1차 — `2f1cca4…` run `91912698768` FAILURE. 4건이 서로 다른 결함이 아니라 **한 축**이었다: 웹캠 없는 기계. 개발 PC에는 웹캠이 있어 로컬에서 전부 초록이었고 GitHub Actions runner에는 없어 전부 걸렸다. 그 기계를 이 PC에서 시늉해 네 건을 같은 값으로 재현한 뒤 고쳤다.
    - **(a) 같은 줄 진입 카드 정렬 2건.** 카드가 줄을 **각자** 나눠 가져서, `1fr`로 마지막 조각을 바닥에 붙이는 방식은 같은 줄 두 카드의 마지막 조각 높이가 우연히 같을 때만 맞았다. 웹캠이 없으면 `명함 앞면 촬영`에 `왜 지금 안 되는지` 조각이 하나 더 붙어 다섯 조각이 되고 마지막이 34px짜리 회복 버튼인데, 옆 `직접 입력`은 네 조각에 14px짜리 글줄이다. 바닥은 맞았고 **시작선이 20px(1280px) / 25px(390px) 어긋났다.** 문구를 줄이거나 값을 맞추지 않고 **구조로** 고쳤다 — 줄의 소유권을 카드 밖 grid로 옮기고 카드는 `subgrid`로 그 줄에 얹는다. 정렬이 "글자가 몇 줄로 접혔는가"에서 완전히 떨어져 나온다.
    - **(b) 촬영 세션 timeout 2건.** 두 spec이 기기 **목록만** 선언하고 카메라를 **여는 것**은 선언하지 않았다. 앱은 `getUserMedia` 관측이 목록 조회를 이기게 만들어져 있고(`noteCameraOutcome`) 그건 방금 거부당한 기기가 열리는 척하지 않기 위한 계약이라 제품 쪽이 옳다. 그래서 첫 촬영은 통과하고(파일 입력은 modal phase와 무관하게 있다) modal 안에서 `NotFoundError`가 나며 카드가 회복 버튼으로 바뀌어, **두 번째** 촬영부터 촬영 modal 대신 파일 올리기 작업면이 열렸다. 실패 지점이 정확히 두 번째 `shootFront`인 것과, 촬영을 한 번만 하는 같은 파일의 시험들이 통과한 것이 그 증거다.
  - 2차 — `9e5eb05…` run `91927632180` FAILURE. 이건 성질이 다르다: **제품이 아니라 게이트가 틀렸다.** `작성 box가 같은 테두리·모서리를 쓴다` 게이트가 여섯 칸 중 직접 입력 시트의 한 칸만 `rgba(35,104,216,0.62)`로 읽었는데, 그 값은 정확히 `--cc-field-line-focus`이고 `getAnimations()`도 0이었다 — 중간 프레임이 아니라 **정착한 focus 색**이다. `ManualPersonSheet`가 `onDidPresent`에서 그 칸에 주는 focus는 400~600ms에 도착하는데 게이트는 `waitForTimeout(400)`으로 쟀다. 그 경계 위에서 같은 코드가 기계 속도에 따라 어떤 판은 평상시를, 어떤 판은 focus 중인 칸을 잰다. 제품 코드·자동 focus·`frontend/src`를 건드리지 않고 게이트만 상태 기반(`sheetFocusLanded()`·`atRest()`)으로 고쳤다. 약해지지 않았다 — 굵기 `3px`, 색 `rgba(42,63,89,0.12)`, 모서리 `4px`, 그림자 `none` 네 축에 일부러 불일치를 넣어 각각 3/3 FAIL을 확인하고 되돌렸으며, 네 판 모두 여섯 칸이 `focusWithin:false`인 상태에서 걸렸다(평상시를 재면서 잡았다는 증거).
- 게이트가 **실행한 기계의 사실에 판정을 맡기던 것**을 없앴다. 정렬 게이트는 카메라 유무를 기계에 물어보고 있었다. 이제 spec이 카메라 있음/없음 두 세계를 **선언해서** 전수로 돈다(2 → 4). 로컬 재실행 로그에 `(camera present)`·`(camera absent)`가 390px·1280px 전 조합으로 남는다. 선언이 실제로 갈렸는지는 `is-unavailable` 카드 수로 함께 확인한다 — 안 갈리면 게이트가 통과하며 아무것도 재지 않는다.
- **`subgrid`는 이 저장소에서 처음 쓴다**(Chrome 117+ · Safari 16+ · Firefox 71+). `@supports (grid-template-rows: subgrid)`로 감쌌고, 모르는 엔진에는 `grid-template-rows: auto auto 1fr auto` 뒷줄이 남는다 — 정렬 보장은 없지만 네 줄 구조와 바닥에 붙는 행동은 유지된다. 카드에 걸려 있던 세로 하한(182/174/190px)은 없앴다: 줄을 바깥이 소유하면 카드 높이는 track의 합이라 그 선언은 아무 일도 하지 않으면서 남는다. 하한 계약은 `int30-capture.spec.ts`의 140px과 `int29-manual.spec.ts`의 "두 카드 같은 높이"이고 둘 다 그대로다.
- Pages: source `main:/docs`, `pages-build-deployment` run `30892404451` SUCCESS(시작 2026-08-04T08:32:47Z), build commit `1235078…`, status `built`, duration 53.3s. live 파일과 merge commit blob이 **참조된 24개 전부** 일치한다(entry HTML·service worker 2종·active JS·CSS와 `docs/next/index.html`이 참조하는 모든 vendor chunk). 주요 값 — `docs/index.html` `90AC1AD2077D21EB6B1D5779DC8D7F8AF039AA473B2DD4CAC071404192A5FF1D`, `docs/sw.js` `5FC49D5DC741A2DC50FF33770DD452790578DA1DCAC7731B176C324D32EA3221`, `docs/next/index.html` `48217F7A9FE2196D3DC4D7E97218055BE580B44A16F9DB2450DD1D0A10376EBC`, `docs/next/sw.js` `276F0705139933475908113ED04FFC8DE1FFB38FE364551864A1C64DDF847DB8`, active JS `docs/next/assets/index-D7ChgPDv.js` `F9B3926E5B37D6300A4321E4D5DF2A29E256DBB83A2DE07D710B7F578131B343`(247,854 B), CSS `docs/next/assets/index-DvazdmBf.css` `F03869FDFDC862CF43C017F38654264782BABCBD69F0E4A46BD199F774E7336C`(99,037 B). live base는 `https://guecom.github.io/card-capture/next/`다.
  - 측정 주의: v2.23.0·v2.24.0과 같이 **원시 바이트로 내려받아 CRLF만 정규화해** SHA-256으로 쟀다. 문자열을 경유한 비교를 release 판정 근거로 쓰지 않는다.
  - merge를 release 증거로 계산하지 않고 **배포본을 열어** 확인했다. live active JS가 앱 버전 `2.25.0`과 build id `src-d027b28abbb1`을 포함하고, 이번 승인 동작의 흔적이 그 안에 직접 있다 — `빠른 조사`·`일반 조사`·`깊은 조사`·`지금은 못 골라요`·`자동 갱신`·`파일 올리기`·`만남 맥락`·`적기 시작`. live CSS 안에서 `grid-template-rows:subgrid`도 확인했다(정렬 수정이 실제로 나갔다는 뜻).
- 이번 변경의 machine-observable behavior — 개발 인터뷰 007(`INT-000030`)의 승인된 다섯 표면이다. 사용자 관점 문장은 `CHANGELOG.md`의 v2.25.0 절이 소유한다.
  1. **캡처 진입 카드 공통 해부 + 만남 맥락 작성 박스**(`TSK-000220` / `ISS-000084`). `명함 앞면 촬영`·`직접 입력`·`파일 올리기`가 아이콘·제목·한 줄 설명·다음 동작의 같은 네 줄 해부를 쓰고, 같은 줄에 선 카드는 그 줄을 문자 그대로 공유한다. 웹캠이 없거나 권한이 막힌 기기에서도 촬영 입구가 사라지지 않고 이유와 대안 하나를 달고 남는다. 앱 안의 모든 작성 칸이 같은 테두리·모서리·포커스 표시를 쓴다.
  2. **조사 composer 위계 + 빠른·일반·깊은 조사**(`TSK-000542` / `ISS-000082`). `모두 선택`이 직접 적는 칸 바로 아래로 올라오고 조사 깊이 세 칸이 생긴다. 새 요청은 언제나 `일반 조사`에서 시작하고, 고른 깊이와 범위는 계약이 정한 봉투(`mode`·`purposes`·`focusIds`·`requestId`)로 **그대로** 나간다. `깊은 조사`는 범위 1개 이상에서만 접수되며 막혀도 깊이를 몰래 낮추거나 범위를 대신 고르지 않는다.
  3. **자동 갱신 ON/OFF 토글과 수동 새로고침 분리**(`TSK-000543` / `ISS-000050`). 스위치는 켜짐·꺼짐만 말하고, 새로고침 아이콘은 **요청이 실제로 떠 있는 동안에만** 돌며, 한 줄이 마지막 수신 시각과 현재 박자를 글자로 말한다. 새로고침 버튼은 스위치 상태와 무관하게 언제나 있고, 끈 상태는 저장하지 않는다.
  4. **설정을 방문 job 중심 IA로 재구성**(`TSK-000544` / `ISS-000217`). `계정·연결 → 캡처·조사 → 알림 → 데이터·개인정보 → 앱 정보·지원` 순서, 바꾸는 설정과 읽기만 하는 상태의 시각적 분리, 진단·버그 리포트는 `문제가 생겼을 때` 접힌 자리, 연결 해제는 결과를 먼저 말하는 위험 영역.
  5. **PC 진입에서 핵심 기능 유지 + secure bootstrap**(`TSK-000545` / `ISS-000235`). 연결 상태가 결정하던 세 질문(서버에 물어볼 수 있는가 / 무엇을 그릴 것인가 / 무엇을 할 수 있는가)을 갈랐다. `blocksShell`은 타입 수준에서 `false`이고, 로컬로 끝나는 일(`capture`·`research`·`records`)은 연결과 무관하게 쓸 수 있으며, 서버가 있어야 하는 일(`serverSync`·`ownerSearch`)만 막히고 안내는 그 기능 옆 inline setup card 한 장으로 붙는다. 전면 경고는 만들지 않고 링크 코드를 주소로 받아 오지 않는다.
- **GAS deployment: 이 기록을 처음 적을 때는 미실행이었고, 2026-08-04에 버전 9로 배포됐다.** `0500800…..1235078…`에서 `Code.gs`가 **+14/−4줄** 바뀌었다 — research mode allowlist에 `quick`을 넣고(`RESEARCH_MODES_ = ['quick', 'standard', 'deep_evidence_graph']`) 그 예산을 `branchCap 4 / timeCapMinutes 10`으로 정했다(deep은 24/90, standard는 10/30 그대로). **처음 적을 때 Apps Script 재배포는 실행되지 않았고, 그래서 live 서버는 이 release 이전 코드를 돌리고 있었다.** 아래 `GAS 재배포 실행` 항목이 그 상태를 끝냈다.
  - 그 구간(merge 2026-08-04T08:32:45Z ~ 배포 2026-08-04T10:03Z, **약 1시간 반**)의 실제 동작: `빠른 조사` 요청은 옛 서버 allowlist에 `quick`이 없어 **요청 전체가 `bad_research_request`로 거절된다**(촬영 업로드 경로에서는 사진까지 함께 거절된다). 클라이언트가 같은 요청의 `mode` 한 칸만 `standard`로 바꿔 **딱 한 번** 다시 보낸다. 그래서 **사용자는 조사를 정상적으로 받고**, 내려갔다는 사실은 개발자 채널(`research-telemetry.ts` — 기억 속에만 사는 read model, 저장소·주소창·화면 어디에도 쓰지 않음)에 `degraded`로만 남는다.
  - 이 되돌림이 **진짜 실패를 감추지 않는 근거**: 두 시도의 차이는 `mode` 한 칸뿐이고 서버의 나머지 검사(빈 요청·target·권한·한도)는 `quick`과 `standard`에서 완전히 같다. 원인이 mode가 아니었다면 두 번째 시도도 정확히 같은 코드로 거절되고 그 실패가 그대로 위로 올라간다. 되돌림은 `mode === 'quick'` **이면서** 응답이 정확히 `bad_research_request`일 때만 참이다(`quickModeRejected()`).
  - `config/public-runtime.json`, Script Properties, credential, production data는 이 release에서 바꾸지 않았다. 아래 재배포도 이 넷을 바꾸지 않았다.
- **GAS 재배포 실행 — 2026-08-04, 버전 9.** founder의 명시 요청으로 agent가 founder의 브라우저를 조작해 수행했다. 결과 문구는 `배포가 업데이트되었습니다`, 버전 9(2026. 8. 4., 오후 7:03 KST = 10:03Z).
  - **덮어쓰기 전에 live editor를 먼저 읽었다** — 1720줄이고 `RESEARCH_MODES_`도 quick 예산도 없는, 이 release 이전 코드였다. 다른 세션의 작업을 덮어쓰지 않았다. 저장소와 대조하면 base `0500800…`의 `Code.gs`가 정확히 1720줄이고, 그 파일은 `v2.24.0`(`916b2e1…`)의 `Code.gs`와 byte 동일하다(`v2.24.1`은 `Code.gs`를 바꾸지 않았다). **따라서 버전 8 = `v2.24.0`의 `Code.gs` 상태다.** 이 연결은 이 파일에 기록된 적이 없었고(`Known Limitations`의 "GAS 배포 version과 Code.gs 상태의 연결은 수동 기록에 의존한다"), 이 줄이 그 기록이다. rollback 대상이 이 값이다.
  - 배포한 source는 `c7188a52…`의 `Code.gs`다. 이것은 release tag가 가리키는 `1235078…`이 **아니라** 그 위의 evidence-only commit이지만, 두 commit의 `Code.gs`는 byte 동일하다(`git diff 1235078 c7188a5 -- Code.gs` 빈 출력). 배포된 서버 코드는 v2.25.0의 `Code.gs`다.
  - 적용 전에 세 표식을 source 원문에서 확인했다 — `var RESEARCH_MODES_ = ['quick', 'standard', 'deep_evidence_graph'];`, `var quick = request.mode === 'quick';`, `branchCap: deep ? 24 : quick ? 4 : 10` / `timeCapMinutes: deep ? 90 : quick ? 10 : 30`. 저장소의 같은 commit에서 각각 1261·1339·1356·1357행이다.
  - **새 deployment를 만들지 않았다.** 배포 관리에서 **기존 deployment를 편집해** `버전: 새 버전`으로 올렸다. deployment id가 바뀌지 않았고 `config/public-runtime.json`의 `apiUrl`에 박힌 값과 일치한다. 따라서 공개 `/exec` URL이 그대로이고 client 재빌드가 필요 없다 — 배포된 v2.25.0 앱이 그대로 이 서버를 가리킨다.
  - 배포 직후 무해 probe: `?action=ping` → `{"ok":true,"service":"card-capture","time":"2026-08-04T10:05:08.648Z"}`. 이 기록을 적으면서 다시 재도 같은 모양이었고(5회, 예: `10:11:06.156Z`), 미등록 GET action은 `{"ok":false,"error":"unknown_action"}`이다. **이것은 배포된 코드가 파싱되고 서비스된다는 뜻이지 `quick` 경로가 동작한다는 뜻이 아니다** — `ping`은 build 표식을 돌려주지 않고(`Code.gs`의 ping 분기가 `{ok, service, time}`만 만든다) 이번에 바뀐 코드가 닿는 경로는 전부 token 뒤에 있다.
  - **`quick` mode end-to-end는 여전히 미검증.** 새 경로는 전부 개인 링크 token 뒤에 있고 agent는 credential을 다루지 않는다. 확인은 사람 몫이며 아래 "남은 사람 단계" 4번이 그 내용이다.
  - 측정 주의 — **줄 수를 배포 판정 근거로 쓰지 않는다.** 위 1720은 editor가 센 값이고 저장소의 `git show 0500800:Code.gs | wc -l`과 정확히 같다. 그런데 배포한 raw 파일은 같은 방식으로 세면 1730줄인데 적용 전 확인에서는 1731줄로 읽혔다 — 파일 끝 개행 뒤의 빈 줄을 세느냐 마느냐의 축 차이이고, 두 축 모두 diff의 net `+10`(=`+14/−4`)과 맞는다. 무엇이 배포됐는지의 근거는 위 세 표식이다.
- **`DEEP_RESEARCH_ENABLED` Script Property: 값을 확인하지 못했다.** 이 값은 `whoami` 응답으로만 나오고 `whoami`는 유효 token을 요구한다. **설정돼 있지 않으면 `깊은 조사` 칸은 보이되 고를 수 없는 상태(`지금은 못 골라요`)로 뜬다.** 그건 결함이 아니라 계약의 fail-closed 동작이다 — 앱의 `deepAvailable` 기본값이 `false`이고("못 들었으면 닫힘이다") 서버도 `deep_feature_disabled`로 닫는다. 그러나 **founder는 첫 사용에서 이 상태를 그대로 만난다.** Script Property 변경은 사람 게이트다.
  - **2026-08-04 재배포 세션에서도 이 값을 확인하지 못했다.** Script Properties 읽기가 막혔고, 그 저장소에는 token이 함께 들어 있다. 재배포는 코드만 바꿨을 뿐 이 값을 건드리지 않았으므로 배포 전후 상태가 같다.
- **watcher: 해당 없음.** 이 release는 `watcher/`, `.github/workflows/`, `config/`, dependency lockfile, `SECURITY.md`, `AGENTS.md`, `docs/` 루트(legacy shell)를 바꾸지 않았다. 운영 watcher 교체·재기동은 이 release에 필요하지 않다. 다만 `v2.24.1`이 남긴 **운영 watcher 재기동은 여전히 미해소이며 이 release가 그것을 해소하지 않는다.**
- 알려진 e2e 흔들림(정직한 경계): `frontend/e2e/capture-flow.spec.ts:258`(`auto capture cancels a shake that starts after ready and retries on a stable frame`)이 실시간 2초 예산을 단언한다 — `expect(retry.stableRetryMs, 'shake가 끝난 뒤 안정 촬영이 2초 안에 끝나야 한다').toBeLessThan(2_000)`. CPU 경합에서 걸리고 단독 실행에서는 통과한다. 이 저장소는 같은 성질을 아래 **v2.14.0 항목의 `정직한 경계`**(`offline-shell.spec.ts:134`·`credential-boundary.spec.ts:294`)와 v2.15.0 항목에서 이미 기록했다 — **이 release가 만든 성질이 아니다.** (그 기록을 줄 번호로 가리키지 않는다. 새 baseline이 위에 쌓일 때마다 줄이 밀려 다음 사람이 엉뚱한 문단을 읽게 된다.) 판정에 쓴 두 판에서는 나지 않았다(exact-head CI run `30891586279` SUCCESS, 로컬 전체 239 passed). 깨끗한 suite인 척하지 않는다.
- 실기기 판정: **미완료.** 실제 폰에서 카메라·crop·OCR·체감 성능을 하나도 실행하지 않았다. founder의 exact live build 판정도 없다. 이 release의 다섯 표면은 전부 사람이 보고 만지는 결과이고 lane도 그래서 `HUMAN TEST REQUIRED`다. 저장소 게이트는 **구현 증거**이며 actual-phone PASS·MVP gate PASS·customer proof가 아니다. MVP build/testability gate comes before customer proof.
- **남은 사람 단계 (원래 번호를 유지한다 — 1·2는 2026-08-04에 닫혔다)**:
  1. **[닫힘 2026-08-04] GitHub Release `v2.25.0` 발행.** `draft: false`·`prerelease: false`·Latest로 09:06:43Z에 발행됐다(위 repository 항목).
  2. **[닫힘 2026-08-04] Apps Script 재배포.** 기존 deployment를 편집해 버전 9로 올렸다, 10:03Z(위 `GAS 재배포 실행` 항목). 이 단계 전까지 `빠른 조사`는 서버에서 거절되고 조용히 `일반 조사`로 나갔다.
  3. **Script Property `DEEP_RESEARCH_ENABLED` 확인·결정.** `true`가 아니면 `깊은 조사`는 계속 `지금은 못 골라요`다. 켤지 말지는 사람이 정한다. **2번이 먼저 닫혔으므로 순서 제약은 해소됐다** — 옛 코드 위에서 이 값만 켜면 `깊은 조사`만 열리고 `빠른 조사`는 계속 내려가는 상태가 되는데, 지금은 그럴 수 없다.
  4. **재배포 뒤 behavior 재검증**(`Release 절차` 6단계). **`quick`이 실제로 도는지는 아직 아무도 확인하지 않았다 — 배포는 코드가 서비스된다는 것까지만 증명한다.** 폰에서 유효 owner token으로 `빠른 조사` 한 번을 접수하고, 그 결과 `capture.json`이 `mode:'quick'`과 `policy.branchCap: 4`·`timeCapMinutes: 10`을 기록하는지 확인한다(`bad_research_request` 없이 접수되는 것만으로는 부족하다 — 클라이언트가 조용히 `standard`로 되돌려도 사용자 눈에는 같아 보인다). 이어 `깊은 조사`가 범위 1개 이상에서 접수되는지, 배포된 앱의 설정 화면이 `버전 2.25.0 · 빌드 src-d027b28abbb1`을 말하는지 확인한다.
  5. **founder actual-phone 판정** — 다섯 표면(진입 카드 정렬·조사 깊이·자동 갱신 스위치·설정 IA·PC 진입). lane이 `HUMAN TEST REQUIRED`인 이유가 이것이고, 이 판정 전까지 `TSK-000220`·`542`·`543`·`544`·`545`는 Done이 아니다.
  6. **(`v2.24.1`에서 넘어온) 운영 watcher 재기동.** 이 release의 산출물은 아니지만 열려 있다.
- rollback: **PR revert 하나로는 부족하다. 서버와 웹을 함께 되돌려야 한다.** 이 기록을 처음 적을 때는 `Code.gs`가 바뀌었어도 배포되지 않아 되돌릴 서버 상태가 없었고, 그래서 "지금은 PR revert 하나로 충분하다"고 적었다. **2026-08-04 버전 9 배포로 그 전제가 사라졌다** — 웹 release만 revert하면 새 서버 코드가 live에 그대로 남는다.
  - 지금 전체 rollback이 필요로 하는 것, **이 순서대로**: (1) **Apps Script 배포 관리에서 기존 deployment를 편집해 버전 8로 되돌리고 재배포한다.** 버전 8은 `v2.24.0`의 `Code.gs` 상태다(위 `GAS 재배포 실행` 항목). **새 deployment를 만들지 않는다** — 만들면 `/exec` URL이 바뀌어 이미 배포된 앱이 서버를 잃는다. (2) PR #83 merge commit `1235078…`을 revert하고 같은 Pages 절차로 재배포한다. (3) `Rollback` 절의 behavior probe를 다시 돌린다. 기존 tag를 움직이거나 재사용하지 않는다. Script Property·credential·production data·watcher rollback은 없다.
  - **순서가 임의가 아닌 이유 — 두 중간 상태의 무게가 다르다.**
    - **옛 서버 + 새 앱**(서버를 먼저 되돌렸고 웹 revert가 아직 안 나간 구간): 클라이언트의 한 번짜리 `standard` 되돌림이 **정확히 이 상태를 위해 만들어졌다.** `빠른 조사`가 `bad_research_request`로 거절되면 같은 요청의 `mode` 한 칸만 바꿔 딱 한 번 다시 보내고(`quickModeRejected()`), 사용자는 조사를 정상적으로 받으며 내려갔다는 사실만 개발자 채널에 `degraded`로 남는다. 이 release가 merge된 뒤 버전 9 배포까지 **약 1시간 반 동안 live에서 실제로 돌던 상태**이기도 하다.
    - **새 서버 + 옛 앱**(웹을 먼저 되돌렸을 때 남는 구간): 옛 앱은 `mode:'quick'`을 보내지 않으므로 서버의 `quick` 분기를 아무도 밟지 않고, `standard`·`deep_evidence_graph` 예산은 이 release가 바꾸지 않았다(10/30·24/90 그대로). **원리상 무해하다. 그러나 이 조합은 한 번도 시험된 적이 없다** — 원리는 검증이 아니다.
    - 따라서 서버를 먼저 되돌린다. 그러면 잠깐 머무는 곳이 **설계된** 상태이고, 웹을 먼저 되돌리면 **시험된 적 없는** 상태다.
  - 되돌리면 사라지는 것: 다섯 표면 전부와 `subgrid` 정렬, 그리고 **서버의 `quick` 접수 자체**(allowlist에서 빠지고 `branchCap 4 / timeCapMinutes 10` 예산도 없어진다). 그리고 `v2.24.1` 기준선으로 돌아가므로 **PC 첫 방문에서 `AI 조사 요청` 작성 자리가 다시 DOM에서 통째로 사라지고 화면 맨 위에 `링크 설정이 필요해요` 전면 경고가 돌아온다**(base `0500800…`의 `App.tsx`에서 실제로 그 상태임을 확인했다).

### v2.24.1 @ `8b98187` — 검증 2026-08-04 (agent:kairen.claude, Kairen-Ref: TSK-000531 / ISS-000232) — **ISS-000232 운영 1차 원인 확정·제거 · watcher 변경 / Code.gs 무변경 · watcher 재기동 미실행 · actual phone NOT RUN**

- repository: annotated release tag `v2.24.1` → merge commit `8b981875d1c190104e956bd8e88b626a44edda71`(main). 구현 PR은 [#81](https://github.com/guecom/card-capture/pull/81)이고 승인·검증된 exact PR head는 `32127601aa6f6349444ecee7b318b08a55434ced`다. 기존 tag는 움직이지 않았다. GitHub Release는 draft·prerelease가 아닌 Latest 정식 release로 발행됐다.
- human gate: founder가 2026-08-04 세션에서 `PR, Merge, Release까지 해줘`로 이 인터뷰 승인안의 구현·PR·exact-head merge·새 불변 tag·GitHub Release·Pages publication을 승인했고, 원인이 확정된 뒤 `일단 됐다 치고 그냥 진행해줘`로 이 수정의 진행을 승인했다. 이 승인은 actual-phone PASS, **운영 watcher 재기동**, `PER-000418` live requeue, credential·Script Property·production data 변경 승인이 **아니다.** 같은 날 founder가 직접 실행한 운영 행동은 둘이다 — 원인 log를 확보하게 한 **watcher 재기동**(그 재기동으로 같은 실패가 다시 났다)과 `v2.24.0`이 요구하던 **GAS 재배포**.
- **원인 확정 — 이 release의 본체.** 2026-08-04 재기동으로 실패가 재발했고 이번에는 processor log가 retention 안에 살아 있었다. 세 attempt log(`research-5a2249e5-77c1-4ef1-9f12-6ff27d9eabde-1/-2/-3.log`, 각 278바이트)가 전부 같은 한 줄이었다 — `Failed to read prompt from stdin: input is not valid UTF-8 (invalid byte at offset 0).` 기전은 두 겹이다.
  1. **CP949로 열린 stdin.** `Invoke-BoundedDeepProcessor`가 대입하던 `$startInfo.StandardInputEncoding`은 .NET Core 2.1에서 생긴 속성이라 **.NET Framework 4.x(=Windows PowerShell 5.1)에 존재하지 않는다.** 세 인코딩 대입이 한 `try` 안에서 `catch {}`로 끝나, 던지는 첫 줄이 stdout·stderr 인코딩 설정까지 함께 건너뛰게 했다. stdin은 ANSI(한글 Windows에서 CP949)로 열렸고 조사 프롬프트 첫 글자가 한글이라 offset 0에서 즉시 깨졌다. deterministic이므로 requeue가 전부 같은 결과를 정확히 재생산했다 — 사용자가 겪은 `다시 처리를 눌러도 반응이 없다`가 이것이다. 이 기계에서 확인: `PSVersion 5.1.26100.8875`, `CLR 4.0.30319.42000`, `ProcessStartInfo has StandardInputEncoding: False`.
  2. **프롬프트보다 먼저 새는 BOM.** 첫 수정은 로컬에서 통과하고 CI에서 실패했다 — 72바이트가 `EF BB BF`가 붙어 75바이트로 도착했다. .NET Framework는 `StandardInput` StreamWriter를 `AutoFlush = true`로 만들고 그 setter가 즉시 flush하므로, 인코딩에 preamble이 있으면 **우리가 한 글자 쓰기도 전에** BOM이 파이프로 나간다. GitHub Actions runner의 `Console.InputEncoding`이 BOM 있는 UTF-8이라 그곳에서만 조건이 성립했다. `StandardInput` 대신 `BaseStream`을 닫는 것으로는 막히지 않는다 — preamble은 닫을 때가 아니라 열릴 때 나간다.
  - 다른 경로가 무사했던 이유: quick-pass는 프롬프트를 argv로 넘기고(`Invoke-StandardProcessor`), Web Push sender는 비ASCII를 `\uXXXX`로 이스케이프한 `ConvertTo-Json` 출력을 넘긴다. **stdin에 한글 원문을 쓰는 경로는 Deep 조사 하나뿐이었다.**
  - 이전에 `v2.24.0` 기록이 남긴 socket 10013은 제한된 로컬 재현의 관측이었고 원인으로 확정된 적이 없다. 이제 **반증됐다.**
- CI: exact PR head `3212760…`의 required `validate` run `30866982454` **SUCCESS**(7m29s). 로컬에서 watcher protocol **93/93**(기존 92 + 신규 stdin 게이트 1), `scripts/validate.ps1` `fail=0 warn=0`.
- 새 게이트의 수정 전 FAIL 확인: 첫 게이트는 **자기 환경을 믿어** 개발 PC에서 초록이었고 같은 시각 CI에서 결함이 살아 있었다. 지금 게이트는 실제로 관측된 두 적대 조건 — CP949와 BOM 있는 UTF-8 — 을 **스스로 만들고**, 하나도 걸지 못했으면 통과로 세지 않는다. 수정 전 실측 `cp949 57/72 head=199`, `utf8-bom 75/72 head=239,187,191` → 수정 후 두 조건 모두 `72/72 head=237,149,156`. 게이트는 "무엇을 설정했는가"가 아니라 **처리기가 실제로 받은 바이트**를 잰다.
- Pages: **이 tag에 대한 live 대조는 수행하지 않았다.** `v2.24.0..v2.24.1`의 frontend 변경은 `frontend/package.json`의 버전 문자열 `2.24.0` → `2.24.1` **하나뿐**이고, 사용자에게 보이는 화면 동작은 바뀌지 않았다. 저장소 쪽 사실만 기록한다 — 버전 문자열 변경으로 번들이 재빌드돼 active JS가 `docs/next/assets/index-C9X4vI-t.js` → `docs/next/assets/index-lG9HHtY9.js`로 바뀌었고 build id는 `src-8bfdbf4abf96`, CSS `docs/next/assets/index-BkLkkMLX.css`는 그대로다. **여기에 per-file Pages SHA-256을 적지 않는다** — 재지 않은 값을 적으면 그것이 다음 release의 거짓 기준선이 된다.
- 이번 변경의 machine-observable behavior: (1) watcher가 Deep 처리기를 띄우기 전에 `Console.InputEncoding`을 preamble 없는 UTF-8로 바꾸고(기존 값에 preamble이 있을 때만, 끝나면 원복) 프롬프트를 `StandardInput.BaseStream`에 **raw UTF-8 바이트로** 쓴다. 따라서 한글로 시작하는 프롬프트가 CP949 환경에서도, BOM 있는 UTF-8 콘솔 환경에서도 정확히 같은 바이트로 처리기에 도달한다. 콘솔 setter가 던지는 daemon 환경에서는 기본값 ANSI에 preamble이 없어 raw byte 쓰기가 그대로 성립한다 — 안전하게 degrade한다. (2) stdout·stderr 인코딩 설정이 각자의 `try`로 분리돼 한쪽 실패가 다른 쪽을 끌고 내려가지 않고, 실패를 삼키는 대신 log에 남긴다. (3) `scripts/validate.ps1`이 다섯 watcher 스위트를 `| Out-Null`로 삼키지 않고, 실패 시 자식 출력(빈 줄 제외 마지막 80줄)을 그대로 찍는다.
- 진단 가시성 회귀도 함께 고쳤다: 예전에는 CI가 `watcher claim/lease/quarantine protocol tests failed` 한 줄만 남겨 92개 게이트 중 무엇이 왜 깨졌는지 알 수 없었다. 그 수정의 **첫 판은 `fail`·`summary`·`RESULT` 줄만 남기는 필터**였는데, 정작 실패 게이트가 찍은 실측값(`stdin probe: bytes=57 expected=72 …`)을 걸러 버려 같은 날 같은 벽에 두 번 부딪혔다. 실패는 드물고 그 출력은 짧다 — 고르지 않고 다 보여 준다.
- **GAS deployment: 해당 없음.** `916b2e1…..8b98187…`에서 `Code.gs`, `config/public-runtime.json`, Script Properties, credential과 production data를 바꾸지 않았다. 다만 `v2.24.0`이 요구하던 Apps Script 재배포는 founder가 2026-08-04에 별도로 실행했다고 보고했다. 이것은 이 release의 산출물이 아니라 앞선 release의 미결 gate이며, **agent가 확인하지 못했다** — `doPost`가 action 라우팅보다 token 검사를 먼저 하므로 무효 token probe로는 배포된 action과 미배포 action을 구분할 수 없고, 유효 owner token은 agent가 쓰지 않는다. `ping`이 build 표식을 돌려주지 않는 한 이 확인은 사람의 폰에서만 가능하다.
- **watcher: 코드는 바뀌었고 운영 watcher는 아직 재기동하지 않았다.** `watcher/CardCapture_Watcher.ps1`과 `watcher/tests/watcher-protocol-tests.ps1`이 변경됐다. 운영 clone `C:\Users\gueco\Kairen_Build_Workspace\card-capture`를 `8b981875d1c190104e956bd8e88b626a44edda71`로 갱신했고 `.ps1`의 UTF-8 BOM이 그대로임을 바이트(`239 187 191`)로 확인했다. **운영 watcher는 현재 중지 상태이며 이 코드로 재기동하지 않았다** — 재기동은 사람의 단계다.
- **PER-000418: 미복구.** requeue하지 않았고 live receipt를 하나도 수정하지 않았다. 증명된 것은 **프롬프트가 처리기에 정확한 UTF-8로 도달한다**는 것이지 **조사가 성공한다**는 것이 아니다. 이 원인 뒤에 다른 원인이 있을 수 있고, 있다면 이제 `v2.24.0`이 넣은 `failure.json` 원인 기록을 남긴다.
- 실기기 판정: **미완료.** 실제 폰에서 아무 동작도 실행하지 않았다. 따라서 이 release evidence는 actual-phone PASS·MVP gate PASS·customer proof가 아니다. MVP build/testability gate comes before customer proof.
- rollback: PR #81 merge commit `8b98187…`을 revert하고 같은 Pages 절차로 재배포한다. 기존 tag를 움직이거나 재사용하지 않는다. 운영 watcher를 재기동하지 않았으므로 watcher rollback은 없고, 서버·credential·data rollback도 없다. 되돌리면 stdin 인코딩 결함이 그대로 돌아온다 — Deep 조사는 다시 100% 실패한다.

### v2.24.0 @ `916b2e1` — 검증 2026-08-04 (agent:kairen.claude, Kairen-Ref: TSK-000537 / INT-000029) — **INT-000029 다섯 lane 통합 · Code.gs·watcher 변경 / GAS 재배포·watcher 교체 미실행 · actual phone NOT RUN**

- repository: annotated release tag `v2.24.0` → merge commit `916b2e1e637f726791f157c9eaf12344ae06d923`(main). 구현 PR은 [#79](https://github.com/guecom/card-capture/pull/79)이고 승인·검증된 exact PR head는 `8fe6b1d75b3a7e2006eb6f6dc12f50b18af4e7cb`다. 기존 tag는 움직이지 않았다. GitHub Release는 draft·prerelease가 아닌 Latest 정식 release로 발행됐다.
- human gate: founder가 2026-08-04 `이 개발 인터뷰 승인안 실행해줘`와 `PR, Merge, Release까지 해줘`로 INT-000029 승인안의 구현, PR, exact-head merge, 새 불변 tag, GitHub Release와 Pages publication을 승인했다. 이 승인은 actual-phone PASS, GAS 재배포, watcher 교체·재기동, `PER-000418` live requeue, credential·Script Property·production data 변경 승인이 **아니다.**
- CI: exact PR head `8fe6b1d…`의 required `validate` run `30840672712` SUCCESS(7m49s). 통합 tree 로컬에서 Vitest 38 files / 496 tests PASS, Chrome E2E 153/153 PASS, repository validator `fail=0 warn=0`(build reproducibility 27 artifact byte-identical 포함), watcher protocol 92/92, watcher health 70/70, prompt-injection 20/20, manual person intake gate PASS. 작업 전 기준선(`main` `f3ccb76`)은 e2e 108/108, unit 411/411, validator `fail=0 warn=0`이었다.
  - lane별 반쪽 상태의 PASS를 통합 판정으로 승격하지 않았다. 다섯 lane을 하나의 통합 branch로 합친 뒤 브라우저 게이트를 포함한 전체 게이트를 **한 번에** 돌렸다.
- 새 게이트의 수정 전 FAIL 확인: 이번에 추가한 게이트는 전부 **수정 전 build에서 실제로 FAIL하는 것을 먼저 확인**했다. 설정 가독성 sweep **42건 FAIL**(`build-line` 10px와 여러 `<small>` 10px — 원인은 Ionic 전역 `small { font-size: 75% }`) → 71개 요소 sweep에 findings 0. `int29-manual.spec.ts` **11/11 FAIL**(`직접 입력` 버튼 부재) → 11/11 PASS. `int29-research.spec.ts` **15/15 FAIL**(`촬영 탭 작성 자리에 아직 진행 막대가 있다`) → 15/15 PASS. watcher protocol 신규 26 게이트 중 **20 FAIL** → 92/92. health G21 **6 FAIL** → 70/70.
- Pages: live 파일과 merge commit 파일이 **참조된 24개 전부** 일치한다(entry HTML·service worker 2종·active JS·CSS와 참조된 모든 vendor chunk). 주요 값 — `docs/index.html` `90ac1ad2077d21eb6b1d5779dc8d7f8af039aa473b2dd4cac071404192a5ff1d`, `docs/sw.js` `5fc49d5dc741a2dc50ff33770dd452790578da1dcac7731b176c324d32ea3221`, `docs/next/index.html` `d91ad87e87452dbdbf2a2fef2a662580a3c5edde24f72c1251518a45ba6955f8`, `docs/next/sw.js` `00b276df2aded46d02323f2e793593fc9eb0fbf110f4e4f0dc78cd8622b35093`, active JS `docs/next/assets/index-C9X4vI-t.js` `58409744cb2d645dfeb9a6d0ff18950461a4a424201ab2ffa563b2b154618959`, CSS `docs/next/assets/index-BkLkkMLX.css` `91e86ceb4873d4ccdeee2b8e5fe721adf221f41454297922fc7aa7e648985bfc`. live active JS가 앱 버전 `2.24.0`과 build id `src-e213f8fd37df`를 포함한다.
  - 측정 주의: v2.23.0에서와 같이 원시 바이트로 내려받아 CRLF만 정규화해 비교했다. 문자열을 경유한 비교를 release 판정 근거로 쓰지 않는다.
  - live bundle 안에서 이번 승인 동작 네 가지의 흔적을 직접 확인했다 — `직접 입력`, `모두 선택`, `버그 리포트`, `복구 필요`. merge 자체를 release 증거로 계산하지 않는다.
- 이번 변경의 machine-observable behavior: (1) Deep 조사의 정상 실패가 비민감 `failure.json`을 남기고 `begin.json`·rollback backup을 함께 닫아 false interrupted recovery를 만들지 않으며, legacy orphan marker는 reconciliation 대상이 되고, 같은 방식으로 `MaxAttempts * 2`회 실패한 receipt는 requeue 수용을 중단하고 `복구 필요`로 잠기며 health가 그 상태를 노출한다. (2) 설정의 계정·연결이 `고급 설정` depth 없이 한 sheet에서 편집되고, 알림·캡처 문구가 action language로 바뀌고, 설명형 도움말 대신 큰 버전·빌드와 자동 발송하지 않는 `mailto:` 버그 리포트(+clipboard fallback)가 보인다. (3) 캡처 탭에 `명함 촬영`과 같은 위계의 `직접 입력`이 생겨 2,000자 이하 자연어 한 칸을 확인 modal 없이 접수하고 `manual_person`·`user-provided`·image-free로 남는다. (4) AI 조사 요청 composer가 processing 진행 표시를 잃고 canonical 명함 기록·진행 block이 실제 progress를 단독 소유하며 제출 성공은 그 block으로 focus를 옮긴다. (5) 조사 범위 block 9종이 tri-state `모두 선택`·`모두 해제`와 선택 개수 표시를 갖고 자유 입력을 덮어쓰지 않으며 기존 2시간 sticky capture context에 유지된다.
- 안전 경계: 버그 리포트 메일 본문에 token, 개인 링크 코드, `apiUrl`과 Person 데이터가 **도달할 수 없음**을 negative test로 고정했다. 운영 endpoint field의 read-only 계약(`canEditApiEndpoint`)은 바꾸지 않았다. 직접 입력의 식별 근거는 merge 권한의 근거이므로 **서버에서 다시 도출**하며 client가 위조해 보낸 `identityEvidence`는 폐기된다. 멱등성은 기존 사진 업로드의 capture id 방식을 재사용해 두 번째 멱등 장치를 만들지 않았다. 조사 permission 계약은 넓히지 않았고, 모두 선택 상태의 지시문이 `public-research-v1` 아래 `riskFlags: []`를 낳는다는 unit test로 그것을 고정했다. 안면 촬영·biometric matching은 명시적 non-goal이다.
- **GAS deployment: 필요하지만 미실행.** `f3ccb76…..8fe6b1d…`에서 `Code.gs`(action `manualperson`과 closed-enum `recovery` 투영, 잠긴 receipt requeue 거절)와 `watcher/`(`CardCapture_Watcher.ps1`·`CardCapture_Health.ps1`과 그 테스트)가 바뀌었고 `PROCESSING_CONTRACT.md`가 staging state machine을 문서화했다. `config/public-runtime.json`, Script Properties, credential과 production data는 바꾸지 않았다. **Apps Script 재배포와 운영 watcher 교체·재기동을 실행하지 않았으므로, 이 release의 서버·watcher 동작은 live에서 증명되지 않았다.**
- **PER-000418: 미복구.** live receipt의 현재 모양을 구조 metadata만 읽어 확인했다 — `attempts: 3`, `quarantined: true`, staging에 291바이트 `begin.json` 하나뿐이고 backup 없음. 읽기만 했고 운영 watcher와 live receipt를 하나도 바꾸지 않았으며 requeue하지 않았다. 최초 실패의 운영 1차 원인은 processor raw log가 retention을 지나 사라져 확정하지 못했고, 이전에 제한된 진단 환경에서 관측된 socket 10013은 **원인으로 확정되지 않았다.** 이번에 증명된 것은 과거 실패의 원인이 아니라 다음 실패가 durable한 비민감 원인 기록을 남긴다는 것이다.
- 실기기 판정: **미완료.** 실제 폰에서 직접 입력 접수, 모두 선택, 진행 소유권 이동, 설정 findability와 버그 리포트 전송을 하나도 실행하지 않았다. founder의 exact live build 판정도 없다. 따라서 이 release evidence는 actual-phone PASS·MVP gate PASS·customer proof가 아니다. MVP build/testability gate comes before customer proof.
- rollback: PR #79 merge commit `916b2e1…`을 revert하고 같은 Pages 절차로 재배포한다. 기존 tag를 움직이거나 재사용하지 않는다. GAS 재배포와 watcher 교체를 하지 않았으므로 서버·watcher·credential·data rollback은 없다.

### v2.23.0 @ `3b8ffb6` — 검증 2026-08-03 (agent:kairen.claude, Kairen-Ref: TSK-000529 / ISS-000230) — **v2.19 기준선 위 선별 재적용 · Code.gs·watcher 무변경 · actual phone NOT RUN**

- repository: annotated release tag `v2.23.0` → merge commit `3b8ffb65e002203b7c94503e59496612c9c8978b`(main). 구현 PR은 [#77](https://github.com/guecom/card-capture/pull/77)이고 승인·검증된 exact PR head는 `35128e9adfba83288efe594ff1d6f320f1fa9259`다. 기존 tag는 움직이지 않았다. GitHub Release는 draft·prerelease가 아닌 Latest 정식 release로 발행됐다.
- human gate: founder가 2026-08-03 `되돌렸었는데, 다음 이슈 해결한 것만 다시 적용해서 release까지 해줘. ISS 50,45,217,110`이라고 지시해 선별 재적용, PR, exact-head merge, 새 불변 tag, GitHub Release와 Pages publication을 승인했다. 이 승인은 actual-phone PASS, GAS 재배포, watcher 재기동, credential·Script Property·production data 변경 승인이 **아니다.**
- CI: exact PR head `35128e9…`의 required `Validate` run `30803105127` SUCCESS(6m51s). 로컬 exact-head에서 Vitest 35 files / 411 tests PASS, Chrome E2E 108/108 PASS, `tsc -b` clean, repository validator `fail=0 warn=0`(build reproducibility 27 artifact byte-identical 포함), Pages source identity `src-06f1a06699f5`.
- Pages: live 파일과 merge commit 파일의 LF-normalized SHA-256이 **24개 전부** 일치한다(entry HTML·service worker 2종·active JS·CSS와 참조된 모든 vendor chunk). 주요 값 — `docs/index.html` `90ac1ad2077d21eb6b1d5779dc8d7f8af039aa473b2dd4cac071404192a5ff1d`, `docs/sw.js` `5fc49d5dc741a2dc50ff33770dd452790578da1dcac7731b176c324d32ea3221`, `docs/next/index.html` `a75166e71a21d9729c2b5a2a773192a5d06ed709c8516677afe5c3a562547837`, `docs/next/sw.js` `058472a9e624d3b380ff1c95c142a35bd50fa08331b7c948e4970da2722bc6a4`, active JS `docs/next/assets/index-Bvin7mfJ.js` `a10aa6de0a42d0818e35a81abf007f3861f772da189d05a59f7140e1773b2ab0`, CSS `docs/next/assets/index-KIQkJ6Qc.css` `4d395bf7abc3761e03819f2c4ecc5366b022fd9eeddc7c73173e10ce57e8dd68`. live active JS가 앱 버전 `2.23.0`과 build id `src-06f1a06699f5`를 포함한다.
  - 측정 주의: 첫 대조에서 HTML·CSS는 일치하고 JS만 전부 불일치로 나왔는데 원인은 배포가 아니라 **측정**이었다. PowerShell이 `application/javascript` 응답을 HTML·CSS와 다른 charset으로 디코드해 문자열 단계에서 값이 달라졌다. 원시 바이트로 내려받아 CRLF만 정규화해 다시 재면 24/24가 일치한다. 문자열을 경유한 비교를 release 판정 근거로 쓰지 않는다.
- 이번 변경의 machine-observable behavior: `v2.22.0`이 되돌린 `v2.19` 시각 기준선을 유지한 채 ISS-000045·050·217의 승인 행동만 다시 올렸다. 관측 소요시간 기반 단계 폭(표본 부족 시 균등 + 그 사실을 문장으로), 지어낸 잔여 ETA 제거, 세대 판정 기반 단일 갱신과 `방금 업데이트`/`갱신 실패 · 다시 시도`, 대기 중 상시 재처리 행동, Web Push client 구독·해지와 3종 event, 알림 focus 소비 후 주소창 정리, 복구 안내, 연결 해제 시 구독 해제, 설정 six-job IA와 `화면 움직임` preference 제거(OS reduced-motion은 CSS로 계속 존중)를 포함한다. v2.20/v2.21의 화면 개편과 Deep Research 표면은 되살리지 않았다.
- 보안 회귀 게이트 복원: ISS-000110의 Cache Storage 자격 정보 누출을 지키던 `frontend/e2e/sw-credential-cache.spec.ts`가 `v2.20.0`(`3e181a6`)에서 삭제된 뒤 v2.22 롤백이 복원하지 않아 **코드만 고쳐진 채 게이트가 없는 상태**였다. `credential-boundary`의 캐시 단언은 `?k=` navigation만 수행하는데 후보 worker가 navigation을 캐시하지 않아 결함 유무와 무관하게 통과하는 공허한 단언이었다. 은퇴한 legacy 부팅 경로만 바꿔 게이트를 복원했고, `docs/sw.js`의 `cacheKeyFor()`를 원래 결함으로 되돌려 넣어 **실제로 FAIL하는 것을 먼저 확인**한 뒤 복구했다. legacy 은퇴 검사도 `docs/next/index.html`·`docs/next/sw.js`까지 넓혔다(e2e·validator 둘 다 루트만 보고 있었다).
- **GAS deployment·watcher: 해당 없음.** `77be34a…..3b8ffb6…`에서 `Code.gs`, `watcher/`, Web Push credential·registry, `config/public-runtime.json`, Script Properties와 production data를 바꾸지 않았다. Web Push는 서버가 이미 활성화된 상태에서 **client 코드만** 복원했고 client가 기존 서버 계약에 맞췄다. GAS 재배포와 watcher 재기동을 실행하지 않았다.
- 실기기 판정: **미완료.** Android opt-in → 닫힌 앱 수신 → same-origin 열기 → revoke, 설정 findability, reduced-motion과 founder의 exact live build 판정을 실행하지 않았다. 따라서 이 release evidence는 actual-phone PASS·MVP gate PASS·customer proof가 아니다. MVP build/testability gate comes before customer proof.
- rollback: PR #77 merge commit `3b8ffb6…`을 revert하고 같은 Pages 절차로 재배포한다. 기존 tag를 움직이거나 재사용하지 않는다. 서버·watcher·credential·data rollback은 없다.

### v2.22.0 @ `8c69f78` — 검증 2026-08-03 (agent:kairen.codex, Kairen-Ref: TSK-000525 / ISS-000225) — **v2.19 frontend rollback · Code.gs·watcher 무변경 · actual phone NOT RUN**

- repository: annotated release tag `v2.22.0` → merge commit `8c69f785faa92646b8fd7c98852a996258c3dd5b`(main). 롤백 PR은 [#75](https://github.com/guecom/card-capture/pull/75)이고 승인·검증된 exact PR head는 `e25bac7e1a2e56d043ab0811f3160f38e06531a3`다. 기존 `v2.19.0` tag는 움직이지 않았다. GitHub Release는 draft·prerelease가 아닌 Latest 정식 release로 발행됐다.
- human gate: founder가 2026-08-03 `카이렌 카드 캡처 v2.19로 되돌려서 릴리즈해줘.`라고 지시해 frontend rollback, PR, exact-head merge, 새 불변 tag, GitHub Release와 Pages publication을 승인했다. 이 승인은 actual-phone PASS, GAS 재배포, watcher 재기동, credential·Script Property·production data 변경 승인이 아니다.
- CI: exact PR head `e25bac7…`의 required `Validate` run `30796519000` SUCCESS — Vitest 31 files / 337 tests PASS, Chrome E2E 101/101 PASS, repository validator `fail=0 warn=0`, Pages source identity `src-2772a6bd35b7`. 로컬 exact-head build·typecheck와 `git diff --check`도 PASS했다.
- independent review: 첫 head는 은퇴한 `legacy.html`을 가리키는 Settings 링크와 이를 놓치는 회귀 gate를 P1/P2로 찾아 FAIL했다. 최종 head는 링크와 dead CSS를 제거하고 `App.tsx`와 committed JS/CSS asset을 gate가 함께 읽도록 보강했다. exact-head 재검토는 P0/P1/P2 finding 0으로 PASS했다.
- Pages: live 파일과 merge commit 파일의 LF-normalized SHA-256이 일치한다 — `docs/index.html` `90ac1ad2077d21eb6b1d5779dc8d7f8af039aa473b2dd4cac071404192a5ff1d`, `docs/sw.js` `5fc49d5dc741a2dc50ff33770dd452790578da1dcac7731b176c324d32ea3221`, `docs/next/index.html` `2e99cbae42ace40a5d2a714b52bd28d5363f97e90b29c27768dc4d881729d28e`, `docs/next/sw.js` `00e05f548f44de4308b474b7cc78f811093812d4002d7153d6b64f207129d140`, active JS `docs/next/assets/index-C9tMNOSf.js` `2b9cd322ce9a3a9acc73f24115ece3639c8654f252594278b794f095e525b448`, CSS `docs/next/assets/index-BbEYIhC3.css` `6121eaf14a75dc0e644a6e0d190a811715f3471495cdcfa5b68326c5bc005fe3`. live active JS가 앱 버전 `2.22.0`을 포함한다.
- 이번 변경의 machine-observable behavior: 사용자-facing frontend source·test·Pages artifact를 `v2.19.0`의 검증된 카메라·캡처·검색·상세 동작 기준으로 복원했다. v2.20/v2.21에서 추가된 비동기 조사·Push 설정·진행 telemetry UI는 current frontend에서 제거했지만 current public-runtime endpoint와 legacy-retirement 보안 경계는 유지했다.
- **GAS deployment·watcher: 해당 없음.** `224531db…..8c69f78…`에서 `Code.gs`, `watcher/`, Web Push config/credential, `config/public-runtime.json`, Script Properties와 production data를 바꾸지 않았다. GAS 재배포와 watcher 재기동을 실행하지 않았고 현재 운영 상태를 이번 frontend rollback의 새 PASS로 재분류하지 않는다.
- 실기기 판정: **미완료.** founder phone에서 steady capture, 손떨림·급이동 reject, 실제 crop, owner-token read/write와 Android Push 회귀를 다시 실행하지 않았다. 따라서 이 release evidence는 actual-phone PASS·MVP gate PASS·customer proof가 아니다. MVP build/testability gate comes before customer proof.
- rollback: PR #75 merge commit `8c69f78…`을 revert하고 같은 Pages 절차로 재배포한다. 기존 tag를 움직이거나 재사용하지 않는다. 서버·watcher·credential·data rollback은 없다.

### v2.20.0 @ `f85f737` — 검증 2026-08-02 (agent:kairen.codex, Kairen-Ref: TSK-000496 / INT-000025) — **Code.gs·watcher 변경 / Web Push ON · Android NOT RUN**

- repository: release tag `v2.20.0` → merge commit `f85f737c4920ac7ce4bdc2f58f7fa847522af0e5`(main). 구현 PR은 [#71](https://github.com/guecom/card-capture/pull/71)이고 승인·검증된 exact PR head는 `44c083221d7f7b45180eb217c31abf343511bd41`다. GitHub Release는 draft·prerelease가 아닌 정식 release로 발행됐다.
- human gate: founder가 INT-000025의 ISS-000218 제외 전체 구현과 `PR, Merge, Release`, 이어 release·deployment까지 승인했다. 이 승인은 repository merge, tag, Pages, 기존 GAS deployment 갱신과 watcher 재기동을 포함한다. 이후 founder가 DPAPI에 저장된 sender token을 GAS `PUSH_SENDER_TOKEN`에 전달·저장하고 `PUSH_NOTIFICATIONS_ENABLED=true`로 활성화하는 정확한 범위를 별도로 승인했다. **이 추가 승인을 Android live Push 결과 승인·검증으로 해석하지 않는다.**
- CI: exact PR head `44c0832…`의 `validate` run `30744478652` SUCCESS(6m53s) — 최종 repository validator `fail=0 warn=0`. 구현자와 분리된 delivery-contract·security review도 exact head에서 P0/P1 finding 0으로 종료됐다.
- Pages: source `main:/docs`, build `1128186017`, build commit `f85f737…`, status `built`. live 파일과 merge commit 파일의 LF-normalized SHA-256이 일치한다 — `docs/index.html` `90ac1ad2077d21eb6b1d5779dc8d7f8af039aa473b2dd4cac071404192a5ff1d`, `docs/sw.js` `5fc49d5dc741a2dc50ff33770dd452790578da1dcac7731b176c324d32ea3221`, `docs/next/index.html` `e1e112b10ec9de37088cf88bb7a367a863c95c2e0c8243374a39b901947da2e0`, `docs/next/sw.js` `6e9a97fc8fb087884e60d9f2f87932a14fd399fd1beccffeb84be10ed0db40b9`, active JS `docs/next/assets/index-DN0UUnJN.js` `4ee286f0f237b472db0998d8858e81f7f85269c78228c719533df022ae7cd756`, CSS `docs/next/assets/index-Dr4c27A3.css` `cc0676fa3568b7000554b61648736d08e11d82d470cc9114ed675dc2c1ccb260`. 배포 앱 버전은 `2.20.0`이다.
- 이번 변경의 machine-observable behavior: 사용자 우선순위 수동 새로고침과 자동 갱신의 enabled·진행·성공·실패 상태, 6개 작업 설정 IA, OS reduced-motion·도움말·version, 조사 전체/부분/없음 선택과 자유 입력, 목적 제한 Deep Research timeline·evidence graph·fact/conflict/unknown/hypothesis·counterevidence·stop reason, 중단·복구·재시도 UX를 하나의 current UI로 통합했다. legacy UI·precache·MailApp 알림 경로는 은퇴했고 Web Push는 개인정보 없는 `final_result`·`human_input_required`·`recovery_required` 세 event만 허용한다.
- GAS deployment: 기존 deployment와 공개 `/exec` URL을 유지한 채 release `Code.gs`로 **version 7**을 배포했다. 운영 safe probe는 `ping` → `ok:true`, legacy `notify` → `notification_channel_retired`, invalid Push sender → `sender_unauthorized`, 무효 토큰 `whoami`·`list`·`persondoc`·`search`와 POST `researchinstruction` → 각각 `invalid_token`, 미등록 GET action → `unknown_action`다. `DEEP_RESEARCH_ENABLED=true`, `PUSH_NOTIFICATIONS_ENABLED=false`로 먼저 배포했으며, 이후 별도 승인에 따라 코드 재배포 없이 Script Properties의 sender 존재와 `PUSH_NOTIFICATIONS_ENABLED=true`를 확인했다. guest `owner_only`, target mismatch와 유효 owner-token read/write는 secret·운영 데이터 경계 때문에 `NOT RUN — HUMAN/EXTERNAL`이다. raw Script/deployment/registry ID와 token·private key는 이 기록에 남기지 않는다.
- Web Push activation receipt: current-user DPAPI의 sender token을 브라우저 런타임에서 만든 일회성 RSA 공개키로 암호화해 localhost challenge-response 경로로 넘기고 GAS 입력에서만 복호화했다. 평문 token은 repository·vault·임시 파일·도구 출력에 기록하지 않았고 전송 뒤 버퍼와 clipboard를 정리했다. 활성화 전 sender probe는 `feature_disabled`, 활성화 후 승인된 sender probe는 `ok:true`, 존재하지 않는 dummy subject는 inactive·subscription 0, 잘못된 sender는 `sender_unauthorized`였다. 따라서 server flag와 sender 인증 경계는 live PASS다. Android opt-in → 닫힌 앱 수신 → same-origin 열기 → revoke는 `NOT RUN — HUMAN/EXTERNAL`; subscription이 아직 없어 실제 알림 전달 PASS로 승격하지 않는다.
- watcher: 운영 clone HEAD는 `f85f737c4920ac7ce4bdc2f58f7fa847522af0e5`, `CardCapture_Watcher.ps1` SHA-256은 `AF3670F1F64275F8F5B08D2B767D8BC730E1921FCA9F7D0B0566D56CF8128713`, watcher-v3.0 PID 19304다. health exit 0, process identity alive, backlog 0, consecutive failures 0, active claim 0, Push configured=true·pending=0, 마지막 zero-event outcome은 `accepted=0,retired=0,failed=0`이다. 이 health는 Push 발송이나 Android 수신 성공을 뜻하지 않는다.
- processing contract: vault `01_Company/00_Company_Operations/05_Tools_and_Systems/CardCapture_Processing.md` SHA-256 `6F6DC8EDFFC3C7A407381D5E473E83913E884F0422097461CC8FB37E927EC0E2`가 write allowlist, processing order와 bounded Deep Research 실행의 당시 governing contract다.
- 실기기·closure 판정: **미완료.** sender 목적지 승인·저장과 server enable은 완료됐다. Android opt-in·닫힌 앱 수신·tap-open·해제를 TSK-000155에서, private owner/guest·Drive live acceptance를 TSK-000218에서 확인할 때까지 TSK-000496과 관련 live Issue는 Blocked/In Progress를 유지한다. 이 release evidence는 Task Done·MVP gate PASS·customer proof가 아니다. MVP build/testability gate comes before customer proof.
- rollback: 먼저 `PUSH_NOTIFICATIONS_ENABLED=false`로 외부 발송을 닫고, 서버는 이전 GAS deployment version, repository·Pages는 PR #71 merge revert, watcher는 이전 검증 commit으로 복원한다. private registry와 durable outbox는 원인 검토 전 삭제하지 않는다.

### v2.19.0 @ `5e3d8ec` — 검증 2026-08-02 (agent:kairen.codex, Kairen-Ref: TSK-000219 / ISS-000083) — **Code.gs 무변경**

- repository: annotated tag `v2.19.0` → merge commit `5e3d8ec672ab888a6c2c422c11bc0a6f316bb811`(main). 구현 PR은 [#69](https://github.com/guecom/card-capture/pull/69)이고 승인·검증된 exact PR head는 `a1e578ab33e9b38c4aaf9b866b6f7b17cdf39c0f`다.
- human gate: founder가 2026-08-02 자동 촬영 순간 손떨림·급이동 결함의 구현과 release를 지시했다. repository `HUMAN TEST REQUIRED` gate가 exact version의 actual-phone outcome 미확인을 이유로 첫 merge 시도를 차단한 뒤, agent가 실제 폰 steady·손떨림·급이동·crop 품질과 threshold 민감도가 `NOT RUN`임을 고지했고 founder가 `배포 승인`으로 PR merge·`v2.19.0` tag·Pages publication을 명시 승인했다. **이 승인을 actual-phone PASS로 해석하지 않는다.** Apps Script 재배포·watcher 재기동·token·credential·Script Property·운영 데이터 변경은 승인·실행 범위 밖이며 이 release에 필요하지도 않다.
- CI: exact PR head `a1e578a…`의 `validate` run `30730231554` SUCCESS(5m35s) — Vitest 31 files / 337 tests PASS, Chrome E2E 115/115 PASS, repository validator `fail=0 warn=0`, 서로 다른 clock·timezone build 27 artifacts byte-identical. focused capture-flow는 6/6 PASS다.
  - 독립 review는 첫 head에서 auto-off·close 뒤 stale capture가 `onCaptured`까지 갈 수 있는 generation race, manual shutter가 pending auto hold에 막히는 문제와 `requestVideoFrameCallback` 미지원 WebView가 `currentTime`만 보고 같은 frame을 3개로 오인할 수 있는 결함을 찾아 FAIL했다. 최종 head는 stop·off·close·side change 때 capture generation을 무효화하고 freeze·warp·blur·publish 뒤 active generation을 재확인하며, manual shutter가 auto hold를 선점한다. fallback은 `totalVideoFrames - droppedVideoFrames`만 신뢰하고 없으면 fail-closed한다. 최종 exact-head 독립 re-review PASS다.
- Pages: source `main:/docs`, `pages-build-deployment` run `30731294400` SUCCESS, build commit `5e3d8ec…`, status `built`(2026-08-02T03:50:36Z). **live와 merge commit blob이 exact-match** — `docs/index.html` `0b491c9253fc2058a88232403667363115e600ff4807f6997734af9bf76cfe16`, `docs/sw.js` `f171a6c6190a541a53169622053a49a55e84d52b0e554ea0f11729eb48fff5f2`, `docs/next/index.html` `9b4edaddbf92c94ec12f1f623be0bf354ba9118475adcf6f728c00af2a2632b2`, `docs/next/sw.js` `58ca87335dcb4bec02b7fd211cf8c2e354f30b812d065fe6c307daf048e0a075`, active JS `docs/next/assets/index-DH2_HlMv.js` `bc542bfb45f53103db976e37dad4be765c90a7e885888789ed8e252a14028ce8`, active CSS `docs/next/assets/index-0bbMrTSJ.css` `a5c0f17823610739c7c5fa71148dc627699dbbb03cd16ed671c64379a6840fd7`. live HTML·service worker가 같은 active JS를 가리키고 live JS 안의 앱 버전 `2.19.0`을 확인했다.
- 이번 변경의 machine-observable behavior: 자동 gate가 ready가 된 뒤 서로 다른 새 camera frame 3개를 비교해 손떨림·translation을 reject하고, 통과한 마지막 frame을 즉시 freeze한 뒤 실제 저장 후보의 sharpness를 다시 확인한다. motion·blur는 저장 없이 gate를 처음부터 다시 모은다. auto-off·닫기는 pending work를 취소하고 manual shutter는 기다리지 않고 실행된다. frame identity를 신뢰할 수 없는 WebView는 자동 촬영을 보류한다. motion fingerprint와 raw frame은 저장·전송하지 않는다.
- **GAS deployment: 해당 없음.** `v2.18.0..v2.19.0`에서 `Code.gs`, watcher와 dependency lockfile 무변경. watcher 재기동, credential·schema·production-data migration도 없다.
- 실기기 판정: **미완료.** exact version의 steady capture, 손떨림, 급이동, 결과 crop과 threshold 민감도는 founder phone에서 확인하지 않았다. 따라서 TSK-000219·ISS-000083은 In Progress/Open이고 TST-000017은 Running이다. 이 machine·release evidence는 Task Done·MVP gate PASS·customer proof가 아니다. MVP build/testability gate comes before customer proof.
- rollback: PR #69 merge commit `5e3d8ec…`를 revert하고 같은 Pages 절차로 재배포하거나, 긴급 시 마지막 검증 baseline `v2.18.0`을 복원한다. 서버·watcher·data rollback은 없다.

### v2.18.0 @ `125acbb` — 검증 2026-08-02 (agent:kairen.codex, Kairen-Ref: TSK-000244 / ISS-000101) — **Code.gs 무변경**

- repository: annotated tag `v2.18.0` → merge commit `125acbb965d9d910d4c24fea48dc1bbd16db1c23`(main). 담은 merge는 PR #66이고, 승인·검증된 exact PR head는 `d1bad9c61a551028e8788d2e019a1d82117a1dfe`다.
- human gate: founder가 2026-08-01 `Approve`로 implementation을 승인했고 2026-08-02 `배포도해줘`로 PR merge·`v2.18.0` tag·Pages publication을 승인했다. **실폰 80회 matrix를 먼저 수행하지 않고 release하는 잔여 위험은 그대로 표시하며, 이 승인을 actual-phone PASS로 해석하지 않는다.** Apps Script 재배포·watcher 재기동·token·credential·Script Property·운영 데이터 변경은 승인·실행 범위 밖이며 이 릴리즈에 필요하지도 않다.
- CI: exact PR head `d1bad9c…`의 `validate` run `30726258930` SUCCESS — Vitest 30 files / 333 tests PASS, Chrome E2E 110/110 PASS, repository validator `fail=0 warn=0`. local exact-head `scripts/validate.ps1`도 `fail=0 warn=0`이다.
  - 독립 review는 첫 release head에서 OpenCV-only crop 허가·모델/OpenCV 후보 불일치·측정만 하고 단언하지 않는 model test·confidence 없는 후보 교체를 차단 결함으로 잡았다. 수정 head `e18951c…`에서 세 항목은 닫혔지만 낮은-confidence 후보가 lock expiry 뒤 재획득되는 우회가 남아 다시 FAIL했다. 최종 head `d1bad9c…`는 낮은-confidence 후보 12연속 frame에서도 기존 lock을 대체하지 못하고, 실제 verified-frame 부재 4회 뒤에만 신규 3-frame acquisition을 허용하며 독립 re-review PASS했다.
  - model hard-scene gate는 3개 장면을 각각 4회 실행해 detection 12/12, malformed 0, max corner error 4px, max repeated drift 0px, max inference 238ms를 확인했다. 모델 asset을 끊은 browser gate도 자동 촬영·자동 크롭 0, 수동 guide capture 가능으로 PASS했다. live synthetic camera의 표시 중심 오차는 1–2px, crop ratio는 1.67이었다.
- Pages: source `main:/docs`, build commit `125acbb…`, status `built`(2026-08-02T01:09:42Z). **live와 merge commit blob이 exact-match** — `docs/index.html` `0b491c9253fc2058…`, `docs/sw.js` `f171a6c6190a541a…`, `docs/next/index.html` `c8f976cfc3798bb…`, `docs/next/sw.js` `d2047b09ca1b68e…`, active JS `docs/next/assets/index-BB2R91t0.js` `d2f15d8fc4df9b…`. live active JS와 service worker가 같은 entry를 가리키며, 실제 Chrome 설정 화면은 `버전 2.18.0 · 빌드 src-a1906ca0cbd5`를 표시했다.
- 이번 변경의 machine-observable behavior: learned corner model과 OpenCV 후보가 같은 위치·크기·꼭짓점에 동의해야만 overlay·auto-capture·perspective warp로 진행한다. 모델 unavailable/negative/waiting 또는 후보 불일치에는 live quad를 지우고 자동 경로를 닫으며 수동 guide capture를 남긴다. lock 교체에는 confidence margin과 4-frame consensus가 모두 필요하다.
- **GAS deployment: 해당 없음.** `v2.17.0..v2.18.0`에서 `Code.gs` 무변경. Watcher도 무변경·재기동 없음. credential·schema·production-data migration도 없다.
- 실기기 판정: **미완료.** 조건별 10회·총 80회 actual-phone matrix, malformed 표시 0, usable stable lock 90% 이상, lock-time p95 2초 이하, 실제 crop 육안 판정은 다음 founder 폰 검증에 남아 있다. 따라서 TSK-000244와 ISS-000101은 In Progress이며, 이 machine·release evidence는 Task Done·MVP gate PASS·customer proof가 아니다. MVP build/testability gate comes before customer proof.
- rollback: PR #66 merge commit `125acbb…`를 revert하고 같은 Pages 절차로 재배포하거나, 긴급 시 마지막 검증 baseline `v2.17.0`을 복원한다. 서버·watcher·data rollback은 없다.

### v2.17.0 @ `09087c9` — 검증 2026-07-30 (agent:kairen.codex, Kairen-Ref: TSK-000161 / ISS-000050) — **Code.gs 무변경**

- repository: annotated tag `v2.17.0` → squash merge commit `09087c96ac45beb0642b39d0904558857b257a56`(main). 담은 merge는 PR #64이고, 승인·검증된 exact PR head는 `e01c32b1f73759299d018052ea18a71651b9de52`다.
- human gate: founder가 2026-07-30 세션에서 active card 상태를 더 빠르게 반영하는 exact proposal과 `PR, Merge, Release`를 승인했다. 이어 실제 폰 사전 검증 없이 먼저 merge·release하고 배포 직후 폰으로 검증하는 잔여 위험도 명시적으로 승인했다. **Apps Script 재배포·워처 재기동·토큰 변경·운영 데이터 쓰기는 승인·실행 범위 밖이며 이 릴리즈에 필요하지도 않다.**
- CI: exact PR head `e01c32b…`에서 `validate` SUCCESS — run `30507448329`, unit 315 PASS, Playwright 107 PASS, repository validator `fail=0 warn=0`. 독립 code/test review도 exact head에서 PASS했다.
  - 앞선 `04c4146…` run `30506771305`는 기존 sending UI E2E의 준비 타이밍 경합 1건을 드러냈다. 제품 코드와 `docs/next`는 그대로 둔 test-only commit `e01c32b…`에서 새 upload 시작을 명시적으로 기다리도록 고쳤고, 해당 race를 2-worker 반복 20/20과 전체 107/107로 재검증했다.
- Pages: merge SHA의 `pages-build-deployment` run `30508457398` SUCCESS. **live와 merge commit blob이 exact-match** — `docs/index.html` `b6d7deb5e3978048…`, `docs/sw.js` `a1f531d6c4cd35a…`, `docs/next/index.html` `25744723050a01e…`, `docs/next/sw.js` `d3a41f5abf62193…`, active JS `docs/next/assets/index-BYsAm3kj.js` `52a8b625d2fe570…`. live active JS 안의 앱 버전 `2.17.0`도 확인했다.
- 이번 릴리즈의 machine-observable behavior: 화면에 보이는 active card는 4초 cadence, active card가 없으면 20초 cadence, 숨김 상태에서는 poll 중지, foreground 복귀 시 즉시 refresh, list refresh single-flight, mutation overlap 시 trailing refresh, token/session 교체 뒤 이전 응답 차단을 회귀 게이트로 고정했다.
- **GAS deployment: 해당 없음.** `Code.gs` 무변경. Watcher: 무변경·재기동 없음. 인증·schema·dependency·운영 데이터 migration도 없다.
- 실기기 판정: **미완료.** founder가 사전 검증 없는 release 위험을 승인했으며, 다음 실제 폰 사용에서 active card/research가 수동 새로고침 없이 완료 상태로 바뀌는지와 background 복귀 즉시 갱신을 확인한다. 이 machine·release evidence를 customer proof로 승격하지 않는다.
- rollback: PR #64의 squash merge commit `09087c9…` 하나를 revert하고 같은 Pages 절차로 재배포한다. 서버·watcher·data rollback은 없다.

> **기록 공백 (정직한 경계):** GitHub의 `v2.16.0` tag와 Release는 존재하지만 이 파일에는 당시 Verified Baseline이 없다. 현재 상태에서 당시의 사람 게이트와 live 검증을 사후에 지어내지 않으며, 이 v2.17.0 기록은 그 공백을 메웠다고 주장하지 않는다.

### v2.15.0 @ `49017d2` — 검증 2026-07-28 (agent:kairen.claude, Kairen-Ref: TSK-000326 / ISS-000129) — **Code.gs 무변경**

- repository: annotated tag `v2.15.0` → merge commit `49017d2`(main). 담은 merge는 PR #58(움직임 설정·서체)·#59(Kairen-Ref 정정·캐시 게이트 안정화)·#60(버전 확정)이다.
- human gate: founder가 2026-07-28 세션에서 실기기 캡처와 함께 두 항목을 지시하고 `PR, Merge, Release`를 승인했다. **Apps Script 재배포·워처 재기동은 승인 범위 밖이며 이 릴리즈에 필요하지도 않다.**
- CI: `validate` SUCCESS — run `30297004229`(release PR), `30296480719`(기능 PR, **첫 시도 통과**). unit 311 PASS, Playwright 97 PASS, repository validator `fail=0 warn=0`.
- Pages: **live와 commit blob이 exact-match** — `docs/index.html` `0b491c9253fc2058…`, `docs/sw.js` `f171a6c6190a541a…`, `docs/next/index.html` `69792e702eb6a07e…`, `docs/next/sw.js` `0f3fa9393759784c…`, `docs/legacy.html` `e1a3a46c4b79639f…`.
- 이번 변경이 실제로 나갔는지는 **배포본을 열어** 확인했다(정적 해시만으로는 동작을 증명하지 못한다):
  - 폰이 움직임을 줄이지 않는 상태 → `data-motion="on"`, 줄이는 상태 → `data-motion="off"`. 두 경우 모두 서체 `Pretendard Variable`이 `loaded`.
  - 설정 화면이 멈춘 이유를 문장으로 말한다("이 폰이 움직임 최소화를 켜 두어서 …").
  - 서체 자산 `next/assets/pretendard-variable-korean-DXHPKs4Y.woff2`(sha256 `58f28b6566154838…`)가 live에서 200으로 응답한다. **SHELL에는 없다** — 의도된 제외이고, 못 받으면 시스템 서체로 그려질 뿐이다.
- **GAS deployment: 해당 없음.** `Code.gs` 무변경. Watcher: 무변경.
- 실기기 판정: **미완료.** 이번 1번 항목은 founder의 폰이 실제로 움직임 최소화를 켜고 있는지에 따라 체감이 갈린다 — 설정의 `화면 움직임` 카드가 그 답을 바로 보여 준다.
- 정직한 경계: 이 저장소 e2e는 부하에 따라 간헐 실패하는 성질이 있다. `main`에서 전체 스윕을 연속 두 번 돌려 1회차 `queue-truth:172` 실패·2회차 통과를 실측했다 — **이 릴리즈가 만든 성질이 아니다.** 이번에 원인이 특정된 `sw-credential-cache` 경합(주소창 정리 `replaceState`가 진행 중인 `goto`를 끊음)은 고쳤다.

> **기록 공백 (정직한 경계):** `v2.10.0`·`v2.11.0`·`v2.12.0`·`v2.13.0`은 tag가 존재하지만 여기에 baseline이 없다. 절차 6단계가 실행되지 않았다. **사후에 지어내지 않는다** — 그때의 Pages live 콘텐츠·워처 상태·사람 게이트 시점을 지금은 확인할 수 없기 때문이다. 각 릴리즈의 내용은 `CHANGELOG.md`와 vault Task에 남아 있다. 이 공백은 `ISS-000123`(배포된 앱이 자기 버전을 틀리게 말함)과 같은 뿌리다 — **릴리즈 기록과 실제 상태가 따로 놀았다.**

### v2.14.0 @ `0380b9e` — 검증 2026-07-28 (agent:kairen.claude, Kairen-Ref: TSK-000312 / ISS-000119) — **Code.gs 무변경**

- repository: annotated tag `v2.14.0` → merge commit `0380b9e`(main). 담은 merge는 PR #52(UI 변경)와 PR #54(버전 표기·CHANGELOG 구간 정리) 둘이다. lane은 둘 다 `HUMAN TEST REQUIRED`.
- human gate: founder가 2026-07-28 세션에서 여섯 항목의 관찰을 전달하고 `PR, Merge, Release`를 지시했다. **Apps Script 재배포·워처 재기동·토큰 변경·실데이터 삭제는 승인 범위 밖이며 이 릴리즈에 필요하지도 않다.**
- CI: `validate` SUCCESS — run `30287039169`. unit 311 PASS, Playwright 95 PASS, repository validator `fail=0 warn=0`. 신규 게이트 `frontend/e2e/surface-polish.spec.ts` 5건 등록.
  - **정직한 경계**: 이 run은 재실행 2회 뒤 성공했다. `offline-shell.spec.ts:134`(큐 배출 5초 폴링 초과)와 `credential-boundary.spec.ts:294`(readonly를 벗기는 테스트 준비 단계가 React 재렌더와 경합)가 러너 부하에 따라 간헐 실패한다. 둘 다 로컬에서 `--repeat-each 3` 통과하고 이 릴리즈의 변경과 무관한 **테스트 쪽 타이밍 경합**이지만, 게이트가 불안정하다는 사실 자체는 기록해 둔다.
- Pages: **live와 commit blob이 exact-match** — `docs/index.html` `0b491c9253fc2058…`, `docs/sw.js` `f171a6c6190a541a…`, `docs/next/index.html` `543bff7e3a315e1c…`, `docs/next/sw.js` `ce3e16e8f528be53…`, `docs/legacy.html` `e1a3a46c4b79639f…`. 이번 변경이 실제로 나갔는지는 앱이 부르는 스타일시트로 따로 확인했다 — `docs/next/assets/index-DQj-IU3x.css` `91ed24e52bff9de8…`, 그 안에 `--cc-accent` 두 테마 값과 `cc-ai-sweep`이 들어 있다.
  - Windows 체크아웃에서 working copy를 그대로 해싱하면 CRLF 때문에 전부 DIFF로 보인다. 위 값은 **commit blob**(`git show HEAD:docs/...`) 기준이다.
- **GAS deployment: 해당 없음.** `Code.gs` 무변경. 이전 baseline의 상태와 확인 해시가 그대로 유효하다.
- Watcher: 이 릴리즈의 diff는 `watcher/`를 바꾸지 않는다. **그러나 v2.10.0부터 열려 있던 재기동 human gate가 이 시점에 해소됐다** — 운영 클론 `watcher/CardCapture_Watcher.ps1`이 저장소와 byte 동일(`525e23340ef3…`)이고, 그 파일이 2026-07-28 01:12:20에 갱신된 **뒤** 01:38:37에 프로세스(PID 50892)가 새로 떴다. 즉 지금 실행 중인 것이 v2.11.0·v2.12.0의 하드닝된 코드다(명함 내용 로그 분리, 프롬프트 경계 하드닝, 지문 미상 시 격리). `eval/prompt-injection.test.js`의 `PENDING_DEPLOY_LIVE_SURFACE_SHA256` 선언을 걷고 엄격 동일성으로 복귀시켰다.
  - **PID 세기 함정**: 워처 프로세스를 세는 진단 명령이 `CommandLine -like '*CardCapture_Watcher*'`로 거르는데 **그 명령 자신이 그 문자열을 갖고 있어 자기를 잡는다.** 이 세션에서 두 번 났다(PID 47728, 55484). `$PID`와 부모를 제외해야 실제 워처가 **하나**임이 드러난다.
  - health 실측(사람이 샌드박스 밖 PowerShell 창에서 확인): `pid 50892` · `startedAt 2026-07-28 01:38:37` · `lastHeartbeat 2026-07-28 02:43:49` · `backlogCount 0` · `consecutiveFailures 0` · **`logDroppedPending 0` · `logDroppedTotal 0`**. 그리고 JSON에 `workerId`·`activeClaims`·`quarantinedCount`·`interruptedCount`·`logDropped*` 필드가 있다는 것 자체가 **v2.12.0 하드닝 코드가 실행 중이라는 직접 증거**다. 로그 쓰기는 한 줄도 실패하지 않았다.
  - **관측 함정 — 에이전트가 `%LOCALAPPDATA%`를 믿으면 안 된다.** 이 릴리즈를 검증한 에이전트는 AppContainer 샌드박스에서 돌아 `%LOCALAPPDATA%\CardCapture\`가 `…\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Local\…`로 리다이렉트된다. 실측: 에이전트가 본 `watcher.log`는 4,609 B / 01:28:52, 실제 파일은 **2,963,372 B / 02:38:49**. 에이전트가 **쓴 적 없는** `watcher-health.json`조차 456 B / 07-26 16:26으로 어긋났다(실제 633 B / 02:43:49). 디렉터리가 한 번 가상화되면 **읽기 전반이 실제와 달라진다.** 그 경로를 근거로 워처 상태를 단정하기 전에 리다이렉트 경로와 크기·시각을 대조해야 한다.
  - 이 함정 때문에 **이 세션의 워처 로그 PII 정리는 실제 파일에 적용되지 않았다** — 지운 것은 샌드박스 사본이다. 실제 `watcher.log`에 명함 내용 약 2.9 MB가 남아 있다. `ISS-000127`(P1). 새로 쌓이지는 않는다: 재기동 이후 줄은 하드닝된 워처의 구조화 이벤트뿐이다.
  - 위 재기동 판정 자체는 유효하다 — 근거로 쓴 워처 파일 해시와 프로세스 시작 시각은 `Kairen_Build_Workspace` 경로와 프로세스 목록에서 얻었고, 그 경로는 리다이렉트되지 않는다(같은 경로에서 git push·PR merge가 정상 동작했다).
- 실기기 판정: **미완료.** 이 릴리즈는 시각 변경이 본체이고 machine gate는 렌더된 픽셀까지만 증명한다. founder의 실폰 확인이 남아 있다.

### v2.9.0 @ `5f90f1b` — 검증 2026-07-27 (agent:kairen.claude, Kairen-Ref: TSK-000288·289·290) — **Code.gs 무변경**

- repository: annotated tag `v2.9.0` → squash merge commit `5f90f1b`. 담은 merge는 PR #47 하나이며 lane 브랜치 3개(`agent/w4-a1-queue-lock`·`agent/w4-a2-subject-ctx`·`agent/w4-a3-repro-build`)가 통합돼 있다.
- human gate: 이전 릴리즈와 동일한 founder 사전 승인. **Apps Script 재배포·워처 재기동·토큰 변경·실데이터 삭제는 승인 범위 밖.**
- CI: `validate` SUCCESS — run `30250321385`. unit 300 PASS, Playwright 72 PASS, repository validator `fail=0 warn=0`. 신규 게이트 `eval/build-reproducibility.test.js` 등록(~1.7s).
- Pages: **live와 commit blob이 exact-match** — `docs/index.html` `0b491c9253fc2058…`, `docs/sw.js` `f171a6c6190a541a…`, `docs/legacy.html` `f176fef409394ee5…`.
- **GAS deployment: 해당 없음.** `Code.gs` 무변경. v2.5.0의 `PENDING`이 그대로 유효하고 확인 해시도 `f9a78d9a…` 그대로다.
- **watcher: 해당 없음.** 이 릴리즈는 `watcher/`를 바꾸지 않았다. 재기동 `PENDING` 유지 — 그리고 live 워처는 여전히 살아 있지 않다(하트비트 2026-07-26 16:26 이후 없음, 실제 워처 프로세스는 PID 18108 **하나**).
- 닫은 것:
  - `ISS-000112` — 만남 맥락(`event`·`relSelf`·`relKairen`·`research`·`stickyAt`)이 `PRIVATE_KEYS` 열거에 없어 subject 격리 밖이었다. **화면 노출로 끝나지 않았다** — `App.tsx:338→341→874` 경로로 sticky 값이 `buildQueuedCapture`에 들어가 **owner의 관계 메모가 guest의 캡처로 서버에 기록**됐다. 함께 확인된 두 번째 결함: legacy 전역 키를 현재 subject로 **이관**하던 동작. `docs/legacy.html`이 그 네 키를 지금도 쓰므로 상속 창이 계속 열리고, `App.tsx:304`가 서버 응답 전에 캐시된 owner 플래그로 UI를 켜므로 오프라인 구간 내내 guest에게 owner 화면이 뜬다. 이제 어느 subject로도 이관하지 않고 버린다.
  - `ISS-000111` — `FI-053` fallback lease가 `localStorage` check-then-act였다(실측: 두 탭 모두 `run()` 진입). read-back 확인으로 **좁혔다. 닫지 못했다.** 남는 창 두 가지가 코드 주석에 있다. **실제로 닫는 것은 미배포 상태인 서버 멱등(`FI-010`)이다 — `FI-053`을 다시 `DELIVERED`로 선언하지 않는다.**
  - `TSK-000290` — 빌드 비재현성. 원인은 `vite.config.ts:137`의 `new Date()`가 빌드 ID로 주입된 것이고, entry 청크 → 파일명 해시 → `index.html` → `sw.js` SHELL/캐시명으로 파급됐다. 빌드 ID를 소스 해시(`src-*`)로 바꿔 화면 값을 저장소에서 재계산해 대조할 수 있다.
- **계약을 다시 썼다**: `SECURITY.md`의 사적 상태 격리 항목이 **닫힌 열거**였던 것이 `ISS-000112`의 원인이다. 원칙 + 예외 하나로 바꿨다 — "그 사람이 직접 적었거나 그 사람에게만 보이는 값은 모두 사적 상태이며, 촬영 대기열만 예외(유일본)".
- **통합자 전제가 두 번 정정됐다**: (1) 목록 사각지대는 30건이 아니라 100건이었다(v2.6.0). (2) "두 번 빌드해 비교" 게이트는 타임스탬프 단위가 1분이고 빌드가 0.6초라 **결함이 있는 채로 약 92% 확률로 통과한다** — lane이 가짜 시계·타임존 주입으로 결정적으로 만들었다.
- **미검증으로 남긴 것**: Node 22 ↔ Node 24 간 빌드 재현성. 게이트는 한 환경 안에서만 강제한다.
- rollback: PR #47 `git revert` 하나.
- **남은 human gate**: (1) Apps Script 재배포, (2) 라이브 워처 재기동, (3) founder actual-phone acceptance.

### v2.8.0 @ `e93fdfe` — 검증 2026-07-27 (agent:kairen.claude, Kairen-Ref: TSK-000287) — **보안 릴리즈 · Code.gs 무변경**

- repository: annotated tag `v2.8.0` → squash merge commit `e93fdfe`. 담은 merge는 PR #46 하나(lane `agent/w3-sw-cache`).
- human gate: v2.6.0·v2.7.0과 동일한 founder 사전 승인. **Apps Script 재배포·워처 재기동·토큰 변경·실데이터 삭제는 승인 범위 밖.**
- CI: `validate` SUCCESS — run `30244898163`. unit 293 PASS, Playwright 72 PASS, repository validator `fail=0 warn=0`.
- Pages: build 성공 후 **live와 commit blob이 exact-match** — `docs/sw.js` `f171a6c6190a541a…`, `docs/legacy.html` `f176fef409394ee5…`. (비교는 commit blob 기준 — Windows CRLF 변환 때문.)
- **GAS deployment: 해당 없음.** `Code.gs` 무변경. v2.5.0의 `PENDING`이 그대로 유효하고 확인 해시도 `f9a78d9a…` 그대로다.
- **watcher: 해당 없음.** `watcher/` 무변경. 재기동 `PENDING` 유지.
- 닫은 것: `ISS-000110`의 두 번째 결함 — `docs/sw.js`가 같은 origin GET을 query string 포함 전체 URL을 키로 캐시해, 초대 링크를 **한 번만 열어도** 개인 링크 코드가 Cache Storage 키에 영구 저장됐고 `연결 해제`가 지우지 않았다. 읽기 경로가 `ignoreSearch: true`라 그 토큰은 기능상 필요조차 없었다. 키에서 query를 떼고 `CACHE` 버전을 올려 오염된 기존 캐시가 activate에서 지워지게 했다.
- **왜 게이트가 못 잡았나**: `credential-boundary.spec.ts:204`가 **이미** "캐시 키에 링크 코드가 없다"를 단언하고 있었다. 그러나 `docs/index.html`·`docs/legacy.html`은 Service Worker를 `location.protocol === 'https:'`일 때만 등록하고 e2e harness는 `http://127.0.0.1`로 서빙한다 — **루트 워커는 어떤 테스트에서도 등록된 적이 없었다.** 신규 게이트가 명시적으로 등록해서 연다.
- 함정 경고를 남겼다: 후보 앱(`next/`) 워커도 전체 URL로 키를 만든다. 누출이 없는 이유는 계약이 아니라 사고다 — `response.clone()`이 `caches.open()` 이후에 평가돼 매번 던진다(실측: 성공 fetch 4회 후 SHELL 밖 항목 0건). **그 순서를 "고치면" 같은 누출이 생긴다.** `frontend/vite.config.ts`에 경고 주석과 선제 게이트가 있다.
- **사용자 영향(실측)**: 캐시 버전 올림으로 앱 셸 0.30MB 즉시 재다운로드, 이전 앱 OCR 사용자는 다음 사용 때 최대 18.53MB. `cardcapture-next-*` 후보 캐시는 영향 없음.
- **남는 한계**: 연결을 끊고 이 주소를 다시 열지 않는 기기는 예전 캐시를 그대로 들고 있다. Service Worker 생애주기상 클라이언트 해결책이 없다 — 코드가 실제 유출됐다면 **서버에서 코드를 회수하는 것이 진짜 대응**이다.
- rollback: PR #46 `git revert` 하나. 되돌리면 캐시 버전이 `v19`로 돌아가 셸을 한 번 더 받는다.
- **남은 human gate**: (1) Apps Script 재배포, (2) 라이브 워처 재기동, (3) founder actual-phone acceptance.

### v2.7.0 @ `35192bd` — 검증 2026-07-27 (agent:kairen.claude, Kairen-Ref: TSK-000285·286) — **보안 릴리즈 · Code.gs 무변경**

> 이 릴리즈는 **`FI-004`·`FI-005`의 v2.1.0 `DELIVERED` 선언이 사실이 아니었음을 고친 것**이다. 독립 보안 평가(`TSK-000284` / `RSL-000152`)가 뒤집었고 통합자가 lane 하네스를 쓰지 않고 소스로 재현했다.

- repository: annotated tag `v2.7.0` → squash merge commit `35192bd`. 담은 merge는 PR #45 하나이며 lane 브랜치 2개(`agent/w3-origin-pin`·`agent/w3-legacy-boundary`)가 통합돼 있다.
- human gate: v2.6.0과 동일한 founder 사전 승인. **Apps Script 재배포·워처 재기동·토큰 변경·실데이터 삭제는 승인 범위 밖.**
- CI: `validate` SUCCESS — run `30244321016`. unit 293 PASS, Playwright 68 PASS, repository validator `fail=0 warn=0`. 신규 게이트 `eval/legacy-credential.test.js` 등록.
- Pages: build run `30244572074` success. **live와 commit blob이 exact-match** — `docs/index.html` `0b491c9253fc2058…`, `docs/legacy.html` `f176fef409394ee5…`, `docs/sw.js` `85e9a4de8ea093cf…`.
- **GAS deployment: 해당 없음.** `Code.gs` 무변경. v2.5.0의 `PENDING`이 그대로 유효하고 확인 해시도 `f9a78d9a…` 그대로다.
- **watcher: 해당 없음.** `watcher/` 무변경. 재기동 `PENDING` 유지.
- 닫은 것:
  - `ISS-000109` — 신뢰 판정을 origin에서 **origin + pathname**으로 좁혔다. `script.google.com`은 누구나 자기 Apps Script를 배포할 수 있는 multi-tenant 호스트라 origin 일치가 "우리 백엔드"를 뜻하지 않았다. `?k=` 없이 `?api=`만으로도 저장된 피해자 토큰이 공격자 배포본으로 나갔다(lane이 실제 요청으로 재현). pinned origin 판정을 page-origin 규칙보다 **먼저** 둬서 경로 판정이 느슨해질 수 없게 했다. 채택 주소에서 query·fragment를 버리고 `연결 해제`가 `api` 키까지 정리한다.
  - `ISS-000110` — `docs/legacy.html`이 `?api=`를 무검증 저장하고 `?k=`를 주소창에서 지우지 않으며 referrer 정책이 없었다. React 앱과 같은 저장 키를 써서 새 앱까지 오염됐다. **이쪽이 더 넓다 — 임의 origin이 가능했다.** 이제 빌드에 박힌 `DEFAULT_API`만 쓰고 저장돼 있던 다른 주소도 실행 시 폐기한다. `DEFAULT_API` 줄은 바이트 단위로 그대로다(확인함).
- **왜 회귀 게이트가 통과시켰나**: 기존 `credential-boundary.spec.ts`는 적대적 주소로 **다른 origin만** 시험했고 5개 테스트가 **전부 `next/`만** 열었다. 새 게이트는 같은 origin 다른 경로를 시험하고, legacy 표면을 실제로 열며, 컨텍스트의 **모든** 요청을 가로채 "빌드에 박힌 origin 밖 요청 == []"로 판정한다.
- 부수: **e2e가 매 실행마다 실제 GAS 배포본에 인증 요청을 보내고 있었다.** `?api=`가 거부되면 앱이 pinned 기본값으로 되돌아가 부팅 직후 실서버를 호출했다. lane F가 발견해 공유 `beforeEach` 가드로 막았다.
- 문서 정정: `SECURITY.md`·`AGENTS.md`가 허용 공개 상수(`DEFAULT_API`) 위치를 `docs/index.html`로 적고 있었으나 그 파일에는 exec URL이 **0건**이다. 실제 상수는 `docs/legacy.html`에만 있고 `frontend/vite.config.ts`가 읽는 React 앱 pinned endpoint의 유일한 원본이다.
- **운영 영향**: `?api=`가 production에서 사실상 무의미해진다. 기존 배포를 새 버전으로 갱신하면 exec URL이 유지되므로 일상적 재배포는 영향 없다. **새 deployment를 만들면 URL이 바뀌고** 그때는 `docs/legacy.html`의 `DEFAULT_API` 수정 → 빌드 → Pages 재배포가 필요하다. 개발 harness(`.test`·`.localhost`)는 그대로다.
- rollback: PR #45 `git revert` 하나.
- **남은 human gate**: (1) Apps Script 재배포, (2) 라이브 워처 재기동, (3) founder actual-phone acceptance.

### v2.6.0 @ `c515873` — 검증 2026-07-27 (agent:kairen.claude, Kairen-Ref: TSK-000280·281·282·283) — **Code.gs 무변경 · 앞선 GAS 배포·워처 재기동 PENDING 유지**

> 이 릴리즈는 `Code.gs`를 건드리지 않았다. 따라서 v2.4.0·v2.5.0의 **미배포 서버 변경 두 건은 그대로 남아 있고 `ISS-000108`도 live에서 여전히 열려 있다.** 이 baseline은 그 상태를 바꾸지 않는다.

- repository: annotated tag `v2.6.0` → squash merge commit `c515873` (`main`에 포함). 담은 merge는 PR #44 하나이며, 그 안에 lane 브랜치 4개(`agent/w3-watcher-health`·`agent/w3-trace`·`agent/w3-recall`·`agent/w3-content-type`)가 통합돼 있다.
- human gate: founder가 세션에서 `Feature Index를 순차적으로 개발 · 문서가 승인 받으라 해도 다시 묻지 말 것 · merge와 release까지 네가 한다 · 결정이 필요하면 네 추천대로`로 구현·merge·tag를 사전 승인함. **Apps Script 재배포·워처 재기동·토큰 변경·실데이터 삭제는 승인 범위에서 명시적으로 제외.**
- CI: `validate` SUCCESS — run `30243635918`. unit 280 PASS(25 files), Playwright 61 PASS, repository validator `fail=0 warn=0`. 신규 게이트 2종 등록: `eval/upload-content-type.test.js`, `watcher/tests/health-tests.ps1`(**워처 스위트가 CI에서 실행되는 첫 사례**).
  - 첫 두 실행은 `int15-surfaces.spec.ts:218`에서 실패했다. 같은 SHA에서 측정값이 `1087.86` / `989.69`로 **매번 달랐다** — 레이아웃 초과가 아니라 ion-modal 시트가 정착하기 전의 중간 프레임이다. 그 검사는 `toBeEnabled()` 직후 `boundingBox()`를 읽고 있었고 정착 대기가 없었다. spec 파일이 늘며 CI 동시 실행 조합이 바뀌어 원래 racy했던 읽기가 드러난 것이다. 재시도하는 `toBeInViewport({ ratio: 1 })`(기존 아래 경계 검사보다 강한 조건)로 고정했고, 시트를 320px 내리는 CSS 주입으로 그 단언이 `viewport ratio 0`으로 FAIL하는 것을 확인해 형식적이지 않음을 증명했다.
- Pages: build run `30243889773` success. **live와 commit blob이 exact-match** — `docs/index.html` `0b491c9253fc2058a88232403667363115e600ff4807f6997734af9bf76cfe16`, `docs/sw.js` `85e9a4de8ea093cf8134a2866c7757214c8c2f6846b60a551e3bd2222c459e7f`. (비교는 working tree가 아니라 commit blob 기준 — Windows CRLF 변환 때문.)
- **GAS deployment: 이번 릴리즈에 해당 없음.** `Code.gs` 무변경(`git diff v2.5.0 v2.6.0 -- Code.gs` 비어 있음). v2.5.0의 `PENDING` 상태가 그대로 유효하며 확인 해시도 그대로 `f9a78d9aa5bc34cc1a70a3ffaf51b5d73db81b0f77a9d90f23e8ff48dfb5b364`다.
- GAS live probe(무효 토큰만, 유효 token 미사용): `ping` → `ok:true`; `whoami`·`list`·`search` → `invalid_token`; `?action=nonsense` → `unknown_action`. **이 probe는 여전히 예전 배포본(version 5)을 확인한 것이다.**
- **watcher: 재기동 미실행 (PENDING) — 그리고 현재 live 워처는 살아 있지 않다.** 관찰: health 파일 `pid=39880`, `lastHeartbeat=2026-07-26 16:26:30`, `watcher.log`에 2026-07-27 기록 0줄. PID 39880은 존재하지 않고, PID 18108이 오늘 14:48에 기동했으나 로그·health를 한 줄도 쓰지 않았다. **agent는 어떤 프로세스도 죽이거나 띄우지 않았다.** 이번 릴리즈의 `CardCapture_Health.ps1`이 이 상태를 `CRITICAL / orphan_watcher_process`로 단정한다. `.ps1` UTF-8 BOM 확인 완료.
- processing contract: vault `CardCapture_Processing.md` 변경 없음.
- 이 release가 닫은 Feature Index slice: `FI-029`(처리 health dashboard), `FI-021`(correlation ID·redacted log), `FI-100`(목록 pagination), `FI-104`(검색 근거 스니펫). `FI-012`(magic-byte)는 **결함을 기계로 증명하고 게이트만 심었다** — 서버 수정은 재배포 사이클 뒤다.
- **실측된 결함**(전부 수정 전 FAIL 관찰): 재전송 대조가 100건만 읽어 이미 접수된 캡처를 다시 올릴 수 있었음(합성 서버 150건에서 101번째 이후 누락) / `예전 기록 더 보기`가 100건에서 죽은 버튼이 됨 / 워처 health가 PID 재사용에 무력해 죽은 워처를 정상으로 보고 / health가 처리기 원본 출력을 노출(실측 로그의 99.8%가 명함 내용) / 서버가 업로드 바이트를 검사하지 않아 비이미지 10종이 `front.jpg`로 접수.
- **재현 불가 확인**: frontend 빌드가 재현 가능하지 않다. 소스가 동일한데 `docs/next` entry 해시가 매 빌드 바뀐다(세 번 연속 확인). 커밋된 `docs/next`가 소스와 맞는지 재빌드로 검증할 수 없다는 뜻이다. 위 Pages 비교는 손으로 쓴 `docs/index.html`·`docs/sw.js`를 쓰므로 그 계약 자체는 유효하다.
- rollback: PR #44 `git revert` 하나. GAS·워처는 건드리지 않았으므로 되돌릴 것이 없다.
- **남은 human gate**: (1) Apps Script 재배포, (2) 라이브 워처 재기동, (3) founder actual-phone acceptance. 셋 다 `NOT RUN — HUMAN/EXTERNAL`이다.

### v2.5.0 @ `e335652a2379fa6c90cdbdca2a8c3230e76fd676` — 검증 2026-07-27 (agent:kairen.claude, Kairen-Ref: TSK-000276·277·278·279) — **GAS 배포 PENDING · 워처 재기동 PENDING**

> ⚠️ **이 baseline은 두 곳이 미완이다.** `Code.gs`와 `watcher/`가 모두 바뀌었고 둘 다 사람 게이트다. 앱(`docs/`)만 자동 반영된다. **재배포·재기동 전까지 `ISS-000108`(브리핑 조작)은 live에서 여전히 열려 있다.**

- repository: annotated tag `v2.5.0` → `e335652a2379fa6c90cdbdca2a8c3230e76fd676`. 담은 merge는 PR #39(`e941e5e`), #41(`3da2003`), #40(`58b475c`), #42(`e335652`)다. v2.4.0의 미배포 `Code.gs` 변경 위에 #41이 쌓였다.
- human gate: founder가 `순차적으로 개발 진행 · 내 승인 없이 그냥 쭉 진행 · 결정 필요사항은 너의 추천대로 · 적절한 시점마다 merge 및 릴리즈`와 `병렬적으로 진행 · 멀티 에이전트`로 구현·merge·tag를 사전 승인함. **Apps Script 재배포와 라이브 워처 재기동은 승인 범위에서 제외**(Google 로그인·운영 프로세스 조작).
- CI: `validate` SUCCESS — PR #39 run `30237928703`, #41 `30238273284`, #40 `30238618766`, #42 `30238847458`. 최종 head 기준 unit 229 PASS, Playwright 56 PASS, repository validator fail=0 warn=0(신규 eval 게이트 4종 등록 포함).
- Pages: build commit `e335652…`, status `built`. live와 commit blob이 exact-match — `docs/index.html` `0b491c9253fc2058a88232403667363115e600ff4807f6997734af9bf76cfe16`; `docs/next/index.html`·active JS `assets/index-H1CA71r5.js`·active CSS `assets/index-DZeXLMal.css` 전부 일치.
- **GAS deployment: 미실행 (PENDING).** repository `Code.gs` 정규화(EOL LF) sha256 = `f9a78d9aa5bc34cc1a70a3ffaf51b5d73db81b0f77a9d90f23e8ff48dfb5b364`. live deployment는 v2.0.0의 **version 5**(`990487a7…`)다. **세 해시가 모두 다르다** — v2.4.0·v2.5.0의 서버 변경이 누적 미배포다. 재배포 시 반드시 **v2.5.0의 해시**로 확인해야 한다(v2.4.0 체크리스트의 값은 이제 낡았다).
- GAS live probe: `ping` ok, 무효 토큰 `invalid_token`, unknown action `unknown_action`. **이 probe는 예전 배포본을 확인한 것이며 이번 변경을 검증하지 않는다.** 유효 token 미사용.
- **watcher: 재기동 미실행 (PENDING).** `watcher/`가 4파일 바뀌었다(`git diff v2.4.0 e335652 -- watcher/`). 실행 중 PID `34896`은 예전 프로토콜이며 **건드리지 않았다.** health 파일이 `pid=39880`을 가리키는 불일치도 관찰만 했다 — 재기동 시 함께 확인해야 한다. `.ps1` 4개 UTF-8 BOM 확인.
- processing contract: vault `CardCapture_Processing.md` 변경 없음. 단, deep 프롬프트에 `TARGET-CAPTURE-ID`가 추가돼 "가장 이른 한 건" 규칙을 좁힌다(`FI-019` 동작에 필수).
- 이 release가 소스에서 닫은 Feature Index slice: `FI-011`·`FI-012`(파일명)·`FI-013`(파일 이름 서버 소유), `FI-017`·`FI-018`·`FI-019`(워처 프로토콜), `FI-022`·`FI-023`(golden·adversarial 게이트), `FI-046`·`FI-049`·`FI-067`(촬영 세션).
- **미해결로 남긴 것**: `TSK-000277`이 찾아낸 D3 — `doPost`가 `prior`를 읽은 뒤 워처가 마감하면 내용 변경 재업로드가 그 마감을 덮으며 이전 결과 표식을 하나도 남기지 않는다(`TSK-000275`의 잔여 race). 후속 lane 필요.
- rollback: Pages는 해당 PR `git revert`. GAS는 배포하지 않았으므로 되돌릴 것이 없다. 워처는 재기동하지 않았으므로 되돌릴 것이 없다.
- **남은 human gate**: (1) Apps Script 재배포, (2) 라이브 워처 재기동, (3) founder actual-phone acceptance. 셋 다 `NOT RUN — HUMAN/EXTERNAL`이다.

### v2.4.0 @ `ba307992b29198442196f33bde11866bd72aaba3` — 검증 2026-07-27 (agent:kairen.claude, Kairen-Ref: TSK-000275) — **GAS 배포 PENDING**

> ⚠️ **이 baseline은 불완전하다.** `Code.gs`가 바뀐 첫 릴리즈이고 **Apps Script 재배포가 아직 실행되지 않았다.** 따라서 이 태그는 "repository와 회귀 게이트가 확정됐다"까지만 증명하며, **live 서버는 여전히 v2.0.0에서 배포한 version 5의 예전 동작**이다. 재배포와 그 후의 live probe가 끝날 때까지 이 릴리즈를 "서버가 보호된다"로 읽지 마라.

- repository: annotated tag `v2.4.0` → squash merge commit `ba307992b29198442196f33bde11866bd72aaba3` (`main`에 포함). 담은 merge는 PR #37 하나다.
- human gate: founder가 세션에서 `순차적으로 개발 진행. 내 승인 없이 그냥 쭉 진행. 결정 필요사항이 있는 경우 너의 추천대로 진행. 적절한 시점마다 merge 및 릴리즈`로 구현·merge·tag를 사전 승인함. **Apps Script 재배포는 Google 로그인이 필요한 사람 전용 작업이라 agent가 실행하지 않았다.**
- CI: GitHub Actions `validate` run `30236320998` SUCCESS. 같은 head에서 repository validator fail=0 warn=0(신규 `eval/upload-idempotency.test.js` 10건 포함), unit 215 PASS, Playwright 52 PASS.
- Pages: **콘텐츠 변경 없음.** 이 릴리즈는 `docs/`를 건드리지 않았다(`git diff --quiet v2.3.0 ba30799 -- docs/`). live 산출물은 v2.3.0 baseline과 동일하다.
- **GAS deployment: 미실행 (PENDING — 사람 게이트).**
  - repository `Code.gs` 정규화(EOL LF) sha256 = `ee7075038c61f0192b3f8d9f678574d4a8828b83fda63d1676c8f967adfd6f56`.
  - live deployment는 v2.0.0에서 배포한 **version 5**이며 그 Code.gs 상태는 `990487a7f09dcc2c0247b2a0e9013ab2dfedbd033560be143fbee7ea19206fb1`(v2.0.0 baseline 기록)이다. **두 해시가 다르다 = live와 repository가 의도적으로 어긋나 있다.**
  - 재배포 절차는 vault `01_Company/00_Company_Operations/05_Tools_and_Systems/CardCapture_Setup.md`의 클릭 단위 절차를 따른다. 재배포 후 Apps Script editor의 Code.gs 정규화 sha256이 위 값과 일치하는지 확인하고, 이 항목을 갱신해야 한다.
- GAS live probe: 2026-07-27 13:16 KST `ping` → `{"ok":true,"service":"card-capture"}`. **이 probe는 예전 배포본을 확인한 것이며 이번 변경을 검증하지 않는다.** 유효 token은 사용하지 않았다.
- watcher: **재기동하지 않았다.** 이 릴리즈는 `watcher/`를 건드리지 않았다(`git diff --quiet v2.3.0 ba30799 -- watcher/`).
- processing contract: vault `CardCapture_Processing.md`를 변경 없이 사용함. `MVP build/testability gate comes before customer proof.`
- 이 release가 소스에서 닫은 Feature Index slice: `FI-009`(captureId ownership), `FI-010`(upload idempotency), `FI-015`(lifecycle monotonicity). **live에서 닫히는 것은 재배포 후다.**
- 재배포 후 달라지는 외부 계약: 업로드 응답에 `deduped`·`status`·`processedAt`가 추가되고, 다른 사람의 captureId로 업로드하면 `capture_conflict`로 거절된다. 현재 클라이언트는 `ok:true`만 읽으므로 **하위 호환**이다.
- rollback: 이 PR `git revert` 하나. 재배포를 했다면 Apps Script 배포 관리에서 이전 version(5)으로 전환하고 probe를 재실행한다.
- **남은 human gate**: (1) Apps Script 재배포, (2) 재배포 후 live probe와 실폰 확인. 둘 다 `NOT RUN — HUMAN/EXTERNAL`이다.

### v2.3.0 @ `85254a059886d827e490c2e6e28d9b200f809616` — 검증 2026-07-27 (agent:kairen.claude, Kairen-Ref: TSK-000273)

- repository: annotated tag `v2.3.0` → squash merge commit `85254a059886d827e490c2e6e28d9b200f809616` (`main`에 포함). 이 release가 담은 merge는 PR #34 하나다.
- human gate: founder가 세션에서 `순차적으로 개발 진행. 내 승인 없이 그냥 쭉 진행. 결정 필요사항이 있는 경우 너의 추천대로 진행. 적절한 시점마다 merge 및 릴리즈`로 구현·PR merge·release tag·Pages 반영을 사전 승인함. **Script Properties·token·credential 변경과 GAS 재배포는 이 승인 범위에서 제외**했고, 실제로도 필요하지 않았다.
- CI: GitHub Actions `validate` run `30235225046` SUCCESS. 같은 head에서 unit 215 PASS, TypeScript/Vite production build PASS, Playwright 52 PASS, repository validator fail=0 warn=0.
- Pages: build source `main:/docs`, build commit `85254a059886d827e490c2e6e28d9b200f809616`, status `built`. live와 commit blob이 exact-match — `docs/index.html` sha256 `0b491c9253fc2058a88232403667363115e600ff4807f6997734af9bf76cfe16`; `docs/sw.js` `85e9a4de8ea093cf8134a2866c7757214c8c2f6846b60a551e3bd2222c459e7f`; `docs/legacy.html` `d1b370a9ab8ae8c8d51b10a6136487cec57c7cd798a43e45ecf9a8e02dd52ab5`; `docs/next/index.html` `7318344b839af537c01c3819c9cd4b6d6cf3574936a79a21032148ebc72a2b09`; active JS `assets/index-CTRDAOhL.js` `8afed4ac9110b6fc11ec954fb303b17e93c5b824f214c4451ae4e56e6c7344c7`; active CSS `assets/index-CURCMqBP.css` `b68968a12c12938d59b97f0d848a7596b5197392ce9694cee1bfdf491cec7d03`. (비교는 working tree가 아니라 commit blob 기준이다 — Windows 체크아웃의 CRLF 변환이 파일 해시를 바꾼다.)
- GAS deployment: **변경 없음.** `Code.gs`는 v2.0.0(`c9036b3`) 이후 바뀌지 않았다(`git diff --quiet v2.2.0 85254a0 -- Code.gs`). Apps Script 재배포가 필요하지 않고 실행하지 않았다. live deployment는 v2.0.0에서 배포한 version 5 그대로다.
- GAS live probe: 2026-07-27 12:46 KST `ping` → `{"ok":true,"service":"card-capture"}`; invalid token `whoami`·`list` → `invalid_token`; unknown action → `unknown_action`. **유효 token은 사용하지 않았다.**
- watcher: **재기동하지 않았다.** 이 release는 `watcher/`를 건드리지 않았다(`git diff --quiet v2.2.0 85254a0 -- watcher/`). 실행 중 PID `34896`.
- processing contract: vault `01_Company/00_Company_Operations/05_Tools_and_Systems/CardCapture_Processing.md`를 변경 없이 사용함. `MVP build/testability gate comes before customer proof.`
- 이 release가 새로 닫은 Feature Index slice: `FI-016`(응답 유실 뒤 서버 대조 후에만 재전송), `FI-021` 일부(업로드 실패를 `rejected`/`ambiguous`로 분류).
- **드러난 서버 측 미결 사항**: `Code.gs`의 업로드 경로는 `captureId` 폴더를 upsert하고 `capture.json`을 `status: 'received'`로 덮어쓴다. 지금은 **클라이언트가 재전송하지 않게** 막았을 뿐, 서버 자체의 idempotency(`FI-010`)와 lifecycle monotonicity(`FI-015`)는 없다. `Code.gs` lane에서 함께 다뤄야 한다.
- 병렬 작업: 구현 중 다른 세션이 PR #33·#35를 merge하고 v2.2.0을 릴리즈했다. 그 위로 rebase하고 `docs/next`를 재빌드했으며, 위 52 PASS에는 v2.2.0의 다크 모드·하단 바 게이트가 모두 포함된다.
- rollback: Pages는 `legacy.html` 또는 PR #34 `git revert` 하나. GAS·watcher·데이터 rollback은 이 release 범위에 없다.
- **남은 human gate**: founder actual-phone acceptance (`V7` = `NOT RUN — HUMAN/EXTERNAL`). merge와 Pages 반영을 actual-phone PASS나 customer proof로 승격하지 않는다.

### v2.2.0 @ `76ce78c8940a22e7cc69e64cea592517110f9426` — 검증 2026-07-27 (agent:kairen.claude, Kairen-Ref: INT-000016, TSK-000272)

- repository: annotated tag `v2.2.0` → squash merge commit `76ce78c8940a22e7cc69e64cea592517110f9426` (`main`에 포함). 이 release가 담은 merge는 PR #33 하나이고, PR의 exact head는 `9083e483eacc4965580488b7f9d8cf44c09ec86a`다.
- human gate: founder가 세션에서 `이거 보고 merge 및 release해줘` → `내 말은 개발하고 merge, release까지 해달라는거 였어`로 구현·PR merge·release tag·Pages 반영을 사전 승인함. **Script Properties·token·credential 변경과 GAS 재배포는 이 승인 범위에서 제외**했고, 실제로도 필요하지 않았다.
- CI: GitHub Actions `validate` run `30234506135` SUCCESS, exact head `9083e48`. 같은 head에서 unit 202 PASS, TypeScript/Vite production build PASS, Playwright 50 PASS(기존 43 + INT-000016 신규 7), repository validator fail=0 warn=0.
- Pages: build source `main:/docs`, build commit `76ce78c8940a22e7cc69e64cea592517110f9426`, status `built`. live와 commit 콘텐츠가 exact-match — `docs/index.html` sha256 `0b491c9253fc2058a88232403667363115e600ff4807f6997734af9bf76cfe16`; `docs/sw.js` `85e9a4de8ea093cf8134a2866c7757214c8c2f6846b60a551e3bd2222c459e7f`; `docs/legacy.html` `d1b370a9ab8ae8c8d51b10a6136487cec57c7cd798a43e45ecf9a8e02dd52ab5`; `docs/next/index.html` `e98e0fc7c8a05a793fa986187fc9178df99dc962e37946eac2a0e5372a401852`; active JS `assets/index-CFg5TftA.js` `33ccfea66e9752d080430b43fe853a43fc2cd5ac64b9adec37be805bfccc5555`; active CSS `assets/index-CURCMqBP.css` `b68968a12c12938d59b97f0d848a7596b5197392ce9694cee1bfdf491cec7d03`. (비교는 working tree가 아니라 commit blob 기준이다 — Windows 체크아웃의 CRLF 변환이 파일 해시를 바꾼다.)
- Pages behavior: live `https://guecom.github.io/card-capture/next/`에서 `--cc-app-height`가 `100dvh`, `--cc-safe-bottom`이 고정 `0px`로 계산되고, 설정의 `화면 테마`에서 `다크`를 고르면 `data-theme="dark"` · 배경 `rgb(15,21,30)` · `theme-color` `#111823`으로 즉시 바뀌며 선택값이 저장되는 것을 확인함. 표시된 빌드 식별자는 `2026-07-27 03:22Z`다.
- GAS deployment: **변경 없음.** `Code.gs`는 v2.0.0(`c9036b3`) 이후 바뀌지 않았다(`git diff --quiet v2.1.0 76ce78c -- Code.gs`). Apps Script 재배포가 필요하지 않고 실행하지 않았다. live deployment는 v2.0.0에서 배포한 version 5 그대로다.
- GAS live probe: 2026-07-27 12:30 KST `ping` → `{"ok":true,"service":"card-capture"}`; invalid token `whoami`·`list` → `invalid_token`; unknown action → `unknown_action`. **유효 token은 사용하지 않았다** — 운영 데이터 쓰기와 token 노출을 피하기 위함이며, owner-token 실동작은 다음 실제 폰 사용이 담당한다.
- watcher: **재기동하지 않았다.** 실행 중 PID `34896`(2026-07-27 10:06 시작), 스크립트 경로 `card-capture/watcher/CardCapture_Watcher.ps1`. 이 release는 `watcher/`를 건드리지 않았고(`git diff --quiet v2.1.0 76ce78c -- watcher/`), 실행 중 워처가 로드한 스크립트와 release commit의 워처 스크립트가 EOL 정규화 기준 sha256 `5b689d1d43c865af3276e51a6ab178e53cdcde2d01c5725829056ba203557e0d`으로 동일함을 확인했다.
- processing contract: vault `01_Company/00_Company_Operations/05_Tools_and_Systems/CardCapture_Processing.md`를 변경 없이 사용함. `MVP build/testability gate comes before customer proof.`
- 병렬 작업: 구현 중 PR #30·#31·#32가 `main`에 들어와 그 위로 rebase하고 `docs/next`를 재빌드했다. 위 50 PASS에는 #30의 queue-truth 게이트와 #31의 접근성 게이트가 모두 포함된다.
- **남은 human gate**: founder actual-phone acceptance. 특히 **하단 탭 바 항목의 최종 판정은 실제 폰이다** — headless Chrome에는 접히는 주소창이 없어, CI가 잠근 것은 원인 조건(껍데기 높이·여백 고정·스크롤 불변)이지 현상 재현이 아니다. merge와 Pages 반영을 actual-phone PASS나 customer proof로 승격하지 않는다.
- rollback: Pages는 `legacy.html` 또는 PR #33 `git revert` 하나. GAS·watcher·데이터 rollback은 이 release 범위에 없다.

### v2.1.0 @ `6bcb664803db04ac7770574acc31c882e1a18f4b` — 검증 2026-07-27 (agent:kairen.claude, Kairen-Ref: TSK-000269, TSK-000270, TSK-000271)

- repository: annotated tag `v2.1.0` → merge commit `6bcb664803db04ac7770574acc31c882e1a18f4b` (`main`에 포함). 이 release가 담은 merge는 PR #29 `f591b79`, PR #30 `e1e8ea5`, PR #31 `6bcb664`이며, v2.0.0 이후 누적된 PR #19~#28도 함께 처음 릴리즈된다.
- human gate: founder가 세션에서 `내 승인 없이(글에 승인 받으라고 했음에도 불구하고) 그냥 쭉 해서 머지 및 릴리즈까지 해줬으면 좋겠어`로 PR merge·release tag·Pages 반영을 사전 승인함. **Script Properties·token·credential 변경과 GAS 재배포는 이 승인 범위에서 제외**했고, 실제로도 필요하지 않았다(아래 참조).
- CI: GitHub Actions `validate` SUCCESS — PR #29 run `30231826796`, PR #30 run `30233443826`, PR #31 run `30233827541`. 최종 head 기준 unit 197 PASS, TypeScript/Vite production build PASS, Playwright 43 PASS, repository validator fail=0 warn=0.
- Pages: build source `main:/docs`, status `built`. live와 commit 콘텐츠가 exact-match — `docs/index.html` sha256 `0b491c9253fc2058a88232403667363115e600ff4807f6997734af9bf76cfe16`; `docs/sw.js` `85e9a4de8ea093cf8134a2866c7757214c8c2f6846b60a551e3bd2222c459e7f`; `docs/legacy.html` `d1b370a9ab8ae8c8d51b10a6136487cec57c7cd798a43e45ecf9a8e02dd52ab5`; `docs/next/index.html` `1b71e099a5ed602932a1a32d38e9527c8a13f28eb41c7620ad146dd9b7b22ead`; active JS `assets/index-BuaW3bsz.js` `5e8a9d4d1c0da998b59d0400e326a43640a5616596f32e569580697ebf9ab522`.
- GAS deployment: **변경 없음.** `Code.gs`는 v2.0.0(`c9036b3`) 이후 한 줄도 바뀌지 않았다(`git diff --quiet v2.0.0 6bcb664 -- Code.gs`). 따라서 이 release에 Apps Script 재배포가 필요하지 않고, 실행하지 않았다. live deployment는 v2.0.0에서 배포한 version 5 그대로다.
- GAS live probe: 2026-07-27 12:04 KST `ping` → `{"ok":true,"service":"card-capture"}`; invalid token `whoami`·`list` → `invalid_token`; unknown action → `unknown_action`. **유효 token은 사용하지 않았다** — 운영 데이터 쓰기와 token 노출을 피하기 위함이며, owner-token 실동작은 다음 실제 폰 사용이 담당한다.
- watcher: **재기동하지 않았다.** 실행 중 PID `34896`(2026-07-27 10:06 시작), 스크립트 경로 `card-capture/watcher/CardCapture_Watcher.ps1`. 이 release의 세 PR은 `watcher/`를 건드리지 않았고, 실행 중 워처가 로드한 스크립트(`8de6316`)와 release commit의 워처 스크립트가 **바이트 동일**함을 확인했다.
- processing contract: vault `01_Company/00_Company_Operations/05_Tools_and_Systems/CardCapture_Processing.md`를 변경 없이 사용함. `MVP build/testability gate comes before customer proof.`
- 이 release가 새로 닫은 Feature Index slice: `FI-004`·`FI-005`·`FI-006`·`FI-007`(credential 전송 경계와 기기 내 subject 격리), `FI-025`·`FI-031`·`FI-032`·`FI-052`·`FI-053`(촬영 저장 진실과 대기열 무결성), `FI-164`(좁은 화면·접근성 회귀 게이트).
- rollback: Pages는 `legacy.html` 또는 해당 PR `git revert`(각 PR이 revert 하나로 되돌아간다). GAS·watcher·데이터 rollback은 이 release 범위에 없다.
- **남은 human gate**: founder actual-phone acceptance. 이 release의 machine evidence는 `V0`~`V6` 수준이며, `V7`(실기기·실사용)은 `NOT RUN — HUMAN/EXTERNAL`이다. merge와 Pages 반영을 actual-phone PASS나 customer proof로 승격하지 않는다.

### v2.0.0 @ `c9036b3982adf5fbb5fdf108aca3656dc56b3ffd` — 검증 2026-07-26 (Codex, Kairen-Ref: TSK-000221)

- repository: annotated tag `v2.0.0` → merge commit `c9036b3982adf5fbb5fdf108aca3656dc56b3ffd`; PR #8의 exact head는 `b2343d4afc814bff31d72c6b12618c7e7292bbf7`이고 `main`에 포함됨.
- human gate: founder가 같은 Codex task에서 `좋아 잘 돼는 것 확인했어`라고 실사용 상태를 확인한 뒤 `병합, 릴리즈, 배포까지 알아서 잘 해봐`로 PR merge·release tag·Pages·기존 Apps Script deployment 새 version·watcher 재기동을 승인함. Script Properties·token·credential 변경은 승인/실행 범위에서 제외함.
- CI: GitHub Actions run `30165011262` SUCCESS, exact head `b2343d4…`, 48/48 unit tests·TypeScript/Vite production build·Playwright 6/6·repository validator PASS.
- Pages: build source `main:/docs`, commit `c9036b3…`, status `built`. live와 commit 콘텐츠가 exact-match: `docs/index.html` sha256 `0b491c9253fc2058a88232403667363115e600ff4807f6997734af9bf76cfe16`; `docs/sw.js` `85e9a4de8ea093cf8134a2866c7757214c8c2f6846b60a551e3bd2222c459e7f`; `docs/legacy.html` `d1b370a9ab8ae8c8d51b10a6136487cec57c7cd798a43e45ecf9a8e02dd52ab5`; `docs/next/index.html` `831b1e623a9684dbe781616c7986cf99ca293c5dc44625e5caaa77790bd71e13`; active JS `cd5d33af010d42ffb415be65240f27cac533319afe92761aa32bb8e711c18286`.
- Pages behavior: `https://guecom.github.io/card-capture/`가 query·hash를 보존해 `/next/` React 앱으로 진입했고 `검색` tab과 `사람 검색` 진입점이 노출됨. `legacy.html`은 복구 링크로 유지되고 root/React service worker cache가 격리됨.
- GAS deployment: 기존 deployment ID와 web-app URL을 유지하고 version 5를 2026-07-26 01:18 KST에 `v2.0.0 React/Ionic root promotion (c9036b3 / TSK-000221)` 메모로 배포함. Apps Script editor 전체 Code.gs와 repository Code.gs의 정규화 sha256 `990487a7f09dcc2c0247b2a0e9013ab2dfedbd033560be143fbee7ea19206fb1` 일치.
- GAS live probe: 2026-07-26 01:19 KST `ping` → `ok:true`; invalid token `whoami`·`search`·POST `researchinstruction` → `invalid_token`; unknown action → `unknown_action`. Script Properties·실 token은 읽거나 변경하지 않음.
- watcher: `watcher-v3.0`을 merge commit working tree에서 단일 재기동. PID `38348`, startedAt `2026-07-26 01:20:44 KST`, health exit `0`, consecutive failures `0`, backlog `0`, lock `false`, process alive.
- processing contract: vault `01_Company/00_Company_Operations/05_Tools_and_Systems/CardCapture_Processing.md`의 2026-07-26 현재 계약을 변경 없이 사용함. `MVP build/testability gate comes before customer proof.`
- rollback: Pages는 `legacy.html` 또는 승인된 `git revert`; GAS는 deployment version 4; watcher는 이전 승인 commit의 BOM 보존 스크립트로 되돌린 뒤 같은 health probe를 재실행.

### v1.0 @ `2d4704c1224e9749c3663818ff68fb546d01be8a` — 검증 2026-07-24 (agent:kairen.claude)

- repository: `main` == 로컬 tracking head == `2d4704c…` (worktree clean). release tag 없음(known gap — 사람 승인 시 `v1.0` 태깅 제안).
- Pages: live `index.html` sha256 `e791c405…acae0af3` == HEAD `docs/index.html` sha256 — **일치**.
- GAS live probe (실토큰 미사용, 무효 토큰만): `ping` ok=true · `whoami`/`list`/`persondoc` 무효 토큰 → `invalid_token` · unknown action → `unknown_action` — **전부 기대 동작**. 유효 토큰 scope 실동작(list·persondoc·업로드)은 2026-07-23 밤 폰 왕복에서 사람이 확인한 기록이 vault TSK-000106/107에 있음; current SHA 기준 재확인은 사람 폰 단계로 남음.
- watcher: PID 46464 가동, heartbeat 10분 간격 기록(마지막 2026-07-24 01:02), inbox 5건 status 일치(processed 3·skipped 2), processing.lock 없음.
- processing contract: vault `CardCapture_Processing.md` 2026-07-23 규칙 8/8-1 포함 상태.
- human gate 이력: 저장소 생성·GAS 배포·토큰 발급은 2026-07-23 사람이 수행.
