# Changelog — Kairen Card Capture

사용자에게 보이는 변화 중심으로 기록한다. 형식: 버전 — 날짜 — 커밋. 배포(GAS/Pages) 시점은 `RELEASE.md`의 release evidence가 진실이다.

## [Unreleased] — 개인 링크 코드가 새어 나갈 수 있던 경로 차단 (branch `agent/tsk-000269-credential-boundary`, Kairen-Ref: TSK-000269, ISS-000032; FI-004·005·006·007)

- **가짜 서버 주소가 붙은 링크를 열어도 개인 링크 코드가 넘어가지 않는다**: 예전에는 주소에 `?api=`를 붙이면 그 값을 **그대로 저장하고**, 이후 모든 명함 업로드와 목록 조회를 그 주소로 보냈다. `?api=https://공격자주소/`가 달린 링크를 한 번만 열면 그 뒤로 찍는 명함 사진과 개인 링크 코드가 전부 그쪽으로 갔다는 뜻이다. 이제 **빌드에 박힌 주소와 앱 자신의 주소로만** 전송하고, 그 밖의 주소는 저장조차 하지 않는다. 예전 버전이 이미 저장해 둔 신뢰 밖 주소도 앱을 열 때 폐기한다. 고급 설정에 직접 입력해도 같은 판정을 받는다. 무시했다는 사실은 화면에 그대로 알린다.
- **개인 링크로 열면 주소창의 코드를 즉시 지운다**: `?k=` 값이 주소창에 계속 남아 방문 기록·화면 공유·스크린샷·다른 사이트로 이동할 때의 Referer로 새어 나가고 있었다. 이제 저장 직후 주소에서 코드를 지우고(뒤로 가기로도 되살아나지 않는다), 나머지 파라미터(`?view=`)는 그대로 둔다.
- **한 폰을 다른 링크로 열면 이전 사람의 기록이 보이지 않는다**: 브리핑 캐시·소유자 권한 표시·최근 검색어가 링크 코드와 무관한 한 칸에 저장돼, owner 링크로 쓰던 폰을 guest 링크로 열면 **서버가 답하기 전에** 이전 사람의 기록이 그대로 떠 있었다. 이제 이 값들은 링크 코드별로 나뉘고, 다른 코드의 사본은 기기에서 지워진다. 링크 코드가 없으면 사적 기록을 읽지도 쓰지도 않는다.
- **설정에 `연결 해제`가 생겼다**: 폰을 넘기거나 링크를 회수할 때 개인 링크 코드·촬영자 이름·이 기기의 브리핑 사본·검색 기록·만남 맥락을 지운다. **전송을 기다리는 촬영은 지우지 않고**, 남아 있으면 건수를 먼저 경고한다.
- **어디로 보내지는지 설정에서 보인다**: `명함과 개인 링크 코드는 script.google.com 으로만 전송돼요`처럼 실제 전송 대상과 주소창 정리 사실을 적어 둔다.
- **회귀 게이트 신설**(`e2e/credential-boundary.spec.ts`): 적대적 origin으로 나가는 요청 0건, 주소창 코드 제거, 서버가 침묵해도 이전 subject 기록 0건, 연결 해제 정리, 오프라인 캐시에 API 응답 없음. 수정 전 빌드(`8de6316`)에서 앞의 네 건이 모두 FAIL임을 먼저 확인했다.

## [Unreleased] — 만난 상황 chip을 일반 상황으로 (branch `agent/int-000015-situation-chips`, Kairen-Ref: INT-000015, ISS-000084, TSK-000220)

- **`어디서 만났나요?` chip이 특정 행사 대신 일반 상황이 된다**: 예전에는 이 기기에서 실제로 입력한 적 있는 행사 이름(`2026 로보월드` 등)을 먼저 chip으로 올리고 남은 자리만 예시로 채웠다. 그런데 chip의 쓸모는 "매번 반복되는 상황을 한 번에 고르기"이지 "지난 행사 이름 되부르기"가 아니다 — 행사 이름은 어차피 직접 적어야 정확하고, **입력한 값은 2시간 유지되므로 같은 자리에서 여러 장을 찍을 때 다시 타이핑할 일이 없다.**
  - 이제 chip은 `고객사 방문 미팅 / 우리 회사 방문 미팅 / 전시회·박람회 / 세미나·컨퍼런스 / 투자·IR 미팅 / 채용 면접 / 소개 자리` 7종 고정이다.
  - placeholder는 `예: 2026 스마트팩토리전 부스 (직접 적어도 돼요)`로 바꿔, chip은 상황·입력은 구체적인 답이라는 역할 분담을 보여 준다.
  - placeholder가 칸 너비를 넘겨 `예: 2026 스마트팩토리전 부스 (직접 적어…`처럼 잘리던 것을 고쳤다. **placeholder 잘림을 회귀 게이트로 고정** — 실제 글꼴로 텍스트 폭을 재서 칸 안에 들어오는지 확인한다(input의 `scrollWidth`는 value 기준이라 placeholder 잘림을 못 잡는다).
  - 회귀 게이트가 **행사 이름이 chip으로 올라오지 않는 것**까지 강제한다 — fixture에 `2026 로보월드` 기록이 있어도 chip 목록에는 연도·행사명 패턴이 하나도 없어야 한다.

## [Unreleased] — 인터뷰 003 실사용 후속: 만남 맥락 가독성 · AI 조사 요청 범위 확대 (branch `agent/int-000015-followup`, Kairen-Ref: INT-000015, ISS-000082, ISS-000084, TSK-000218, TSK-000220)

founder가 배포본을 실제로 열어 본 뒤 나온 다섯 가지 지시를 반영했다.

- **`AI 조사 요청`이 진짜 필요한 판단까지 맡는다**: 예시가 `최근 경력·회사 소식`처럼 무난한 것들뿐이라 무엇을 시킬 수 있는지가 좁게 읽혔다. 이제 **실력·전문성 추정 / 의사결정 권한 / 평판·레퍼런스 / 최근 성과와 실패 / 이해관계·경쟁 관계**를 먼저 꺼내 놓는다. 묻기 껄끄럽지만 만나기 전에 알아야 하는 것들이다.
  - **권한을 넓힌 게 아니다.** 기존 `public-research-v1`의 mode는 원래 `public_professional_background`이고, 금지되는 `민감 특성`은 정치성향·종교·성적 지향·건강·질병·인종이다. 공개된 결과물을 근거로 한 직업적 판단은 처음부터 계약 안이었다 — 화면이 그걸 좁게 보여 주고 있었을 뿐이다.
  - 대신 조건을 화면에 못 박았다: `공개된 결과물(경력·논문·특허·발표·제품·기사·평가)을 근거로 판단까지 합니다. 근거와 확신도를 함께 적고, 모르는 건 모른다고 남겨요.` 그리고 하지 않는 일을 시각적으로 분리해 함께 표시한다.
- **만남 맥락이 처음부터 펼쳐진다**: 촬영 직후 바로 쓰는 칸이라 접어 둘 이유가 없었다. 접기 버튼은 그대로 남는다.
- **입력 칸이 입력 칸으로 보인다**: 카드 배경(#f8fafd)과 입력 배경(#f5f7fb)이 사실상 같은 색인데 테두리마저 없어 "글쓰기 박스인지 구별이 안 되는" 상태였다. 카드는 흰색, 입력은 눌린 회색 + 또렷한 테두리 + 50px 높이 + 14.5px 글자로 분리했다. 라벨도 Ionic 기본 라벨(placeholder와 같은 회색조)에서 진한 잉크색 12.5px 굵은 글씨로 꺼냈다.
- **만난 자리 예시가 실무 동선으로 바뀐다**: 기록이 없어도 `전시회 부스 / 고객사 방문 / 우리 사무실 / IR·투자 미팅 / 세미나·학회 / 채용 면접`이 먼저 뜬다. 실제로 입력한 적 있는 만난 곳이 있으면 그게 항상 앞이다.
- **음성 입력 안내가 처음부터 보인다**: `키보드 마이크로 말해도 돼요`가 메모 placeholder 안에 묻혀 있었다. 이제 `만남 맥락` 제목 옆 배지로 올렸다 — 가장 빠른 입력 수단이 가장 늦게 발견되던 문제다.
- **접수 버튼이 화면 밖으로 밀리지 않는다**: 예시 chip이 8개로 늘자 조사 요청 시트에서 `조사 요청 접수` 버튼이 화면 아래로 밀려 **누를 수 없게 됐다**(회귀 게이트가 잡았다 — 844px 화면에서 버튼 하단이 986px). 버튼을 시트 바닥에 고정하고 조사 요청 시트만 더 높게 연다.
- **날짜가 바뀌면 빨개지던 게이트 수정**: `지난주` 회귀 fixture가 "8일 전"이라 일→월 요일이 넘어가는 순간 창 밖으로 밀렸다. 주 시작(월요일) 기준으로 계산해 어느 요일에 돌려도 같은 구간에 들어오게 했다.

## [Unreleased] — 개선 인터뷰 003 반영: 만남 맥락 정리 · AI 조사 요청 표면 · 즉시 응답 사람 찾기 · 다중 후보 (branch `agent/int-000015-ai-surfaces`, Kairen-Ref: INT-000015, ISS-000082, ISS-000084, ISS-000103, TSK-000218, TSK-000220, TSK-000249)

- **`기억할 맥락`이 `만남 맥락` 카드가 됐다**: 네 칸이 같은 무게로 쌓이고 칸마다 `(선택, 2시간 유지)`가 반복돼, 무엇을 왜 적는지가 안 읽히고 촬영 버튼이 화면 아래로 밀렸다. 이제 **기본은 접힘**이고, 열면 `어디서 만났나요?`가 첫 칸이며 안내는 영역 위에 한 번만 나온다. 최근에 실제로 입력한 만난 곳과 자주 쓰는 관계(`잠재 고객`, `오늘 처음` 등)를 **chip으로 먼저 제안**한다. 입력이 생기면 접힌 줄에 `2026 로보월드 · Kairen: 잠재 고객 · 나: 오늘 처음`처럼 **저장될 내용 그대로** 요약된다.
- **`조사 지시` → `AI 조사 요청`**: 일반 메모와 똑같이 생긴 입력 박스라 AI에게 일을 맡기는 기능임이 드러나지 않았다. 이제 전용 표면(구분되는 배경·테두리 + sparkle 표식 + `소유자 전용` 배지)에 놓이고, 접수 뒤 거치는 단계를 **미리** 보여 준다 — `작성 중 › 접수됨 › 공개 자료 조사 중 › 출처 정리 중 › 완료`. 예시 chip은 이미 쓴 문장을 지우지 않고 덧붙인다. **권한은 넓히지 않았다** — owner 전용, 공개·합법 출처만, 로그인 자료·민감 특성 추론·외부 전송 없음이 화면에 그대로 적혀 있다. 캡처 화면과 인물 카드가 같은 표면을 쓴다.
- **AI 사람 찾기가 네트워크를 기다리지 않는다**: 예전에는 문장을 넣으면 **먼저 서버에서 기록 100건을 받아 온 뒤에** 대조를 시작했다 — 체감 지연의 전부가 그 왕복이었다. 이제 이 기기에 이미 있는 기록으로 **즉시** 후보를 내고(실측 0.0초대), 그다음 배경에서 최신 기록을 받아 다시 대조한다. 문장이 밖으로 나가지 않는 계약은 그대로다.
- **기다리는 동안 무엇을 하는지 보인다**: 불투명한 로딩 대신 실제 단계를 보여 준다 — `말한 단서를 해석하고 있어요` → `이 기기에 있는 기록 86건을 대조하고 있어요`(실제 건수) → `가능성 높은 후보부터 정리하고 있어요`. **남은 시간은 지어내지 않고** 경과 시간과 `중단` 버튼을 준다. 끝나면 `기록 4건 대조 · 0.2초`처럼 실측값으로 말한다.
- **결과가 한 명으로 수렴하지 않는다**: `한화`처럼 여러 명이 걸리는 질의에서 후보를 전부 남기고 `후보 3명`과 적용된 단서 chip(`지난주`, `한화`)을 함께 보여 준다. 근거가 둘 이상 맞으면 `강하게 일치`, 하나뿐이거나 시점이 살짝 벗어나면 `가능성 있음`으로 나눈다. 후보가 많으면 소속·만난 곳 chip으로 즉시 좁힌다. 후보 상한도 12명 → 60명으로 올렸다.
- **못 찾은 이유를 구분한다**: `조건에 맞는 기록이 없어요`와 `이 기기에 아직 기록이 없어요`를 다르게 말한다.
- **OCR이 이름 줄을 쪼개도 살린다**: 실제 명함 렌더가 흐릴 때 `Jane Kim`이 `Jane |` + `Kim` 두 박스로 나뉘고 신뢰도가 함께 떨어져 이름을 통째로 놓쳤다(CI 실측, 벤치 9/10). 같은 줄 판정을 중심 거리 대신 **세로 구간 겹침**으로 바꾸고, 직함 줄이 붙어 있거나 이메일이 뒷받침하면 낮은 신뢰도 줄도 버리지 않으며, 이메일 아이디(`jane.kim`)로 쪼개진 낱말을 되붙인다. `@` 앞 공백도 이메일로 읽는다.
- **회귀 게이트 신설**: 맥락 카드가 접힌 채 시작해 저장될 내용만 요약하는지, AI 조사 요청이 권한을 넓히지 않고 단계·경계를 보여 주는지, 사람 찾기가 서버 응답 전에 후보를 내고 진행 단계를 말하는지, 회사 질의가 후보를 전부 남기고 좁혀지는지를 실제 앱 경로로 검증한다. 수정 전 빌드에서 4건 모두 FAIL을 확인했다.

## [Unreleased] — 개선 인터뷰 002 반영: 자연어 사람 찾기·상단 정리·갤러리 정책·프로필 UX (branch `agent/int-000013-card-capture`, Kairen-Ref: INT-000013, ISS-000058, ISS-000066, ISS-000084, ISS-000102, ISS-000103, TSK-000220)

- **기억나는 대로 말해서 사람을 찾는다**: 검색 화면에 `AI 사람 찾기`가 생겼다. "지난주쯤 만났던 사람인데 한화 다니던 구매팀장이었어"처럼 문장으로 말하면 시점(`지난주`→7월 13일~19일)·소속·만난 곳·직함으로 나눠 기록과 대조하고, 후보마다 **왜 그 사람인지**를 근거 줄로 보여 준다. 기존 `빠른 검색`(이름·회사·만난 곳)은 그대로 남아 기본값이다.
  - **문장은 기기 밖으로 나가지 않는다**: 해석과 대조를 전부 휴대폰 안에서 한다. 외부 AI를 호출하지 않고, 기기 기록에서 못 찾았을 때만 기존 검색에 **단어 하나**(예: `한화`)를 넘긴다.
  - **기록에 없는 건 지어내지 않는다**: "여자였어", "안경 썼어" 같은 조건은 명함 기록에 없으므로 조용히 무시하지 않고 *"성별은 명함 기록에 남기지 않아 조건으로 쓰지 않았어요"* 라고 알려 준다. 못 찾은 단어도 그대로 되돌려 말한다.
- **명함 사진이 휴대폰 갤러리를 더럽히지 않는다**: 카이렌 카메라로 찍은 사진은 원래 갤러리에 저장되지 않는다 — 갤러리에 사본을 만드는 건 `기본 카메라 앱으로 찍기` 경로뿐이었다. 이제 그 버튼은 기본으로 숨기고(설정 `기본 카메라 앱 쓰지 않기`), 카메라가 열리지 않는 기기에서만 *"갤러리에 남아요"* 를 붙여 예외로 보여 준다. 캡처 수정 화면의 다시 찍기도 앱 카메라를 쓴다.
  - **전송이 끝난 원본을 기기에서 지운다**: 예전에는 최근 50건의 명함 원본이 전송 뒤에도 기기에 계속 남았다. 이제 서버가 성공을 확인하고 10분이 지나면 원본을 지우고 목록용 작은 썸네일만 남긴다. 대기·실패 항목은 재전송을 위해 그대로 둔다.
  - **지울 수 없는 건 지운다고 하지 않는다**: 이미 갤러리에 쌓인 사진은 웹 앱이 삭제할 수 없다 — 설정에서 그 사실을 그대로 알린다.
- **상단이 제품 이름 대신 지금 상태를 보여 준다**: 최상단 `Kairen Card Capture 모바일 메모리` + 떠 있는 새로고침 조합을 없앴다. 상단 바가 **현재 화면 이름 + 한 줄 상태**(`3건 처리 중`, `모두 최신`, `연결 필요`)와 새로고침 하나를 소유하고, 화면마다 또 그리던 큰 제목을 걷어냈다 — 폰에서 내용이 약 90px 위로 올라온다.
- **사람을 열면 `지금 알아둘 것`이 먼저 보인다**: 인물 전문 맨 위에 최근 변화·다음 대화 단서·Kairen 접점을 최대 3줄로 보여 준다. 요약을 새로 쓰지 않고 기록의 문장을 **그대로 인용**하며, 어느 섹션에서 왔는지와 언제 확인된 기록인지를 함께 표시한다. 해당 내용이 기록에 없으면 아무것도 만들지 않는다.
- **프로필의 웹 주소를 바로 연다**: 회사 홈페이지·LinkedIn·기사처럼 브리핑 본문, 표, 프로필 메타데이터에 있는 `http`·`https` 주소를 링크로 표시하고 새 탭에서 안전하게 연다. 실행형 주소나 앱 내부 상대 경로는 링크로 만들지 않는다.
- **액션을 목적에 따라 묶는다**: 전화·문자·메일·연락처 저장은 `연락`, 메모·조사 지시는 `기록`, 전체 프로필·수정 요청·캡처 수정은 `관리`로 나눴다. 메모 추가만 파란색으로 강조하던 근거 없는 우선순위도 제거했다.
- **브라우저 기본 팝업을 앱 입력창으로 교체한다**: 메모 추가·조사 지시·수정 요청은 맥락 설명, 입력 예시, 글자 수, 취소·저장 동작이 있는 앱 내부 입력창에서 작성한다. 주소나 기술 용어가 노출되던 브라우저 문구는 더 이상 표시되지 않는다.
- **설정 문구를 사용자 관점으로 바꾼다**: `연결과 경계` 같은 내부 용어 대신 `내 앱 설정`, `이 기기`, `사용자·연결 정보 편집`처럼 실제로 무엇을 확인하고 바꾸는지 설명한다. API 주소와 토큰은 `고급 설정` 안에 유지한다.
- **회귀 게이트 신설**: 상단 바가 화면 이름·상태·새로고침을 소유하는지, 회상 검색이 근거를 붙이고 기록에 없는 속성을 조건으로 쓰지 않는지(문장이 네트워크로 나가지 않는지 포함), 기본 카메라 경로가 기본으로 숨는지, 인용 하이라이트가 출처를 밝히는지를 실제 앱 경로로 검증한다. 수정 전 빌드에서 4건 모두 FAIL을 확인했다.

## [Unreleased] — 이름 인식 캐시 오염 제거 · 진행 단계 표시 · 자동 새로고침 안내 (branch `agent/tsk-000249-name-and-progress`, Kairen-Ref: ISS-000096, TSK-000249)

- **다른 사람 이름이 나오던 원인 제거**: OCR 라이브러리가 결과를 캐시하는데, 캐시 키가 **이미지 앞 1024바이트(좌상단 256픽셀) 해시 + 길이**였다. 명함은 대개 같은 비율로 크롭되고 좌상단이 여백이라 키가 충돌해서 **직전에 처리한 사람의 인식 결과가 그대로 나올 수 있었다.** 캐시를 껐다(캡처당 한 번만 호출하므로 손해도 없다).
- **성이 떨어져 나가지 않는다**: 자간이 넓은 이름을 OCR이 `이 강 윤`처럼 쪼개 놓으면 `강윤`만 잡히거나 아무것도 못 잡았다. 한글 토큰만으로 이루어진 줄은 합쳐서 한 이름으로 되돌리고, 고른 이름이 다른 후보의 끝부분이면 더 긴 쪽을 택한다.
- **이름 인식 벤치 신설**: 실제 PP-OCR 엔진이 뱉은 줄을 실제 픽커에 그대로 먹여 10개 명함 레이아웃에서 정확도를 잰다 — **2/10 → 10/10**.
- **처리 진행이 한 사다리로 보인다**: 기기 쪽(전송 대기·전송됨)과 서버 쪽(접수됨·처리 중·완료)이 서로 다른 문구로 흩어져 있던 것을 **4단계 하나의 진행**으로 합쳤다 — 사진 전송 → 서버 접수 → 이름·정보 인식 → 웹 조사·기록 정리. 카드마다 `4단계 중 3단계 · 이름·정보 인식 중`과 `8분 경과 · 약 7분 남음 · 보통 6~20분`, 그리고 단계 막대를 보여 준다. 오래 걸릴 때는 남은 시간을 약속하는 대신 재요청·문의 버튼을 띄운다.
- **자동 새로고침이 언제 도는지 보인다**: 상단 한 줄 상태와 기록 섹션에 `방금 업데이트 · 12초 뒤 자동 새로고침`처럼 남은 시간이 표시된다(주기 20초, 앱 복귀 시 즉시 갱신).

## [Unreleased] — 본 대로 잘리는 촬영 결과 · 뒷면 확인 단계 · 목록 한 줄 요약 (branch `agent/tsk-000246-wysiwyg-crop`, Kairen-Ref: ISS-000100, TSK-000246)

- **화면에서 본 사각형 그대로 잘린다**: 라이브 미리보기는 명함을 잡아 놓고, 촬영 시점에는 그 사각형을 버리고 전체 프레임에서 **처음부터 다시 감지**하고 있었다. 조건이 달라 재감지가 실패하면 결과물이 프레임 전체로 나왔다 — "기껏 인식해 놓고 왜 안 자르냐"의 정체다. 이제 촬영 직전 1.5초 안에 화면에 그려진 사각형을 그대로 써서 자른다. 그게 없을 때만 재감지 → 가이드 크롭 → 전체 프레임 순으로 내려간다.
- **뒷면도 "이대로 괜찮나요?"를 묻는다**: 앞면만 확인 단계가 있고 뒷면은 촬영 즉시 닫혔다. 자동 촬영이면 결과를 볼 기회조차 없었다. 이제 뒷면도 `완료` / `뒷면 다시 찍기`를 고를 수 있다.
- **명함 기록이 "이름 — 한 줄 요약"으로 보인다**: 목록이 브리핑 제목을 그대로 써서 모든 항목이 `이런 분이에요`였다. 이제 브리핑 본문의 첫 문장을 한 줄 요약으로 보여 준다(머리글·불릿·표는 건너뛰고 64자에서 자름). 요약이 아직 없으면 진행 상태를 보여 준다. 브리핑 파일 형식과 파싱 규칙은 그대로다.

## [Unreleased] — 명함 감지 알고리즘 재설계와 이름 오탐 수정 (branch `agent/tsk-000244-detect-v2`, Kairen-Ref: ISS-000101, TSK-000244, ISS-000096, TSK-000245)

- **수직으로 내려다봐도 명함을 잡는다**: 기존 감지기는 카드 윤곽이 한 군데라도 끊기면 후보 자체를 못 만들었다. 사선 촬영이 잘 되던 건 드롭 섀도가 윤곽을 그려 줬기 때문이고, 수직에서는 폰이 광원을 가려 그 그림자가 사라진다 — 즉 **그림자에 의존하는 알고리즘**이었다. 이제 조명을 먼저 평탄화하고, edge map 3종(Canny·모폴로지 그래디언트·adaptive)에서 **Hough 직선 교점 + 컨투어 + 최소 외접 사각형**을 모두 후보로 만든 뒤 고른다. 실측(감지 벤치): 수직·그림자 없음 검출률 0.50 → **1.00**(IoU 0.48 → 0.98), 수직·저대비 0.75 → **1.00**(IoU 0.17 → 0.84).
- **박스가 촐싹대지 않는다**: 후보 선택 기준을 "면적 최대"에서 **실제 경계 근거(edge support) + 종횡비 + 직각도 + 면적** 합산 점수로 바꿨다. 여기에 직전 프레임 사각형에 대한 근접 보너스·교체 비용·데드밴드를 더하고, 화면 보간을 적응형으로(조금 움직이면 천천히, 크게 움직이면 빠르게) 만들었다.
- **엉뚱한 박스 대신 "못 찾음"**: 점수가 최소 품질선에 못 미치면 사각형을 그리지 않는다. 배경의 큰 사각형을 명함이라고 우기지 않는다.
- **이름 인식이 로고를 사람 이름으로 착각하지 않는다**: 이름 선택 규칙이 "카드에서 가장 큰 글자"였는데, 로고를 크게 넣는 명함에서는 정반대로 작동했다. 이제 **한국 성씨 prior**(2~4자 한글은 실제 성씨로 시작해야 함 — `카이렌`·`로보틱스` 제거), **직함 인접 가점**, **이메일 아이디 대조**, **전부 대문자 영문 감점**을 함께 본다. 확신 가는 사람 이름이 없으면 회사명을 채우는 대신 **비워 둔다**.
- **더 정확해졌는데 더 빨라졌다**: 프레임당 비용이 깊은 프레임 18~31ms, 가벼운 프레임 7~9ms다(데스크톱, 320px 축소 프레임). 조명 추정을 1/4 축소본에서 계산하고, 가벼운 프레임은 직전 사각형이 아직 경계 위에 있으면 재탐색 없이 그대로 추적한다. 깊은 프레임은 항상 다시 찾으므로 잘못 잡힌 박스가 굳지 않는다.
- **감지 벤치 신설**: 빌드된 감지 워커를 그대로 불러 실패 조건 장면(그림자 없음·폰 그림자·저대비·반사광·어두운 책상·나무 책상)에 대해 검출률·IoU·프레임 간 떨림·프레임당 지연을 수치로 낸다. 앞으로 감지 알고리즘 변경은 이 수치로 판정한다.

## [Unreleased] — 뒷면이 안 찍히던 문제와 잘려 보이던 결과물 수정 (branch `agent/tsk-000243-capture-flow`, Kairen-Ref: ISS-000100, TSK-000243)

- **뒷면이 실제로 찍힌다**: `뒷면도 찍기`를 누르면 명함을 뒤집기도 전에 **1.5초 만에** 자동 촬영이 다시 발사돼, 앞면이 뒷면으로 저장되고 있었다(실측). 이제 명함이 화면에서 한 번 벗어난 뒤에만 자동 촬영이 재개된다 — 뒤집는 동안은 `명함을 뒤집어 뒷면을 대주세요`로 안내하고, 오래 걸리면 수동 버튼을 안내한다. 수동 촬영은 언제나 가능하다.
- **촬영 결과가 잘리지 않는다**: 촬영 직후 확인 화면의 썸네일이 `cover`라 가로로 긴 명함이 세로에 맞춰 확대되고 양옆이 잘려 "잘못 찍힌 것처럼" 보였다. 확인 화면·대기열 목록·편집 화면의 명함 이미지를 전부 잘리지 않게(`contain`) 바꿨다.
- **대기열에서 앞·뒷면 여부가 보인다**: 목록 행에 `앞면` / `앞·뒷면` 표시를 추가했다 — 예전에는 편집 화면을 열어야만 뒷면이 담겼는지 알 수 있었다.
- **촬영 흐름 회귀 게이트 신설**: 장면을 바꿀 수 있는 합성 카메라로 앞면 자동 촬영 → 같은 장이 남아 있는 6초 동안 재발사 없음 → 명함 교체 후 뒷면 자동 촬영 → 두 장 저장까지를 실제 앱 경로로 검증한다.

## [Unreleased] — 감지 사각형이 명함 위로 뜨던 오프셋 수정 (branch `agent/tsk-000241-overlay-geometry`, Kairen-Ref: ISS-000098, TSK-000241)

- **감지 사각형이 명함에 정확히 앉는다**: 인식은 되는데 박스만 위로 떠 있던 증상을 고쳤다. 미리보기의 video가 grid 아이템이라 `height:100%`가 auto로 풀려 비디오가 스테이지보다 길어져 있었고(폰 화면 계측: 오버레이 341x455 위에 비디오 341x606), 화면에는 카메라 프레임의 **위쪽**이 보이는데 앱은 가운데가 보인다고 계산해 박스를 76px 위에 그렸다. legacy(v1.0)에 있던 `position:absolute; inset:0`을 복원하고, 좌표 변환을 **비디오·오버레이의 실제 렌더 상자를 재서** 만들도록 바꿨다 — 레이아웃이 다시 바뀌어도 같은 오차가 재발하지 않는다.
- **촬영 결과물도 함께 교정**: 감지 실패 시의 가이드 영역 크롭이 같은 좌표 변환을 쓰므로, 잘려 나오던 위치가 함께 맞춰졌다.
- **미리보기가 프레임 가운데를 보여 준다**: 이전에는 카메라 프레임의 위쪽 3/4만 보였다. 이제 legacy처럼 가운데를 보여 준다.
- **회귀 게이트를 진실값 기준으로 교체**: 기존 게이트는 기대 위치를 앱과 **똑같은 공식으로 다시 계산**해, 좌표 변환이 통째로 틀려도 항상 통과했다(실폰 보고가 반복돼도 CI는 초록이었다). 이제 오버레이를 숨기고 화면을 찍어 **실제로 보이는 명함**을 픽셀에서 찾아 비교한다. 수정 전 빌드에서 FAIL(세로 오차 73px) → 수정 후 PASS(3px)를 확인했다.

## [Unreleased] — 명함 감지 즉시 동작 복원 (branch `agent/tsk-000237-live-detect-debug`, Kairen-Ref: ISS-000098, TSK-000239)

- **카메라를 열면 곧바로 명함을 잡는다**: 감지 엔진을 앱 시작 직후(유휴 2.5초)에 워커에서 미리 기동하도록 되돌렸다. 직전 버전은 프리즈를 막으려고 엔진 실행을 카메라 진입 시점으로 미뤘는데, 실기기에서는 10MB 엔진 컴파일이 수십 초 걸려 **그동안 감지가 아예 죽고 화면 42% 지점의 고정 가이드 사각형만 떠 있었다**(갤럭시 폴드 7 재현 보고: "명함 위로 올라오는 사각형 인식이 완전 안 됨", "legacy는 잘 됨"). 엔진이 워커에서 돌기 때문에 미리 기동해도 화면은 멈추지 않는다.
- **대기 상태를 오해 없이 표시**: 아직 명함을 못 찾은 대기 프레임은 **점선**으로 그리고 코너 브래킷을 숨겨 감지된 박스와 구분한다. 엔진이 준비 중이면 "명함 감지 엔진 준비 중 N초… 지금도 촬영할 수 있어요"로 경과를 알린다.
- **느린 기기에서 박스가 끊기지 않게**: 감지 박스 유지 시간을 0.45초 → 1.1초로 늘려 왕복이 느린 기기에서도 사각형이 카드에 붙어 있는다.
- **감지 회귀 게이트 신설**: 카메라 스트림을 주입해 실제 앱 경로를 구동하는 e2e 두 종 — (1) **엔진 준비를 기다리지 않고** 카메라 진입 후 박스가 카드에 붙기까지의 시간을 재는 게이트(이번 회귀가 재발하면 실패), (2) 실사 시나리오 5종(기울기·반사광·저대비·복잡한 배경·둥근 모서리)에서 박스가 카드 위에 앉는지 검증하는 매트릭스.

## [Unreleased] — 빠른 이름 인식 엔진 교체: PP-OCRv5 한국어 (branch `agent/tsk-000236-ppocr-quickname`, Kairen-Ref: ISS-000096, TSK-000236)

- **인식 엔진 교체**: 촬영 직후 "이름 먼저 확인"이 Tesseract에서 **PaddleOCR PP-OCRv5 모바일(한국어 인식 모델)**로 바뀌었다. 공개 벤치마크에서 한국어 포함 scene text 정확도가 Tesseract를 크게 앞서는 최신 신경망 OCR이다. 게이트 실측(합성 명함): 이름·직함·회사·연락처 4줄 전부 정확 인식, 추론 0.7초.
- **이름 선택 로직 교체**: "카드에서 가장 큰 글씨의 사람 이름" — 단어 박스 크기(글꼴 크기)·신뢰도 기반 픽커로 바꿔 회사명·직함을 이름으로 뽑는 오탐을 줄였다. 직함이 같은 줄에 붙은 경우("홍길동 대표이사")도 이름만 분리한다.
- **온디바이스·자체 호스팅 유지**: 모델(det 4.7MB + 한국어 rec 13.4MB + 사전)과 ONNX Runtime WASM(13.5MB)을 전부 자체 호스팅하고 전용 Web Worker에서 실행한다 — 명함 이미지는 외부로 나가지 않고, 메인 스레드 프리즈도 없다(게이트 실측 long task 0ms). 자산은 pinned hash로 validate에 고정.
- **폴백 유지**: PP-OCR 초기화 실패 기기는 기존 TextDetector → Tesseract 경로로 그대로 동작한다. 준비가 늦으면(7초 상한) 해당 캡처만 폴백을 쓰고 다음 캡처부터 새 엔진을 쓴다.
- **품질 게이트 신설**: 실제 모델을 워커에 로드해 합성 한국어 명함의 이름 정확 추출 + 메인 스레드 long task 한도를 CI에서 강제한다.

## [Unreleased] — 카메라 프리즈 근본 수정: 감지 엔진 워커 격리 (branch `agent/tsk-000230-opencv-worker`, Kairen-Ref: ISS-000091, TSK-000230)

- **감지 엔진을 Web Worker로 완전 이전**: OpenCV(WASM ~10MB)의 로드·컴파일·명함 감지·perspective 보정·흐림 점수가 전부 별도 스레드에서 돌아, 카메라를 여는 순간을 포함해 **어떤 시점에도 메인 스레드가 잠기지 않는다.** 직전 수정(prefetch+지연 실행)은 다운로드 대기만 없앴을 뿐 컴파일 블로킹이 카메라 진입 시점에 남아 폰에서 "준비 중… + 전 버튼 무반응"이 재현됐다 — 이번이 근본 수정이다.
- **프리즈 회귀 게이트 신설**: 실제 vendor 엔진을 워커에 로드·감지시키며 메인 스레드 long task를 계측하는 Playwright 게이트 추가 — 측정: 엔진 로드 603ms 동안 메인 스레드 최대 점유 66ms(이전 방식은 404ms+ 블로킹, 폰에서는 수 배).
- **새 배포 자동 반영**: 새 버전이 활성화되면 페이지를 한 번 자동 새로고침한다 — 홈 화면 앱이 "한 번 전 실행" 빌드를 계속 보여주던 혼선 제거(최초 설치 시에는 새로고침하지 않음).
- **빌드 표시**: 설정 화면 하단에 빌드 시각을 표시해 "지금 무슨 버전을 보고 있나"를 바로 확인할 수 있다.

## [Unreleased] — 실폰 1차 피드백 반영 (branch `agent/tsk-000230-phone-feedback`, Kairen-Ref: ISS-000091, ISS-000094, ISS-000095, TSK-000230~232)

- **카메라 프리즈 해소**: 명함 감지 엔진(OpenCV WASM)을 앱 유휴 시점에 **내려받기만** 하고, 실행·컴파일은 카메라 미리보기가 뜬 뒤로 옮겼다. 촬영 버튼을 누른 직후 메인 스레드가 잠겨 아무 버튼도 반응하지 않던 문제가 사라졌다(측정: 실행 시 메인 스레드 점유 약 0.4초/데스크톱). 엔진 준비 전에도 수동 촬영·가이드 크롭은 그대로 동작하며 안내 문구도 그렇게 바뀌었다.
- **2시간 유지 실동작**: 만난 곳·관계·조사 지시를 입력 즉시 저장한다. 완료를 누르지 못하고 앱을 벗어나도 값이 유지되며, 과거 캡처를 수정해도 현재 맥락이 덮어써지지 않는다. 조사 지시도 2시간 유지 대상에 포함했다.
- **명함 기록 통합**: 최근 캡처와 받은 명함 브리핑을 하나의 목록으로 합쳤다. 같은 명함이 두 곳에 나뉘어 보이지 않고, 브리핑 카드 안에서 내가 적은 맥락과 캡처 수정 진입을 함께 볼 수 있다. 전송 대기·실패 항목은 대기 행으로 남는다.
- **화면 간결화**: 캡처 첫 화면의 큰 제목·설명 문단을 한 줄로 줄이고, 캡처 화면의 사람 검색 바로가기를 없앴다(하단 검색 탭 유지).
- **조사 지시 예시**: 자주 쓰는 형태를 입력창 예시로 넣었다 — 최근 경력·이직, 회사 투자·뉴스, 인터뷰·발표, 공통 접점.
- **새로고침 반응**: 우측 상단·목록 새로고침이 진행("새로고침 중…")과 완료를 즉시 알린다.
- **전방위 인물 조사**: 처리 절차의 웹 보강 규칙을 LinkedIn 편중에서 소스군 체크리스트로 바꿨다 — 사람 6군(전문 프로필·뉴스/인터뷰·발표/컨퍼런스·논문/특허/GitHub·협회/수상·최근 90일 활동) 중 4군 이상, 회사 5군 중 3군 이상을 실제로 조회하고, 한글·영문·이니셜 질의 변형과 동일인 2개 이상 근거 일치, 출처별 신뢰도·확인일·충돌 기록을 남긴다. 브리핑에는 "만나기 전에 알면 좋은 것" 대화 포인트 3~5개가 들어간다. 공개·합법 출처 한정과 민감 정보 금지 경계는 그대로다.

## [Unreleased] — 캡처 작업 화면·온보딩 복원 (branch `agent/tsk-000228-capture-surface`, Kairen-Ref: ISS-000091, TSK-000228)

- **한 화면 촬영 흐름 복원**: 캡처 탭이 랜딩형 hero에서 legacy 작업 화면으로 돌아왔다 — 촬영 버튼·기기 내 이름 확인·기억할 맥락 4필드(스티키 값이 촬영 전에 보임)·완료가 한 화면에 있고, 최근 캡처·받은 명함 브리핑이 같은 스크롤에서 접기 상태(legacy와 같은 저장 키)로 이어진다. 완료 후에는 만난 곳·관계를 유지한 채 즉시 다음 명함을 찍을 수 있다.
- **카메라 모달은 촬영 전용**: 앞면 촬영 직후 카메라를 벗어나지 않는 선택지(뒷면도 찍기 / 뒷면 없이 완료 / 앞면 다시 찍기)를 복원했고, 스트림을 유지해 뒷면 촬영이 즉시 시작된다. 맥락 입력·이름 확인·완료는 메인 화면이 소유한다.
- **링크 우선 온보딩 복원**: 첫 실행은 legacy처럼 이름 한 칸("처음 오셨네요 👋")만 묻는다. 주소·토큰 입력은 설정의 "고급 — 직접 연결 설정" 뒤로 숨고, 토큰 라벨이 "개인 링크 토큰 (?k= 값)"과 자동 저장 안내로 바뀌었으며, 토큰 없이 열면 개인 링크 안내 배너가 뜬다.
- **회귀 게이트**: Playwright `parity-restore`에 한 화면 캡처 표면·접기 저장·온보딩 게이트 검증을 추가했다 (총 10/10).

## [Unreleased] — legacy 파리티 회귀 복원 (branch `agent/tsk-000222-react-parity-regressions`, Kairen-Ref: ISS-000091, TSK-000222, TSK-000223)

- **브리핑·프로필 렌더링 복원**: 브리핑 본문과 전체 프로필이 마크다운 원문 덤프 대신 legacy `mdLite` 규칙(제목·불릿·구분선·화살표·표, escaped pipe 포함)으로 렌더링된다. 전체 프로필에는 미팅 프렙 카드(이름·직함·회사·마지막 확인·바로 연락)와 frontmatter 요약 박스가 돌아왔고, 검색 결과 프로필에서도 메모 추가·조사 지시를 바로 실행할 수 있다.
- **연락처 추출 폴백**: 서버 contact 요약이 없는 기존 브리핑에서도 본문 텍스트에서 전화·메일을 추출해 전화·문자·메일·연락처 저장 버튼을 복원한다.
- **최근 캡처 맥락 복원**: 처리 완료된 브리핑의 이름이 로컬 캡처 목록에 매핑되고, 만난 곳·관계 맥락 줄·바로 메모 버튼·"예전 캡처 더 보기" 페이지네이션이 돌아왔다. 새 캡처는 104px 썸네일을 저장해 전송 후 원본이 정리돼도 목록 이미지가 유지된다.
- **진행 문구·제목 규칙 복원**: 처리 대기 3단계 문구(통상 소요·완료까지 약 N분 남음)와 captureId 폴백 경과 계산, note/조사 지시 receipt의 "메모 → 대상" 제목, skipped 항목의 만난 곳 폴백 제목을 복원했다.
- **오프라인 신뢰성**: 배경 20초 자동 새로고침 실패가 더 이상 토스트를 띄우지 않는다(수동 새로고침만 안내). 서버 오류 코드는 legacy처럼 한글 안내로 매핑되고, owner 게이트는 localStorage에 캐시되어 전파가 약해도 전체 프로필·검색 UI가 유지된다. 최근 검색 칩은 누르면 즉시 검색한다.
- **카메라 라이브 오버레이 복원**: 감지된 명함 사각형을 주변 스크림·코너 브래킷·프레임 보간·잠금 상태로 실시간 표시하고(자동 촬영이 꺼져 있어도 표시), 자동 촬영 진행 링을 스테이지에 그린다. 감지 실패 시 가이드 영역 크롭 폴백으로 배경 전체 업로드를 막고, 촬영 직후 흐림 경고를 복원했다.
- **빠른 이름 보호**: 사용자가 이미 입력·수정한 이름을 OCR 완료가 덮어쓰지 않으며, Tesseract 모델을 유휴 시간에 미리 로드해 첫 촬영의 이름 인식 대기를 줄인다.
- **회귀 게이트**: 위 파리티를 단위 테스트 36건과 Playwright `parity-restore` e2e 3건으로 고정했다.

## v2.0.0 — 2026-07-26 — `c9036b3` (Kairen-Ref: TSK-000221)

- **운영 승격**: 기본 Pages URL이 token·view query를 보존해 React 앱을 열고, 기존 static 앱은 `legacy.html` 복구 경로로 보존한다. root와 React service worker가 서로의 cache를 지우지 않도록 경계를 분리했다.
- **검색 진입점**: 하단 내비게이션의 모호한 `사람` 탭을 `검색`으로 바꾸고 캡처 첫 화면에 `사람 검색` 바로가기를 추가해 이름·회사 검색을 즉시 발견할 수 있게 했다.
- **운영 app shell**: React·TypeScript·Vite와 Ionic React로 `docs/next/` 앱을 만들고 legacy 앱은 `docs/legacy.html` rollback baseline으로 유지한다.
- **style ownership**: Ionic은 mobile shell·safe area·modal·toast를 소유하고, Tailwind는 Preflight 없이 Kairen-owned layout·content에만 사용한다.
- **contract adapter**: 기존 GAS `list`·`search`·`doc`·`persondoc`·`requeue`·`addnote`·`researchinstruction`·`correction`·upload payload와 IndexedDB `cardcapture/q`를 typed boundary로 고정했다.
- **설정·offline queue parity**: 기존 `?api`·`?k` 링크와 `cc_*` 저장 key를 그대로 받고, IndexedDB reopen·captureId 순차 전송·local `sent` 비중복·online/visibility 재전송·실패 data/tries·수동 재시도·50건 이후 sent 원본 정리를 복원했다.
- **offline shell**: 취약한 Workbox dependency를 채택하지 않고 작은 build-time generator로 `/next/` scope 전용 service worker와 web manifest를 생성한다. root와 legacy cache는 React cache와 분리한다.
- **server-off recovery gate**: Playwright가 실제 정적 서버를 종료한 뒤 Chrome reload를 수행해 cached shell·navigation이 복구되는지 검증한다.
- **촬영 parity**: 후면 camera permission·resolution·failure mapping·track cleanup, 앞·뒷면, retake, torch, 기본 카메라 fallback, stable auto-capture, 맥락·메모·owner 조사 지시를 React 앱에 연결했다.
- **captured-image queue gate**: camera frame을 legacy와 같은 최대 2000px long edge·JPEG 0.85로 만들고 기존 IndexedDB queue에 보관한 뒤 설정된 기존 GAS로 자동 전송한다. 미설정·offline에서는 POST 0을 유지하고 연결 복귀 뒤 같은 captureId로 보낸다.
- **quick-name OCR boundary**: legacy `nameCandidate`와 parity를 고정하고, 기기 `TextDetector`를 우선한 뒤 pinned self-hosted Tesseract로 fallback해 `quickName`을 같은 queue payload에 보존한다. OCR 실패는 촬영·로컬 보관을 막지 않는다.
- **OpenCV geometry boundary**: 기존 Otsu/Canny·adaptive threshold·명함 비율 scoring·perspective warp를 typed service로 분리하고, 엔진 지연·검출 실패 때 전체 프레임으로 비차단 fallback한다. 실제 명함 검출 품질 판정은 phone gate에 남긴다.
- **read/action parity**: 최근 캡처 상세·정보/사진 수정·같은 captureId 재전송, 20초 briefing refresh·지연 재처리·pagination·offline cache, Person profile, 전화·문자·메일·vCard, 사후 메모·조사 지시·수정 요청, 최근 검색과 PWA shortcut을 복원했다.
- **운영 전환**: 기존 사용자 기능을 React 앱에 직접 연결하고 legacy app은 rollback link로 유지한다. 계약·브라우저·CI 검증과 founder 실사용 확인 뒤 PR #8을 병합하고 Pages·GAS·watcher를 운영 배포했다.

## [Unreleased] — capture experience wave (branch `agent/card-capture-interview-wave`, Kairen-Ref: TSK-000178, TSK-000217, TSK-000218, TSK-000219, TSK-000220)

- **이름 먼저 확인**: 촬영 직후 브라우저 내장 OCR을 먼저 시도하고, 미지원 기기에서는 자체 호스팅 Tesseract.js 한국어+영어 모델을 사용한다. 이름은 즉시 확인·수정할 수 있으며 기기 OCR 결과와 provenance를 `quickName`으로 보존한다. 명함 이미지는 제3자 OCR 서비스로 전송하지 않는다.
- **안정 감지 자동 촬영**: 명함 사각형, 프레임 간 흔들림, 선명도, 심한 과노출을 함께 확인해 안정되면 자동 촬영한다. 사용자는 자동 촬영을 끄거나 언제든 수동 셔터·기본 카메라 폴백을 사용할 수 있다.
- **저대비 감지 보강**: 빠른 Canny 경로가 명함을 놓치면 적응형 임계값 경로를 제한 주기로 실행해 흰 배경·약한 테두리 인식을 보강한다.
- **시각 품질 개선**: 브랜드 헤더, 촬영 우선 계층, 차분한 색·타이포·카드 표면, 접근 가능한 포커스와 자동 촬영 진행 피드백을 적용했다.
- **조사 지시 탭**: owner는 최초 명함 등록과 기존 Person 카드에서 메모와 분리된 조사 지시를 남길 수 있다. GAS는 owner·feature flag·target 일치를 재검증하고 raw/requester/target/time/receipt/policy provenance를 별도 저장한다.
- **조사 지시 안전 경계**: raw 원문을 system prompt에 합치지 않고 public·lawful source bounded plan으로만 처리한다. private/login 자료, credential, 민감 특성 추론, doxxing, 외부 send/write, paid API, protected write와 human gate 우회는 실행하지 않는다.
- **회귀 검증**: 자동 촬영의 정상·오발·흔들림·흐림·과노출을 독립 순수 함수와 결정적 테스트로 고정했다.
- **조사 지시 회귀 검증**: owner/guest, 최초/기존 Person, target mismatch, note 혼입, prompt injection, private·sensitive·doxxing·credential·external·paid effect, source conflict를 합성 fixture로 고정했다.
- **처리 상태 정합성**: 브리핑 목록 조회는 cache-busting과 `no-store`로 최신 서버 상태를 읽고, 재처리 요청은 POST에서 terminal 상태를 재검증해 이미 완료·건너뜀인 카드를 `received`로 되돌리지 않는다. 재처리 경과 시간은 최신 `receivedAt`부터 다시 계산한다. (Kairen-Ref: TSK-000161)

## [Unreleased] — watcher v3 (branch `agent/watcher-v3`)

- **워처 v3 카드별 처리**: 대량 연속 캡처를 한 번의 긴 실행이 아니라 가장 이른 캡처부터 **한 건씩** 처리 → 카드마다 폰에 하나씩 도착하고, 사이마다 하트비트·backlog가 갱신됨. 무한 루프 방지 상한(25장/실행)·무진행 가드 포함 (ISS-000065, fixture 31/31 PASS). **라이브 워처 교체는 별도 단계**(로컬 pull+재시작, Setup 참조).

## v1.x UI (merged: PR #2 `agent/ux-retake`, PR #3 `agent/ux-round3`, PR #4 `agent/ux-round4`)

- **앞면 다시 찍기**: 앞면 촬영 직후 선택지([뒷면도 찍기 / 뒷면 없이 완료])에 "앞면 다시 찍기" 추가 — 카메라를 벗어나지 않고 앞면 교체 (2026-07-24 실사용 피드백), sw v13.

## [Unreleased] — v1.1 후보 (branch `agent/product-baseline-v1`, Kairen-Ref: TSK-000140~164)

추가 (배포 전 — GAS 재배포·사람 승인 필요):
- **바로 연락**: 브리핑·프렙 카드에서 전화·문자·메일·연락처 저장(vCard) — brief 텍스트 추출 폴백으로 기존 캡처도 동작, 처리 계약의 `contact` 요약 필드로 정확도 향상.
- **미팅 프렙 카드**: 검색 결과·전체 프로필 상단에 이름·직함·회사·마지막 확인·연락 버튼 요약 먼저 표시.
- **검색 상단 이동 + 최근 검색 3건** — 미팅 직전 10초 회상 동선 단축. 홈화면 **shortcuts**(인맥 검색/브리핑, `?view=`) 추가.
- **다시 처리 요청**: 30분 이상 지연된 캡처에서 사용자가 직접 재큐잉(`action=requeue`) + 문의 메일 버튼.
- **연속 촬영 동선**: 전송 즉시 상단 복귀 — 행사장에서 다음 명함을 끊김 없이.
- **검색**: owner 토큰 한정 Person 검색(`action=search`) + 웹앱 "검색" 탭 — 이름·회사로 축적된 인맥을 폰에서 회상.
- **사후 메모**: 브리핑·검색 결과에서 "메모 추가"(`action=addnote`) — 회의 끝나고, 또는 나중에 기억났을 때 그 사람 기록에 병합(-note 캡처로 파이프라인 재사용).
- **더 보기**: 브리핑(`action=list` limit/offset·hasMore)과 최근 캡처 목록에서 예전 것까지 조회.
- **처리 진행 표시**: 대기 중 브리핑에 경과 시간·통상 소요(6~20분)·30분 초과 경고 표시 — "깜깜무소식" 해소 (2026-07-24 실사용 피드백).
- **촬영 확인 토스트 상단 이동**: 하단 토스트가 "뒷면 추가" 버튼을 가리던 문제 수정 (2026-07-24 실사용 피드백).
- **처리 완료 알림**: 워처가 처리 완료 시 GAS(`action=notify`)를 통해 소유자 메일로 알림(0원, 옵트인 설정 파일).
- **수정 요청**: 브리핑에서 "수정 요청" 전송(`action=correction`) → 다음 처리 때 사용자 정정으로 반영.
- **워처 v2**: health 파일(`watcher-health.json`)·상태 점검 스크립트·연속 실패 추적·백로그 나이 노출 + 처리 프롬프트에 쓰기 allowlist·untrusted 입력 방어 삽입.
- **저장소 거버넌스**: AGENTS.md, SECURITY.md(위협 모델·토큰 runbook), RELEASE.md(release evidence 계약), CHANGELOG.md, `scripts/validate.ps1`(secret scan 포함), `eval/`(합성 회귀 fixture 16종 + 채점기).

## v1.0 — 2026-07-23 — `2d4704c`
- 어디서·관계 유지 필드에 2시간 만료 도입(오래된 행사명이 다음 캡처에 잘못 남는 문제 방지).

## v0.9 — 2026-07-23 — `6103230`
- 브리핑 자동 새로고침이 펼쳐 읽던 항목을 접지 않음(내용 무변경 시 재렌더 생략).
- 관계 필드 순서를 "Kairen과의 관계" 먼저로 변경, 최근 캡처 목록 표시 개편(이름·어디서 중심, 메모 숨김).

## v0.8 — 2026-07-23 — `01bc624`
- 브리핑 20초 자동 새로고침.
- 워처 인코딩 수정(UTF-8 BOM — 한글 경로 FATAL 해결), GAS가 Drive 중복 파일 중 최신 capture.json을 읽도록 수정(폰 '처리 대기중' 오표시 해결).

## v0.7 — 2026-07-23 — `c4554b1`
- 카메라 화면 개선(스크림+코너 브래킷+보간), 처리 엔진을 Codex로 전환(로그인 단계 제거).

## v0.6 — 2026-07-23 — `9177196`
- 브리핑에서 "전체 프로필 보기" — Person `.md` 전문 뷰어(`action=persondoc`, owner 한정).
- 즉시 처리 워처 도입(파일 이벤트+60초 폴링+시작 스윕, processing.lock).

## v0.5 — 2026-07-23 — `e496d02`
- "받은 명함 브리핑" 탭 + GAS `action=list`(토큰 scope, OWNER_NAMES 전체 열람).

## v0.4 — 2026-07-23 — `e914e1b`
- 뒷면 선택 명확화, 최근 캡처 상세(다시 찍기·수정·재전송).

## v0.3 — 2026-07-23 — `ff00186`
- 캡처 컨텍스트 4필드: 어디서 만났는지(유지형)·나와의 관계·Kairen과의 관계·메모.

## v0.2 — 2026-07-23 — `9433d19`
- 인페이지 카메라 + OpenCV.js 명함 사각형 자동 인식·크롭·원근 보정(self-hosted).

## v0.1 — 2026-07-23 — `7f9d007`
- 캡처 PWA가 기본 GAS 배포 URL을 내장.

## v0 (G0) — 2026-07-23 — `d85f248`
- 업로드 API(Code.gs: ping/whoami/POST)와 캡처 PWA 최초 공개(토큰 검증, Drive inbox 저장).
