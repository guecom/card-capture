# Changelog — Kairen Card Capture

사용자에게 보이는 변화 중심으로 기록한다. 형식: 버전 — 날짜 — 커밋. 배포(GAS/Pages) 시점은 `RELEASE.md`의 release evidence가 진실이다.

## [Unreleased]

## v2.17.0 — 2026-07-30 — `46ac9a3` (Kairen-Ref: TSK-000161; ISS-000050)

**완료된 명함·AI 조사 카드를 더 빨리 알아봅니다** (Kairen-Ref: TSK-000161; ISS-000050)

- 처리 중인 명함이나 AI 조사 요청이 있으면 앱이 4초마다 최신 상태를 확인합니다. 전부 끝나면 기존 20초 주기로 돌아가므로, 완료를 모르고 화면을 오래 보며 기다리는 시간이 줄어듭니다.
- 자동 확인·수동 새로고침·앱 복귀가 겹쳐도 서버에는 한 번씩만 묻습니다. 새 등록·조사 요청 직전의 조회와 겹치면 그 조회가 끝난 직후 한 번 더 확인해 새 카드를 놓치지 않습니다.
- 연결 해제 뒤 늦게 도착하거나 시작된 이전 연결의 응답이 화면과 사적 캐시를 다시 채우지 못하게 막았습니다.
- 앱을 닫은 뒤의 push·email 알림은 이번 범위가 아닙니다. 앱이 화면에 떠 있을 때의 완료 인지를 빠르게 하는 변경입니다.

## v2.16.0 — 2026-07-28 — `3fdcd4d` (Kairen-Ref: TSK-000328; ISS-000130)

**흰 화면에 갇히지 않게 한다** (branch `agent/iss-white-screen`, Kairen-Ref: TSK-000328; ISS-000130)

founder 보고: "컴퓨터에서는 들어가지는데, 핸드폰에서는 흰 화면만 보여."

- **앱 화면을 항상 최신 배포에서 먼저 받아온다**: 지금까지는 기기에 저장해 둔 예전 화면을 먼저 썼습니다. 그 예전 화면이 가리키는 파일은 새 배포가 나오면 이름이 바뀌기 때문에, 기기에서 그 파일이 사라진 경우 아무것도 못 열고 **하얀 화면**만 남았어요. 이제 인터넷이 있으면 항상 최신 화면을 받고, 인터넷이 없을 때만 저장해 둔 화면을 씁니다.
- **앱이 안 열리면 이유를 말하고, 직접 고칠 수 있습니다**: 지금까지는 실패하면 정말 아무것도 없었어요. 이제 무엇이 잘못됐는지 적힌 화면과 **`앱 새로 받기`** 버튼이 뜹니다. 저장된 화면을 비우고 최신 버전을 새로 받아요. **보내야 할 촬영은 지워지지 않습니다.**
- 정상적으로 열릴 때는 이 화면이 절대 뜨지 않습니다.

## v2.15.0 — 2026-07-28 — `00e7b0c` (Kairen-Ref: TSK-000326 · TSK-000325; ISS-000129)

**멈춘 이유를 말하고 다시 켤 수 있게 한다 · 서체를 실제로 싣는다** (branch `agent/tsk-motion-font`, Kairen-Ref: TSK-000326; ISS-000129)

founder가 실기기 캡처와 함께 두 가지를 보고했다. 하나는 지난 수정이 듣지 않은 것이고, 하나는 지난 회차에 미뤄 둔 것이다.

- **설정에 `화면 움직임`이 생겼다 (켜기 / 끄기 / 시스템)**: 실기기에서 빛이 안 보인 이유는 폰이 **움직임 최소화**를 켜고 있어서였다. 그 상태에서 우리 빛은 한 픽셀도 그려지지 않는다(실측 확인). 그건 원래 존중해야 하는 설정이지만, **말없이 사라지면 고장으로 읽힌다.** 이제 멈춰 있으면 왜 멈췄는지 설정 화면이 알려 주고, `켜기`를 고르면 폰 설정과 무관하게 앱 안에서 다시 움직인다. 아무것도 고르지 않으면 지금까지처럼 폰 설정을 따라갑니다.
- **AI 표면의 빛이 더 또렷해졌다**: 세기를 약 두 배로 올리고, 움직임을 매 프레임 다시 칠하는 방식에서 화면 합성만 하는 방식으로 바꿨다 — 폰에서 프레임이 떨어져도 띠가 멈춰 보이지 않는다.
- **글자가 제대로 된 서체로 나온다**: 지금까지 앱은 `Inter`·`Pretendard`를 쓰겠다고 적어만 두고 **한 번도 싣지 않았다.** 그래서 모든 기기가 OS 기본 서체로 떨어졌고, 화면 위계를 위해 쓰는 굵기 15단계가 굵게/보통 두 가지로 뭉개져 있었다(실측: `650`과 `780`의 글자 폭이 완전히 동일). 이제 한글 음절 전체를 담은 가변 서체를 앱이 직접 싣는다 — 제목·라벨·본문의 굵기 차이가 화면에서 실제로 보인다.
  - 서체는 1.7 MB이고 **필요할 때 받습니다.** 받기 전이나 인터넷이 없을 때는 지금까지의 시스템 서체로 그려지고 기능은 하나도 달라지지 않아요.

**버전이 다시 어긋나지 않게 막는다** (branch `agent/w7-version-sync`, Kairen-Ref: TSK-000317; ISS-000123)

앱 화면은 이번에 바뀌지 않는다. v2.14.0이 고친 "설정 화면의 버전이 실제 릴리즈와 어긋난다"가 **다시 일어나지 않도록** 하는 변경이다.

- **어긋남을 CI가 막는다**: `frontend/package.json`의 version, `CHANGELOG`의 최신 릴리즈 heading, **실제로 붙어 있는 릴리즈 tag** 셋이 맞지 않으면 merge 전에 실패한다. 특히 **CHANGELOG에 기록되지 않은 tag**를 잡는다 — v2.13.0에서 실제로 일어난 형태가 그것이었다(두 값이 *함께* 뒤처져 서로 일치했기 때문에 두 곳만 대조하는 검사로는 보이지 않았다).
- **릴리즈 절차에서 버전 확정이 1단계가 된다**: tag를 만든 뒤로 미루지 않는다. 배포 검증에도 "앱 설정 화면이 이 tag와 같은 버전을 말하는가"가 들어간다.
- **v2.12.0의 워처 하드닝이 실제로 적용됐다**: 그 항목에 달려 있던 "라이브 워처 재기동(사람) 전까지 적용되지 않는다" 경고가 해소됐다. 운영 워처 파일이 저장소와 byte 동일해졌고(`525e23340ef3…`), 그 파일이 갱신된 **뒤에** 프로세스가 새로 떴음을 시작 시각으로 확인했다. 워처 상태 파일도 정상이다 — 하트비트가 돌고 있고 로그 쓰기 실패가 0건이다.
- **다만 예전 워처 로그에 남아 있던 명함 내용은 아직 지워지지 않았다** (`ISS-000127`): 정리했다고 기록해 두었지만 실제 파일에는 적용되지 않았다. 정리를 수행한 에이전트가 샌드박스 안에서 돌아 사본을 지웠기 때문이다. **새로 쌓이지는 않는다** — 처리기 출력 분리가 실제로 돌고 있어 재기동 이후 줄에는 명함 내용이 없다. 남은 것은 그 이전에 쌓인 약 2.9 MB이고, 사람이 한 번 지워야 한다.

**만난 기록이 만난 시각을 지어내지 않는다** (branch `agent/w9-encounter-contract`, Kairen-Ref: TSK-000325)

> ⚠️ **라이브 워처 재기동(사람) 전까지 적용되지 않는다.** 그동안에도 명함 처리는 정상 진행된다 — 만남 기록만 잠시 만들지 않는다.

- **명함을 언제 찍었는지와 언제 만났는지를 구분한다**: 지금까지는 만난 시각 자리에 **명함을 찍은 시각**을 넣었다. 실제로 넉 건이 그렇게 기록됐고, 그중 하나는 "지지난달 만났다"는 메모가 있는데도 촬영 시각이 들어가 **약 2개월** 어긋났다. 이제 단서가 있으면 그 시각을, 없으면 **"모름"이라고 적는다.** 찍은 시각은 캡처 기록에 그대로 남으므로 "적어도 이때 이전에 만났다"는 사실은 잃지 않는다.
- **만남과 작업 세션을 다른 서랍에 넣는다**: 지금까지 둘이 한 곳에 섞여, 관계 타임라인을 열면 창업자 혼자 진행한 제품 개발 세션 기록이 함께 나왔다. 이제 사람을 만난 기록은 만남 전용 서랍에 들어간다.
- **같은 사람을 다시 만나면 기록이 하나씩 쌓인다**: 예전 기록을 덮어쓰지 않는다.

**버전이 다시 어긋나지 않게 막는다** (branch `agent/w7-version-sync`, Kairen-Ref: TSK-000317; ISS-000123)

앱 화면은 이번에 바뀌지 않는다. v2.14.0이 고친 "설정 화면의 버전이 실제 릴리즈와 어긋난다"가 **다시 일어나지 않도록** 하는 변경이다.

- **어긋남을 CI가 막는다**: `frontend/package.json`의 version, `CHANGELOG`의 최신 릴리즈 heading, **실제로 붙어 있는 릴리즈 tag** 셋이 맞지 않으면 merge 전에 실패한다. 특히 **CHANGELOG에 기록되지 않은 tag**를 잡는다 — v2.13.0에서 실제로 일어난 형태가 그것이었다(두 값이 *함께* 뒤처져 서로 일치했기 때문에 두 곳만 대조하는 검사로는 보이지 않았다).
- **릴리즈 절차에서 버전 확정이 1단계가 된다**: tag를 만든 뒤로 미루지 않는다. 배포 검증에도 "앱 설정 화면이 이 tag와 같은 버전을 말하는가"가 들어간다.
- **v2.12.0의 워처 하드닝이 실제로 적용됐다**: 그 항목에 달려 있던 "라이브 워처 재기동(사람) 전까지 적용되지 않는다" 경고가 해소됐다. 운영 워처 파일이 저장소와 byte 동일해졌고(`525e23340ef3…`), 그 파일이 갱신된 **뒤에** 프로세스가 새로 떴음을 시작 시각으로 확인했다. 워처 상태 파일도 정상이다 — 하트비트가 돌고 있고 로그 쓰기 실패가 0건이다.
- **다만 예전 워처 로그에 남아 있던 명함 내용은 아직 지워지지 않았다** (`ISS-000127`): 정리했다고 기록해 두었지만 실제 파일에는 적용되지 않았다. 정리를 수행한 에이전트가 샌드박스 안에서 돌아 사본을 지웠기 때문이다. **새로 쌓이지는 않는다** — 처리기 출력 분리가 실제로 돌고 있어 재기동 이후 줄에는 명함 내용이 없다. 남은 것은 그 이전에 쌓인 약 2.9 MB이고, 사람이 한 번 지워야 한다.

## v2.14.0 — 2026-07-28 — `def1a0d` (Kairen-Ref: TSK-000312; ISS-000119)

**색과 모서리를 한 체계로 정리하고, AI 표면의 빛이 박스 전체를 지나게 한다** (branch `agent/tsk-dark-refine`)

founder가 다크 모드 실기기에서 관찰한 여섯 항목이다. 증상마다 값을 하나씩 고치는 대신, 색·모서리·높이가 각각 한 곳에서 정해지도록 바꿨다.

- **왼쪽 위 `K` 표식이 테마를 따라간다**: 다크에서 이것만 파랑이었다. 원인은 강조색이 CSS 34곳에 손으로 박혀 있던 것이라 촬영 버튼 테두리·아바타·칩도 같이 파랑으로 남아 있었다. 이제 강조색은 테마 토큰 한 곳에서 나온다.
- **다크에서 AI 표면의 은은한 빛이 실제로 보인다**: 빛은 꺼져 있지 않았고 **박스를 다 지나지 못했다** — 한 주기 동안 폭의 72%(다크)·48%(라이트)만 스쳐서, 빛나는 박스가 아니라 구석의 얼룩으로 읽혔다. 이제 표면보다 넓은 띠가 왼쪽 밖에서 오른쪽 밖까지 건너간다(두 테마 모두 폭 100%).
- **`AI 사람 찾기`가 `AI 조사 요청`과 같은 색이다**: 사람 찾기만 초록 계열이라 다른 기능처럼 보였다. "기기 안에서 대조한다"는 경계는 배지 글자가 그대로 말한다.
- **모서리와 그림자에 위계가 생겼다**: 12~25px로 제각각이던 반경을 네 칸으로, 18~50px로 번지던 그림자를 두 칸으로 줄였다. 알약 모양은 **읽는 이름표**에만 남기고 누르는 칩에는 각을 준다.
- **파랑·보라를 덜 쓴다**: 촬영 버튼의 두 색 그라데이션, 목록마다 반복되던 진한 아바타, 배경의 보라 후광을 한 단계씩 낮췄다.
- **다크에서 브라우저 상단 바 색이 화면과 맞는다**: 앱 배경은 보라 계열인데 상단 바만 파랑 계열이었다.

움직임을 끈 기기에서는 지금까지처럼 빛이 멈추되 사라지지 않는다. 다크 네 화면의 글자 대비는 렌더된 픽셀에서 다시 재 전부 기준 이상이다.

## v2.13.0 — 2026-07-27 — `c763892` (Kairen-Ref: TSK-000305; FI-034)

**기다리는 자리마다 지금 실제로 일어나는 일만 말한다** (branch `agent/w6-ux-truth`)

founder가 실기기에서 관찰한 것들이다. `App.tsx`의 대기 지점 22개를 전수 판정해 그중 5개를 고쳤다.

- **`명함 기록` 옆의 `전송 중…`이 진짜 보낼 때만 뜬다**: 전송 표시가 전역 스위치 하나여서 20초 자동 주기와 재연결 때마다, **보낼 촬영이 하나도 없어도** 켜졌다. 이제 어느 명함을 보내는 중인지 이름으로 말하고, 그 명함의 행이 직접 표시한다.
- **상단 `N건 처리 중`의 N이 무엇인지 알 수 있다**: 이 폰에 남은 미전송 촬영과 서버가 처리 중인 캡처를 한 숫자로 더하고 있었다. 이제 `전송 대기 N건 · 서버에서 M건 처리 중`으로 나뉜다.
- **설정에 앱 버전이 보인다**: 기존 빌드 식별자 옆에 사람이 말할 수 있는 릴리즈 버전이 함께 표시된다.
- **빠른 검색이 찾는 동안 결과 자리에 진행이 보인다**: 무엇을 찾는 중인지와 경과 시간을 보여 준다(있지도 않은 진행률은 만들지 않는다).
- **`다시 처리 요청`·`다시 보내기`·`저장하고 다시 보내기`가 눌린 것을 알려 준다**: 세 버튼 모두 누른 뒤 아무 반응이 없어 두 번 누르기 쉬웠다.
- **다크 테마가 파랑이 아니라 보라 중심이 된다**: 색조만 옮겼고 성공·경고·위험 색의 뜻과 라이트 테마는 그대로다. 네 화면 151개 글자의 대비를 렌더된 픽셀에서 다시 재 전부 기준 이상(최저 4.64:1)임을 확인했다.
- **AI 기능 박스의 테두리가 아주 은은하게 빛난다**: 움직임 최소화를 켠 기기에서는 빛이 멈추되 **사라지지 않는다**.

## v2.12.0 — 2026-07-27 — `e4014e4` (Kairen-Ref: TSK-000300, TSK-000301, TSK-000302)

founder가 승인한 세 결정을 담는다. **`Code.gs` 변경 없음.**

### 워처 로그에서 명함 내용을 분리하고 보존 기간을 둔다 (branch `agent/w5-d1-log-pii`, Kairen-Ref: TSK-000300)

> ⚠️ **라이브 워처 재기동(사람) 전까지 적용되지 않는다.**

- **워처 로그가 더 이상 명함 내용을 담지 않는다**: 처리기 원본 출력이 `watcher.log`에 그대로 이어 붙고 있었다 — 실측 **48,252줄 중 48,169줄(99.8%)이 명함 내용·이름·연락처**였다. 이제 처리기 출력은 캡처별 파일로 나가고 **90분 뒤 지워진다**(= 시도 상한 3회 × lease 30분, 새 수치를 만들지 않고 기존 임계값에서 유도). `watcher.log`에는 어느 캡처를 언제 처리했고 출력이 어디 있는지만 남는다.
- **로그를 못 쓰면 그 사실이 보인다**: 지금까지 로그 쓰기 실패는 아무 흔적도 남기지 않았다. 이제 실패 사실과 잃은 줄 수가 진단에 뜬다.

### 이전 앱을 촬영·업로드·전송 상태 확인만 하는 화면으로 줄인다 (branch `agent/w5-d2-legacy-scope`, Kairen-Ref: TSK-000301)

- **설정의 `이전 앱 열기` 화면에서 브리핑 열람·인맥 검색·메모 추가·조사 지시·수정 요청·연락처 저장이 사라진다.** 남는 것은 촬영, 업로드, 내가 올린 명함이 어디까지 처리됐는지 보는 것뿐이다. 없앤 기능은 전부 새 앱에 그대로 있다.
- **이전 앱이 기기에 남기던 브리핑 사본을 지운다**: 받은 브리핑 전문·소유자 여부·검색 기록을 앱 구분 없는 자리에 저장해 왔다 — 폰을 다른 사람 링크로 열어도 남을 수 있는 자리였다. 이제 저장하지 않고, 예전에 저장된 것은 실행할 때 지운다.
- 처리 상태 표시가 `1/3단계 이름 인식 중`에서 `처리 중 · N분 경과`로 간략해진다.

### 배포된 앱에서는 연결 주소를 바꿀 수 없다 (branch `agent/w5-d3-endpoint-lock`, Kairen-Ref: TSK-000302)

- **고급 설정의 `연결 주소` 칸이 배포된 앱에서 읽기 전용이 된다**: v2.7.0부터 무엇을 넣어도 거부됐지만 칸은 계속 열려 있어 **바꿀 수 있는 것처럼 보였다.** 이제 주소는 그대로 보여 주되(어디에 연결돼 있는지는 알 권리) 편집을 막고 이유를 표시한다. 낭독기도 읽기 전용 상태와 이유를 함께 읽는다. 로컬 개발 환경에서는 종전대로 편집할 수 있다.
- **주소 전체가 한눈에 보인다**: 한 줄 칸이라 100자 넘는 주소의 앞부분만 보이던 것을 줄바꿈 표시로 바꿨다.
- "비상용 두 번째 주소는 만들지 않는다" 결정을 코드 계약과 회귀 게이트로 못 박았다.

## v2.11.0 — 2026-07-27 — `e25762f` (Kairen-Ref: TSK-000297)

**워처가 "지시로 읽지 않는다"고 못 박는 입력 채널을 계약과 맞추고, 검토 상태 승격 검사의 구멍을 막는다** (branch `agent/w4-c1-prompt-harden`, Kairen-Ref: TSK-000297; FI-020)

사용자에게 보이는 변화는 없다.

> ⚠️ **라이브 워처 재기동(사람) 전까지 워처 프롬프트 변화는 적용되지 않는다.**

- **명함·메모·웹 말고도 못 박는다**: 워처가 처리 세션에 주는 경계 문구는 세 채널만 나열했다. 계약이 신뢰하지 않는 입력으로 지정한 **수정 요청 본문·기기 인식 이름·조사 지시 원문**이 빠져 있었다(조사 지시 원문은 워처 파일에 **한 번도 등장하지 않았다**). 이제 여섯 채널을 모두 나열하고, 그 지시가 "정책을 바꿔라"·"쓸 수 있는 곳을 넓혀라"라고 해도 무시하라고 명시한다.
  - `event`(만난 곳)는 **넣지 않았다** — 계약 문서가 아직 untrusted로 지정하지 않았기 때문이다. 계약이 지정하면 게이트가 FAIL해서 함께 넣으라고 요구한다.
- **캡처 번호 검사에서 줄바꿈 한 글자가 통과하던 것을 막는다**: 뒤에 아무것도 실을 수 없어 실제 피해는 없었지만, 그 값이 처리 대상 지정 줄과 상태 파일 경로에 들어가므로 문자열 끝을 정확히 잡도록 좁혔다. 서버가 발급하는 정상 번호는 그대로 통과한다.
- **검토 상태 승격 검사가 따옴표 하나에 그냥 지나가던 것을 막는다**: `reviewStatus: "human_validated"`는 따옴표 없는 것과 같은 값인데 회귀 검사가 놓쳤고, 반대로 **계약이 요구하는 provenance 인용문을 승격으로 잘못 잡았다**(10건 중 6건 오판). 이제 값을 읽어 비교하고 그 판정 자체를 실제 PowerShell 10건으로 고정한다.

## v2.10.0 — 2026-07-27 — `8617ec8` (Kairen-Ref: TSK-000291, TSK-000292, TSK-000293)

**멈춰둔 명함이 저절로 다시 시도되던 문제를 고치고, 두 세션 동안 "검증 불가"로 남아 있던 두 가지를 실제 게이트로 만든다.**

> 이 릴리즈에도 **`Code.gs` 변경이 없다.**

### 멈춰둔 명함이 저절로 다시 시도되던 문제 수정 + 유령 워처를 시작 시각과 함께 보여준다 (branch `agent/w4-a4-watcher`, Kairen-Ref: TSK-000291; FI-019·FI-029)

> ⚠️ **라이브 워처 재기동(사람) 전까지 워처 동작 변화는 적용되지 않는다.**

- **처리에 실패해 잠시 멈춰둔 명함이, 아무도 손대지 않았는데 조용히 다시 시도되던 문제 수정**: 워처가 캡처 정보 파일을 못 읽은 순간(동기화 중 부분 기록)을 "내용이 바뀌었다"로 오해해 멈춤을 풀고 시도 횟수를 0으로 되돌렸다. 이 판정은 분당 여러 번 돌기 때문에 읽기 실패 한 번이 곧바로 재시도 3회로 번졌다. 이제 못 읽으면 멈춤을 그대로 유지한다. **사람이 내용을 고쳐 다시 보내면 새로 시도하는 동작은 그대로다.**
- **유령 워처를 시작 시각과 함께 보여준다**: 예전에는 pid만 나와서 여러 개가 떠 있을 때 어느 것을 이미 정리했는지, 어느 것이 먼저 떠서 자리를 붙잡았는지 알 수 없었다. 이제 먼저 뜬 것부터 `pid + 시작 시각`으로 한 줄씩 낸다.
- 진단이 "격리 해제"를 "격리 발생"으로 집계하던 문제 수정.

### 검증할 수 없던 두 가지를 실제 게이트로 (branch `agent/w4-b1-injection`·`agent/w4-b2-drive-stub`, Kairen-Ref: TSK-000292·TSK-000293; FI-020)

사용자에게 보이는 변화는 없다. 두 항목 모두 독립 보안 평가가 `UNPROVEN`으로 판정했던 것이다.

- **명함에 인쇄된 문장이 처리 지시를 덮어쓸 수 있는가**: 검사할 게이트가 하나도 없었다. 조사 결과 워처 프롬프트에는 **캡처에서 온 자유 텍스트가 아예 들어가지 않는다**(문자열이 통째로 리터럴이고, 끼워 넣는 값은 형식 검사를 통과한 캡처 번호 하나뿐). 그 방어를 프롬프트 실물과 대조해 고정했고, 적대적 캡처 번호 20종·주입 corpus 11종으로 형식적이지 않음을 매 실행 확인한다.
- **owner 전용 조회의 성공 경로**: Drive 계층 모사가 없어 거절 경로만 검증돼 있었다. 모사를 만들되 **무엇을 모사하지 않는지 10가지를 명시**했다 — 그 목록 없이는 통과가 의미 없기 때문이다.

## v2.9.0 — 2026-07-27 — `5f90f1b` (Kairen-Ref: TSK-000288, TSK-000289, TSK-000290)

**한 폰을 여러 개인 링크로 쓸 때 앞사람의 것이 뒷사람에게 넘어가던 경로를 막고, 빌드가 어느 소스에서 나왔는지 대조할 수 있게 한다.**

> 이 릴리즈에도 **`Code.gs` 변경이 없다.** 앱(`docs/`)만 바뀌므로 Pages 배포로 자동 반영된다.

### 앞사람이 적어 둔 만남 맥락이 뒷사람의 명함에 붙지 않는다 (branch `agent/w4-a2-subject-ctx`, Kairen-Ref: TSK-000289; FI-006·FI-007)

- **이게 화면 문제가 아니었다**: 만난 상황·관계 메모·조사 지시 초안은 개인 링크와 무관하게 폰 한 곳에 저장됐다. 그래서 한 폰을 다른 개인 링크로 열면 앞사람이 적은 값이 그대로 보였고, **다음 촬영에 그대로 붙어 서버에 올라갔다** — 앞사람이 어떤 인물에 대해 적은 메모가 뒷사람이 적은 것으로 기록됐다. 이제 개인 링크별로 분리되고, 링크 코드가 없으면 저장·복원되지 않는다.
- **이전 앱이나 예전 버전이 남겨 둔 브리핑 캐시·owner 권한 표시·검색 기록을, 그 뒤에 접속한 다른 개인 링크가 물려받지 않는다.** 예전에는 물려받았고, 그중 owner 권한 표시는 **서버 응답이 오기 전까지** 화면을 owner 것으로 켰다 — 오프라인이면 계속 켜져 있었다. 이제 그런 기록은 기기에서 지우고 서버에서 다시 받는다.

### 웹 잠금이 없는 브라우저의 중복 전송 (branch `agent/w4-a1-queue-lock`, Kairen-Ref: TSK-000288; FI-053)

- 탭 두 개가 같은 순간 전송을 시작하면 같은 명함이 두 번 올라갈 수 있던 문제를 **줄였습니다.** 완전히 막지는 못하며, **서버 중복 접수 방지가 배포되어야 닫힙니다.**
- 함께 고친 것: 먼저 끝난 탭이 다른 탭의 살아 있는 전송 권한을 지워 세 번째 탭까지 들어올 수 있던 경로.

### 화면의 빌드 식별자로 어느 소스에서 나온 앱인지 대조할 수 있다 (branch `agent/w4-a3-repro-build`, Kairen-Ref: TSK-000290)

- 설정 화면의 빌드 식별자가 빌드 **시각**(`2026-07-27 08:08Z`) 대신 **소스 내용 해시**(`src-1cacc263c248`)를 표시한다. 화면에 보이는 값을 저장소에서 다시 계산해 대조할 수 있다.
- 예전에는 소스를 하나도 바꾸지 않아도 빌드할 때마다 결과물이 달라져서, 배포된 앱이 어느 소스에서 나왔는지 확인할 방법이 없었다.

## v2.8.0 — 2026-07-27 — `e93fdfe` (Kairen-Ref: TSK-000287)

**개인 링크 코드가 폰에 영구 저장되던 세 번째 경로를 막는다.** 앞선 두 건(v2.7.0)과 같은 뿌리다.

### 초대 링크를 한 번만 열어도 개인 링크 코드가 폰에 영구 저장되던 문제 수정 (branch `agent/w3-sw-cache`, Kairen-Ref: TSK-000287; FI-007)

- **개인 링크 코드가 폰의 오프라인 캐시에 남지 않는다**: 예전에는 초대 링크(`…/?k=코드`)나 이전 앱 링크(`…/legacy.html?api=…&k=코드`)를 **한 번만 열어도** 링크 코드가 오프라인 캐시 목록에 주소 그대로 저장됐고, `연결 해제`를 해도 지워지지 않았다(실측). 이제 캐시에는 주소에서 코드를 떼어낸 형태만 저장되고, **이미 저장돼 있던 예전 캐시는 앱을 다시 열 때 통째로 지워진다.** 오프라인에서 앱이 열리는 동작은 그대로다.
- ⚠️ **이 수정으로 오프라인 캐시가 한 번 새로 만들어진다.** 앱 화면은 즉시 다시 받고(약 0.3MB), **이전 앱의 글자 인식 기능을 쓰던 폰은 다음 사용 때 인식 파일을 한 번 다시 받는다(최대 약 18.5MB).** 정상 화면 사용에는 영향이 없다.
- 남는 한계: 연결을 끊은 뒤 이 주소를 다시 열지 않는 폰은 예전 캐시를 그대로 들고 있다. Service Worker 생애주기상 클라이언트에서 해결할 방법이 없다 — 링크 코드가 실제로 유출됐다면 서버에서 코드를 회수하는 것이 진짜 대응이다.

### 왜 회귀 게이트가 이것을 못 잡았나

`credential-boundary.spec.ts`는 이미 "캐시 키에 링크 코드가 없다"를 단언하고 있었다. 그런데 `docs/index.html`·`docs/legacy.html`은 Service Worker를 `location.protocol === 'https:'`일 때만 등록하고 e2e harness는 `http://127.0.0.1`로 서빙한다 — **루트 워커는 어떤 테스트에서도 한 번도 등록된 적이 없었다.** 새 게이트는 루트 워커를 명시적으로 등록해서 연다.

또한 후보 앱(`next/`) 워커의 런타임 캐시 쓰기는 **실제로는 아무것도 저장하지 않는다** — `clone()`이 `caches.open()` 이후에 평가돼 매번 던진다(실측: 성공 fetch 4회 후 SHELL 밖 항목 0건). 그래서 같은 누출이 없었을 뿐이다. **그 순서를 "고치면" 같은 누출이 그대로 생긴다.** 경고 주석과 선제 게이트를 남겼다.

## v2.7.0 — 2026-07-27 — `35192bd` (Kairen-Ref: TSK-000285, TSK-000286)

**개인 링크 코드가 새어 나갈 수 있던 경로 두 가지를 막는다.** 둘 다 `FI-004`·`FI-005`로 v2.1.0에서 "닫혔다"고 기록돼 있던 것이고, 독립 보안 평가가 뒤집었다.

> 이 릴리즈에도 **`Code.gs` 변경이 없다.** 앱(`docs/`)만 바뀌므로 Pages 배포로 자동 반영된다.

### 개인 링크 코드가 나갈 수 있는 주소를 배포본 하나로 못박았다 (branch `agent/w3-origin-pin`, Kairen-Ref: TSK-000285; FI-004·FI-007)

- **같은 서버라도 다른 배포본이면 막는다**: 예전에는 주소의 **서버 이름**만 확인했다. 그런데 우리 서버(`script.google.com`)는 **누구나 자기 앱을 올릴 수 있는 공용 호스트**다. 그래서 그 서버를 가리키되 남의 앱으로 향하는 링크를 한 번 열면, 저장돼 있던 개인 링크 코드가 **그대로 그쪽으로 넘어갔다**(실측: 피해자 토큰이 붙은 요청이 공격자 주소로 나가는 것을 확인). 이제 앱에 들어 있는 주소와 **정확히 같을 때만** 연결하고, 아니면 왜 무시했는지 화면에 표시한다.
- **주소에 코드가 섞여 있어도 기기에 저장되지 않는다**: 링크 주소 뒤에 코드가 붙어 있으면 그 부분을 떼고 저장한다.
- **`연결 해제`가 저장된 연결 주소까지 지운다**: 예전에는 연결 주소가 남아 있었다. 촬영 대기열은 그대로 보존한다.

### 이전 앱 화면이 링크에 붙은 서버 주소를 더 이상 믿지 않는다 (branch `agent/w3-legacy-boundary`, Kairen-Ref: TSK-000286; FI-004·FI-005)

- **링크에 `?api=주소`를 붙여 보내면 그 주소로 명함과 개인 링크 코드가 전부 넘어갔다** — 설정의 `이전 앱 열기`로 열리는 화면 이야기다. 한 번만 열면 주소가 기기에 저장돼 그 뒤 모든 업로드·조회가 그리로 갔고, 새 앱과 같은 저장소를 쓰기 때문에 **새 앱까지 함께 오염됐다.** 이제 이전 앱은 앱에 들어 있는 주소로만 연결하고, 링크로 온 주소는 저장하지 않고 무시했다고 화면에 알린다. 예전에 저장돼 있던 다른 주소도 실행할 때 지운다.
- **개인 링크 코드가 주소창에 남지 않는다**: 초대 링크로 열면 코드는 기기에 저장하고 주소창에서는 즉시 지운다. 뒤로 가기로도 되살아나지 않는다.
- **링크 코드가 바깥으로 딸려 나가지 않는다**: 화면이 부르는 모든 요청에 링크 코드가 담긴 주소를 붙이지 않는다.
- 정상 초대 링크(`?k=`만 있는 링크) 사용에는 변화가 없다.

### 문서 정정

- `SECURITY.md`와 `AGENTS.md`가 허용된 공개 상수(`DEFAULT_API`)의 위치를 `docs/index.html`로 적고 있었으나 실제로는 `docs/legacy.html`에만 있다. 그 한 줄이 React 앱의 pinned endpoint 원본이기도 하다는 사실을 함께 적었다.

## v2.6.0 — 2026-07-27 — `c515873` (Kairen-Ref: TSK-000280, TSK-000281, TSK-000282, TSK-000283)

병렬 lane 4개(워처 상태 진단 · 기록 추적 · 목록/검색 · 업로드 바이트 검증)를 함께 담는다.

> ⚠️ **v2.5.0의 사람 게이트 두 가지가 아직 그대로다.** Apps Script 재배포와 라이브 워처 재기동. 이 릴리즈는 그것을 대신하지 않는다.
>
> 이 릴리즈에는 **`Code.gs` 변경이 없다.** 앱(`docs/`)과 워처 스크립트, 검증 게이트만 바뀐다.

### 100건 넘는 과거 기록도 끝까지 보이고, 검색 결과가 왜 맞았는지 말한다 (branch `agent/w3-recall`, Kairen-Ref: TSK-000282; FI-100·FI-104)

- **`예전 기록 더 보기`가 100건에서 멈추지 않는다**: 예전에는 눌러도 100건까지만 늘어나고 그 뒤로는 버튼만 남은 채 아무 일이 없었다 — 101번째 이후 기록은 앱에서 볼 방법 자체가 없었다. 이제 끝까지 이어 읽고, 몇 건을 더 받았는지·다 받았는지를 화면과 낭독기에 알린다.
- **이미 접수된 명함을 다시 올릴 수 있던 경로를 막았다**: 응답을 못 받은 촬영을 다시 보낼지 판단할 때 서버 기록을 100건만 확인해서, 캡처가 100건을 넘으면 오래된 것을 "서버에 없다"고 오판할 수 있었다(실측: 150건 중 101번째 이후가 통째로 빠짐). 이제 전부 확인하고, 다 확인하지 못하면 **"없다"고 단정하지 않는다.**
- **검색 결과에 왜 그 사람이 나왔는지가 보인다**: 이름이 맞으면 맞은 부분을 표시하고, 본문이 맞으면 맞은 줄에서 짧은 근거 문장을 함께 보여 준다. 프로필의 비공개 구획·내부 항목·접속 코드 형태 값은 근거에 절대 실리지 않고, 보여 줄 수 있는 구간이 없으면 근거를 지어내지 않는다.

### 워처가 죽었는지 한 번에 알 수 있다 (branch `agent/w3-watcher-health`, Kairen-Ref: TSK-000280; FI-029)

> ⚠️ **라이브 워처 재기동(사람) 전까지 새 워처 동작은 적용되지 않는다.** 다만 상태 점검 도구 자체는 지금 바로 쓸 수 있다.

- **예전 상태 점검은 기록된 프로세스 번호가 살아 있기만 하면 "정상"이라고 답했다** — Windows가 번호를 재사용하면 워처가 죽어 있어도 정상으로 보인다. 실측된 실제 사고가 정확히 그것이었다: 기록된 번호는 없는 프로세스였고, 로그를 한 줄도 쓰지 않는 다른 워처가 떠 있었다. 이제 그 프로세스가 **정말 워처인지**까지 확인해 정상/경고/위험을 단정한다.
- **밀린 명함·잡아 둔 처리 권한·격리된 명함**을 함께 보여 주고, 값이 없는 항목은 빈칸이 아니라 `모름`으로 표시한다(예전에는 빈칸이 0처럼 보여 경보가 조용히 꺼졌다).
- **점검 결과에는 명함 내용·이름·메모가 절대 나오지 않는다.** 예전에는 처리기 원본 출력을 그대로 이어 붙여 보여 줬다.

### 전송이 실패해도 명함 하나의 여정을 이어서 볼 수 있다 (branch `agent/w3-trace`, Kairen-Ref: TSK-000281; FI-021)

- 촬영 → 전송 시도 → 실패 이유 분류 → 서버 대조 → 접수까지를 하나의 흐름으로 확인할 수 있다. **진단 기록에는 이름·연락처·메모·행사 이름·사진이 남지 않는다** — 허용된 형태의 오류 코드만 기록되고 나머지는 전부 가려진다.

### 이미지가 아닌 파일이 명함으로 접수되는 것을 기계로 증명했다 (branch `agent/w3-content-type`, Kairen-Ref: TSK-000283; FI-012)

- 사용자에게 보이는 변화는 없다. 서버가 업로드 **바이트를 한 번도 보지 않는다**는 사실을 합성 corpus 10종(텍스트·HTML·SVG·PDF·ZIP·PNG 등)으로 재현해 회귀 게이트로 남겼다. 서버 수정은 **재배포 사이클이 정리된 뒤** 적용한다 — 미배포 변경을 더 쌓지 않기 위해서다.

## v2.5.0 — 2026-07-27 — `e335652` (Kairen-Ref: TSK-000276, TSK-000277, TSK-000278, TSK-000279)

병렬 lane 3개(워처·검증·앱)와 그 검증이 찾아낸 보안 수정을 함께 담는다.

> ⚠️ **사람이 해야 하는 일이 두 가지다.**
> 1. **Apps Script 재배포** — `Code.gs`가 v2.4.0에서 또 바뀌었다(보안 수정). 재배포 전까지 **live 서버는 여전히 취약하다.**
> 2. **라이브 워처 재기동** — `watcher/`가 바뀌었다. 재기동 전까지 실행 중 워처는 예전 프로토콜이다.
>
> 앱(`docs/`)은 자동 반영된다.

### 처리가 중간에 죽어도 복구되고, 나쁜 명함 하나가 줄을 막지 않는다 (branch `agent/par-watcher`, Kairen-Ref: TSK-000276; FI-017·018·019)

> ⚠️ **라이브 워처 재기동(사람) 전까지 적용되지 않는다.**

- **처리 중 한 건이 꼬여도 나머지가 계속 처리된다**: 예전에는 처리에 실패한 명함이 매 주기 다시 시도되며 실패 횟수만 올라가고 **그 뒤에 줄 서 있던 명함들이 계속 밀렸다**(실측: 같은 항목 6회 재시도, 뒤 항목 계속 대기). 이제 명함별 시도 상한에 도달하면 그 명함은 **지우지 않고** 줄에서만 빼고 다음으로 넘어간다. 다시 넣는 것은 기존 `재요청` 경로로만 된다.
- **"끝났다"고 기록됐는데 결과물이 없는 상태가 사라진다**: 예전에는 처리 프로그램이 오류 없이 끝나기만 하면 완료로 봤다. 실제로 브리핑 파일이 없어도 완료였다(실측). 이제 **처리 상태·사람 연결·비어 있지 않은 브리핑 파일이 모두 실제로 있어야** 완료로 인정한다.
- **두 워처가 같은 명함을 동시에 집지 않는다**: 예전 잠금은 전체 하나뿐이었고 확인과 생성 사이에 틈이 있었다. 이제 명함별로 소유권을 원자적으로 잡고, 소유자만 갱신할 수 있으며, 만료된 소유권은 회수되고 **회수당한 예전 소유자는 아무것도 확정할 수 없다.** 실제 두 프로세스를 띄워 정확히 하나만 이기는 것을 검증한다.

### 명함 처리 계약을 결정적 검증으로 고정 (branch `agent/par-evalsuite`, Kairen-Ref: TSK-000277; FI-022·023)

- **자동 검사가 명함 처리 계약을 지킨다**: 합성 명함 16종이 이제 CI에서 **자동으로** 업로드 기록과 목록 응답을 검증한다. 예전에는 사람이 손으로 처리한 뒤 채점하는 경로밖에 없어 자동 검사가 0건이었다. 서버가 정하는 값(누가 찍었는지, 처리 상태 등)을 앱이 위조할 수 없다는 것도 함께 잠갔다.
- **공격 시나리오 17가지가 막혀 있음을 증명한다**: 망가진 요청, 경로 벗어나기, 신뢰할 수 없는 기기 인식 결과, 남의 명함 건드리기, 메일 오발송, 하루 한도 우회. **이 검사를 만드는 과정에서 실제 보안 결함 3건이 나왔다** — 그중 가장 심각한 것이 위의 브리핑 조작이다.

### 남이 내 브리핑을 조작할 수 있던 구멍 차단 (branch `agent/tsk-000279-filename-allowlist`, Kairen-Ref: TSK-000279, ISS-000032; FI-011·012·013)

> ⚠️ **`Code.gs` 변경 — Apps Script 재배포(사람 게이트) 전까지 live 서버에는 적용되지 않는다.**

- **초대받은 사람이 내 브리핑을 바꿔 쓸 수 있었다**: 업로드하는 사진의 **파일 이름**을 `brief.md`로 지정하면, 서버가 그 이름을 그대로 써서 저장했다. 그런데 앱의 브리핑 목록은 캡처 폴더의 `brief*.md` 중 **가장 최신 파일**을 브리핑 본문으로 읽는다. 즉 **유효한 링크를 가진 사람(초대 게스트 포함)이 내 브리핑 목록에 아무 문장이나 "카이렌이 만든 브리핑"으로 띄울 수 있었다.** 이미 처리가 끝난 명함에 올리면 진짜 브리핑을 영구히 대체한다.
  - 독립 재현 실측: 업로드 응답 `ok:true, files:["brief-zzz.md"]` → owner 목록의 브리핑이 `# 조작된 브리핑 … 송금해도 된다`로 바뀜.
  - 이제 사진이 들어갈 수 있는 자리는 **`front.jpg`와 `back.jpg` 둘뿐**이고 **이름은 서버가 정한다.** 그 밖의 이름은 거절하며 폴더도 파일도 만들지 않는다.
- **같은 면을 두 장 보내면 거절한다**: 예전에는 `front.jpg` 두 장을 보내면 접수 기록에는 2장으로 남는데 실제 파일은 1개였다 — 처리기가 없는 파일을 찾는다.
- **한 장이라도 잘못되면 아무것도 저장하지 않는다**: 예전에는 앞면을 먼저 저장한 뒤 뒷면에서 실패해 **반쪽 캡처**가 남았다. 이제 전체를 먼저 검사하고, 통과했을 때만 쓴다. 빈 이미지도 거절한다.
- **회귀 게이트**: `eval/upload-filename-allowlist.test.js` 7건 신설 + `adversarial-capture.test.js`의 파일명 계약을 "정규화"에서 "거절"로 교체(그 정규화가 바로 이 구멍의 원인이었다).

### 잘못 찍은 명함을 즉시 되돌리고, 이름을 몰라도 정상으로 넘어간다 (branch `agent/par-frontend`, Kairen-Ref: TSK-000278; FI-046·049·067)

- **방금 찍은 명함을 되돌릴 수 있다**: 예전에는 `완료`를 누른 뒤 그 촬영을 빼낼 방법이 **아예 없었다.** 같은 명함을 두 번 찍었거나 엉뚱한 것을 찍어도 대기열에 그대로 남아 서버로 올라가고 사람 기록과 브리핑까지 만들어졌다. 사용자가 할 수 있는 일은 처리가 끝난 뒤 수정 요청을 보내는 것뿐이었다.
  - 이제 저장 직후 촬영 화면에 `방금 저장 · <이름>` 줄이 생기고 **되돌리기**와 **다시 열기**가 함께 있다.
  - 되돌리기는 **지우는 것이 아니다.** 사진(앞·뒷면)·메모·확인한 이름을 촬영 화면으로 그대로 되살려서, 다시 찍든 그대로 다시 저장하든 사용자가 고른다. 사진을 다시 인코딩하지 않으므로 화질이 한 단계 더 떨어지지 않는다. 되돌린 상태에서는 아직 기기에도 저장돼 있지 않다는 사실을 함께 알린다.
  - **되돌릴 수 없는 경우에는 되돌리기를 내밀지 않는다.** 서버가 이미 받은 촬영(전송됨·대조 완료)에는 버튼 자체가 없고, 지금 전송 중이면 대기열을 그대로 둔 채 `지금 전송 중이라 되돌릴 수 없어요`라고 말한다. 로컬에서 지워도 서버의 캡처는 사라지지 않기 때문이다. 판정은 전송 잠금 안에서 대기열을 **다시 읽어** 하므로, 화면이 알던 옛 상태를 믿고 전송이 시작된 촬영을 지우는 일이 없다.
  - **다음 장을 찍기 시작하면 되돌리기 줄이 내려간다.** 남겨 두면 되돌린 사진이 방금 찍은 다음 장을 덮어쓴다. 카메라가 열려 있는 동안에도 촬영 화면을 바꿔치기하지 않는다.
- **이름을 모르는 것이 실패처럼 보이지 않는다**: 이름을 못 읽으면 `직접 확인해 주세요`, 칸을 비우면 `이름을 입력해 주세요`라고 말해서 모르는 것이 못 끝낸 숙제로 보였다. 목록에서는 이름 없는 촬영이 계속 `이름 인식 대기`로 남았는데 — 인식은 이미 끝나 있었다. 기다리는 처리가 없는데 기다리는 것처럼 보이는 거짓 표시였다.
  - 이제 `모름 / 나중에`를 **직접 고를 수 있다.** 잘못 읽힌 후보를 지우고 이름 없이 저장하며, 뒤늦게 도착한 인식 결과가 이 선택을 덮어쓰지 않는다.
  - 문구도 사실대로 바꿨다: 못 읽었으면 `이름을 못 읽었어요 · 지금 적어도 되고 나중에 해도 돼요`, 칸을 비웠으면 `이름 없이 저장해도 괜찮아요`, 모름을 고르면 `이름은 나중에 · 사진과 맥락으로 정리해요`. 목록의 이름 없는 촬영은 `이름 미정`이다.
  - **저장되는 값은 그대로다.** `모름 / 나중에`는 화면 표시일 뿐이고, 저장·전송되는 이름은 예전처럼 "없음" 하나다. 사용자가 명시적으로 모른다고 고른 사실까지 서버에 남기는 것은 업로드 payload와 워처 계약을 바꾸는 일이라 이번 변경에 넣지 않았다.
- **한 자리에서 여러 장을 찍을 때 남의 메모가 섞이지 않는다 — 이미 성립해 계약으로 고정**: 만난 곳·관계는 2시간 유지되지만 메모는 명함마다 새로 쓰는 동작이 이미 맞게 돌고 있었다. 결함이 없었으므로 고치지 않고 30장 연속 촬영 fixture로 현재 계약을 고정했다. 게이트가 형식적이지 않다는 것은 회귀 주입으로 확인했다: 메모 초기화를 제거하니 첫 장의 메모가 다음 촬영 칸에 그대로 남아 게이트가 FAIL했다.
- **회귀 게이트 신설**(`frontend/e2e/capture-session.spec.ts` 4건 + unit 14건): 30장 연속 촬영에서 메모 누출 0건·세션 공통값 30장 유지, 되돌리기 후 대기열 0건·사진 복귀·재저장 시 1건(유령 항목 없음), 다음 장 촬영 중 되돌리기 미노출, 전송된 촬영에 되돌리기 미노출·기기 기록 보존, 이름 없는 캡처의 정상 표시. unit은 되돌리기 거절 사유(없음·전송됨·대조됨·원본 없음·전송 중)와 정상 경로, 구버전 항목 복원, 이름 문구 계약을 덮는다.

## v2.4.0 — 2026-07-27 — `ba30799` (Kairen-Ref: TSK-000275)

서버 쪽 업로드 계약을 멱등으로 만들고 처리 상태 되돌림을 막는다.

> ⚠️ **이 릴리즈는 `Code.gs`를 바꾼 첫 릴리즈다.** 지금까지의 v2.1.0~v2.3.0은 모두 `Code.gs` 무변경이라 GAS 재배포가 필요 없었다. 이번에는 **사람이 Apps Script를 재배포해야** 서버 동작이 실제로 바뀐다. 재배포 전까지 live 서버는 여전히 예전 동작이다. 앱(`docs/`)은 이번 릴리즈에서 바뀌지 않았다.

### 서버도 같은 촬영을 두 번 처리하지 않는다 (branch `agent/tsk-000274-upload-idempotency`, Kairen-Ref: TSK-000275, ISS-000032, ISS-000035; FI-009·010·015)

> **이 변경은 `Code.gs`를 건드린다 — Apps Script 재배포(사람 게이트)를 하기 전까지 live 서버 동작은 바뀌지 않는다.**

- **이미 끝난 명함이 처음부터 다시 처리되지 않는다**: v2.3.0에서 **앱이** 응답 유실 뒤 재전송하지 않게 막았지만, 그것은 클라이언트 한 종류의 방어였다. 서버 자체는 여전히 같은 캡처 폴더의 기록을 `접수됨`으로 덮어썼다 — 구버전 앱, 다른 기기, 수동 재요청 어느 경로로든 이미 완료된 명함이 재처리될 수 있었다.
  - 이제 서버가 **업로드 내용 지문**을 저장하고, 같은 내용이 다시 오면 **파일도 기록도 쓰지 않고** 기존 접수 결과를 그대로 돌려준다. 실측: 처리 완료된 캡처에 같은 촬영을 다시 올렸을 때 예전 서버는 `상태=접수됨 · 사람 연결 지워짐 · 처리 시각 지워짐`, 새 서버는 `상태=완료 · 사람 연결 유지 · 처리 시각 유지`.
  - **지금 운영 중인 캡처는 모두 지문이 없다.** 그래서 지문이 없는 기록은 촬영 시각·만난 곳·메모·파일 목록으로 판정한다 — 배포 첫날부터 보호가 걸린다.
  - 내용이 **실제로 달라진** 재업로드(수정·다시 찍기)는 예전처럼 받아들이되, 조용한 되돌림으로 남기지 않는다. `requeueRequested`와 이전 상태·처리 시각·연결된 사람을 함께 기록해 무엇이 왜 다시 처리됐는지 남는다.
- **남의 명함 폴더에 덮어쓸 수 없다**: 다른 사람의 captureId를 알아내 그 폴더에 업로드하면 이제 거절된다(소유자는 예외). 거절 시 파일과 기록은 **하나도 바뀌지 않는다.**
- **회귀 게이트 신설**(`eval/upload-idempotency.test.js`, 10건): 같은 내용 재전송 멱등, 완료 상태 비되돌림, 지문 없는 예전 캡처 보호, 내용 변경 시 명시적 감사 흔적, 다른 사람 캡처 거절·무변경, 소유자 권한 유지, 지문 동등성·순서 무관성, 뒷면 추가 구분. `scripts/validate.ps1`과 CI에서 함께 돈다.

## v2.3.0 — 2026-07-27 — `85254a0` (Kairen-Ref: TSK-000273)

전송이 끊겼을 때 이미 끝난 처리가 되돌아가던 경로를 막는다. `Code.gs`는 v2.0.0 이후 바뀌지 않았고, 이 릴리즈에도 GAS 재배포가 필요하지 않다.

### 응답을 못 받았을 때 같은 명함을 다시 올리지 않는다 (branch `agent/tsk-000273-ambiguous-retry`, Kairen-Ref: TSK-000273, ISS-000035; FI-016·021)

- **전송이 끊겨도 서버의 처리가 처음으로 되돌아가지 않는다**: 사진을 보냈는데 **응답만 유실되면**(전시장 와이파이가 끊기는 상황) 앱은 그 촬영을 실패로 보고 다음 주기에 그대로 다시 올렸다. 그런데 서버는 같은 캡처 폴더의 기록을 덮어쓰며 상태를 `접수됨`으로 되돌리므로, **이미 브리핑까지 끝난 명함이 처음부터 다시 처리**됐다. 회귀 게이트가 수정 전 빌드에서 재전송을 실측했다.
  - 이제 앱은 실패를 **서버가 거절함**과 **답을 못 받음**으로 나눈다. 답을 못 받은 항목은 다시 올리기 전에 **서버 기록과 한 번 대조**하고, 이미 접수돼 있으면 올리지 않고 `이미 접수돼 있어 다시 보내지 않았어요`로 정리한다.
  - 서버에 물어볼 수 없으면(대개 오프라인) **추측하지 않고 다음 연결로 미룬다.** 서버가 정말 갖고 있지 않으면 예전처럼 정상 재전송한다.
  - 대조 조회는 전송 한 묶음당 **한 번**만 한다.
- **회귀 게이트 신설**(`e2e/ambiguous-retry.spec.ts` 2건 + unit 13건): 응답 유실 후 재전송 0회·로컬 항목 접수 완료 정리, 서버가 정말 없을 때 정상 재전송, 실패 분류 5종, 목록 조회 실패 시 `모름` 판정.

## v2.2.0 — 2026-07-27 — `76ce78c` (Kairen-Ref: INT-000016, TSK-000272)

개선 인터뷰 004에서 나온 세 가지 화면 요구를 담는다. `Code.gs`는 v2.0.0 이후 바뀌지 않았고, 이 릴리즈에도 GAS 재배포가 필요하지 않다.


### 하단 바 고정 · AI 표면의 동적 시인성 · 다크 모드 (branch `agent/int-000016-visual-baseline`, Kairen-Ref: INT-000016, ISS-000084, TSK-000220, TSK-000272)

개선 인터뷰 004의 세 가지를 반영했다.

- **하단 탭 바가 스크롤 중에 사라지지 않는다**: 빠르게 위아래로 스크롤하면 캡처·진행·검색·설정 바가 "아래로 없어졌다 위로 올라왔다". 앱에 바를 숨기는 코드는 없었다 — 원인은 **껍데기 높이**였다. Ionic이 `body`를 `height: 100%`로 잡는데 모바일 브라우저에서 그 100%는 **주소창이 접힌 상태의 큰 화면**이라, 주소창이 펴져 있는 동안 화면 아래 수십 px이 주소창에 가리고 거기 놓인 탭 바가 통째로 안 보였다. 스크롤로 주소창이 접히면 다시 나타난 것이다.
  - 이제 껍데기 높이를 **지금 보이는 높이**(`100dvh`)로 잡는다. 스크롤 이벤트를 듣지 않으므로 스크롤 중에 앱이 만드는 흔들림이 없다.
  - 탭 바 높이도 고정했다. iOS는 주소창 상태에 따라 `env(safe-area-inset-bottom)`을 0 ↔ 홈 인디케이터 높이로 바꿔 보고하는데, 그 값이 바 높이 계산에 그대로 들어가 있어 스크롤 도중 바가 오르내릴 수 있었다. 부팅 시 실측한 값으로 고정하고, 값이 커질 때만 갱신한다.
- **AI 표면이 평소에도 은은하게 살아 있다**: `AI 조사 요청`과 `AI 사람 찾기` **두 표면 모두** 실제로 처리 중이 아닐 때도 아주 느리고 옅은 빛이 지나간다. 처리 중에는 같은 빛이 빨라지고 진해지며 단계 막대가 실제로 흐른다. 글자 뒤에서만 움직이므로 지금의 가독성은 그대로다.
  - 장식이 상태를 대신하지 않는다 — 테두리와 단계 이름은 움직임 없이도 상태를 말한다. `AI 사람 찾기`의 검색 칸과 진행 표면도 이제 조사 요청과 같은 AI 표면 문법을 쓴다.
  - **움직임 최소화**(`prefers-reduced-motion`)를 켠 기기에서는 효과가 사라지고 정적 화면으로 돌아간다.
- **다크 모드가 생겼다**: 설정 → `화면 테마`에서 **라이트 / 다크 / 시스템**을 직접 고르고, 고른 값은 이 기기에 남는다. 최초 기본값은 라이트다. `시스템`을 고른 경우에만 폰 설정을 따라가고, 폰이 다크여도 라이트를 골랐으면 라이트를 유지한다. 연결 해제로도 지워지지 않는다(사적 기록이 아니라 이 기기의 표시 설정이다).
  - 배경만 뒤집지 않았다 — 면·잉크·선·강조색·상태 배지·입력 칸을 다크에서 다시 만들었고, 브라우저 주소창 색(`theme-color`)도 함께 바뀐다.
- **회귀 게이트 신설**(`e2e/int16-surfaces.spec.ts`, 7건): 껍데기 높이가 dvh에서 오는지, 탭 바 여백이 고정 px인지, 문서 자체가 스크롤되지 않는지, 실제 왕복 스크롤에 바가 1px도 안 움직이는지, 두 AI 표면이 idle에도 실제로 애니메이션을 돌리는지, 처리 중에 상태·테두리가 달라지는지, reduced-motion에서 무한 애니메이션이 0인지, 첫 실행이 라이트인지, 고른 테마가 폰 설정을 이기는지, **다크에서 네 화면의 모든 글자가 WCAG 4.5:1(큰 글자 3:1)을 넘는지**. 수정 전 빌드(`f591b79`)에서 7건 전부 FAIL임을 먼저 확인했다(그중 1건은 fixture 보강 뒤 새 회귀를 막는 잠금 게이트로 성격이 바뀌었다).
  - 정직한 경계: headless Chrome에는 접히는 주소창이 없어, 하단 바 항목의 최종 판정은 **실제 폰**이다.

## v2.1.0 — 2026-07-27 — `6bcb664` (Kairen-Ref: TSK-000269, TSK-000270, TSK-000271)

v2.0.0 이후 누적된 사용자 표면 변화 전체와, 이번에 닫은 신뢰 기반 세 가지를 함께 담는다.

**이번 릴리즈에서 새로 닫은 것** — Card Capture Feature Index의 `FI-004`·`005`·`006`·`007`(개인 링크 코드 전송 경계와 기기 내 격리), `FI-025`·`031`·`032`·`052`·`053`(촬영 저장 진실과 대기열 무결성), `FI-164`(좁은 화면·접근성 회귀 게이트).

**`Code.gs`는 v2.0.0 이후 바뀌지 않았다** — 이 릴리즈에 GAS 재배포가 필요하지 않다.

### "폰을 넣어도 된다"를 실제로 저장된 뒤에만 말하기 (branch `agent/tsk-000270-queue-truth`, Kairen-Ref: TSK-000270, ISS-000035; FI-025·031·032·052·053)

- **저장이 확인된 다음에 저장했다고 말한다**: 촬영을 마치면 `업로드 시작!`이라고 했지만 그 시점에 전송은 시작조차 하지 않았고, 저장된 사진을 다시 읽어 확인하지도 않았다. 이제 기기에 쓰고 **다시 읽어 사진이 온전한지 확인한 뒤에만** `이 폰에 저장했어요 — 이제 폰을 넣어도 됩니다`라고 말한다. 전송은 그 다음에 알아서 이어간다.
- **저장에 실패하면 실패했다고 말한다**: 예전에는 원인이 무엇이든 `다시 시도해 주세요` 한 줄이었다. 저장 공간이 부족한 상황에서 다시 시도하는 건 도움이 되지 않는다. 이제 **저장 공간 부족 / 저장소를 열 수 없음 / 다시 읽었을 때 사진이 온전하지 않음**을 나눠 안내한다. **실패했을 때 촬영 화면을 비우지 않는다** — 비우면 방금 찍은 사진이 사라진다.
- **보낼 수 없는 촬영이 나머지를 막지 않는다**: 사진 내용이 비었거나 상태가 깨진 기록이 있으면 전송 주기마다 재시도되며 실패 횟수만 올라갔다. 이제 그런 항목은 전송·목록에서 빼고 진행 화면에 `보낼 수 없는 촬영 N건`으로 사유와 함께 보여 준다. **지우지 않는다** — 기기에 그대로 두고 사람이 판단한다.
- **탭을 두 개 열어도 같은 명함이 두 번 접수되지 않는다**: 전송 가드가 탭 안에만 있어서 두 탭이 동시에 열려 있으면 같은 촬영이 두 번 올라갔다. 이제 탭 하나만 전송한다(Web Locks, 없으면 만료가 있는 저장소 lease).
- **회귀 게이트 신설**(`e2e/queue-truth.spec.ts` 4건 + unit 9건): 실제 촬영 경로에서 저장 확인 후 문구, 저장 공간 부족 시 안전 주장 금지·사진 보존, 손상 항목 격리·미삭제·나머지 전송, 두 탭 동시 전송 시 captureId별 1회 접수.

### 개인 링크 코드가 새어 나갈 수 있던 경로 차단 (branch `agent/tsk-000269-credential-boundary`, Kairen-Ref: TSK-000269, ISS-000032; FI-004·005·006·007)

- **가짜 서버 주소가 붙은 링크를 열어도 개인 링크 코드가 넘어가지 않는다**: 예전에는 주소에 `?api=`를 붙이면 그 값을 **그대로 저장하고**, 이후 모든 명함 업로드와 목록 조회를 그 주소로 보냈다. `?api=https://공격자주소/`가 달린 링크를 한 번만 열면 그 뒤로 찍는 명함 사진과 개인 링크 코드가 전부 그쪽으로 갔다는 뜻이다. 이제 **빌드에 박힌 주소와 앱 자신의 주소로만** 전송하고, 그 밖의 주소는 저장조차 하지 않는다. 예전 버전이 이미 저장해 둔 신뢰 밖 주소도 앱을 열 때 폐기한다. 고급 설정에 직접 입력해도 같은 판정을 받는다. 무시했다는 사실은 화면에 그대로 알린다.
- **개인 링크로 열면 주소창의 코드를 즉시 지운다**: `?k=` 값이 주소창에 계속 남아 방문 기록·화면 공유·스크린샷·다른 사이트로 이동할 때의 Referer로 새어 나가고 있었다. 이제 저장 직후 주소에서 코드를 지우고(뒤로 가기로도 되살아나지 않는다), 나머지 파라미터(`?view=`)는 그대로 둔다.
- **한 폰을 다른 링크로 열면 이전 사람의 기록이 보이지 않는다**: 브리핑 캐시·소유자 권한 표시·최근 검색어가 링크 코드와 무관한 한 칸에 저장돼, owner 링크로 쓰던 폰을 guest 링크로 열면 **서버가 답하기 전에** 이전 사람의 기록이 그대로 떠 있었다. 이제 이 값들은 링크 코드별로 나뉘고, 다른 코드의 사본은 기기에서 지워진다. 링크 코드가 없으면 사적 기록을 읽지도 쓰지도 않는다.
- **설정에 `연결 해제`가 생겼다**: 폰을 넘기거나 링크를 회수할 때 개인 링크 코드·촬영자 이름·이 기기의 브리핑 사본·검색 기록·만남 맥락을 지운다. **전송을 기다리는 촬영은 지우지 않고**, 남아 있으면 건수를 먼저 경고한다.
- **어디로 보내지는지 설정에서 보인다**: `명함과 개인 링크 코드는 script.google.com 으로만 전송돼요`처럼 실제 전송 대상과 주소창 정리 사실을 적어 둔다.
- **회귀 게이트 신설**(`e2e/credential-boundary.spec.ts`): 적대적 origin으로 나가는 요청 0건, 주소창 코드 제거, 서버가 침묵해도 이전 subject 기록 0건, 연결 해제 정리, 오프라인 캐시에 API 응답 없음. 수정 전 빌드(`8de6316`)에서 앞의 네 건이 모두 FAIL임을 먼저 확인했다.

### 만난 상황 chip을 일반 상황으로 (branch `agent/int-000015-situation-chips`, Kairen-Ref: INT-000015, ISS-000084, TSK-000220)

- **`어디서 만났나요?` chip이 특정 행사 대신 일반 상황이 된다**: 예전에는 이 기기에서 실제로 입력한 적 있는 행사 이름(`2026 로보월드` 등)을 먼저 chip으로 올리고 남은 자리만 예시로 채웠다. 그런데 chip의 쓸모는 "매번 반복되는 상황을 한 번에 고르기"이지 "지난 행사 이름 되부르기"가 아니다 — 행사 이름은 어차피 직접 적어야 정확하고, **입력한 값은 2시간 유지되므로 같은 자리에서 여러 장을 찍을 때 다시 타이핑할 일이 없다.**
  - 이제 chip은 `고객사 방문 미팅 / 우리 회사 방문 미팅 / 전시회·박람회 / 세미나·컨퍼런스 / 투자·IR 미팅 / 채용 면접 / 소개 자리` 7종 고정이다.
  - placeholder는 `예: 2026 스마트팩토리전 부스 (직접 적어도 돼요)`로 바꿔, chip은 상황·입력은 구체적인 답이라는 역할 분담을 보여 준다.
  - placeholder가 칸 너비를 넘겨 `예: 2026 스마트팩토리전 부스 (직접 적어…`처럼 잘리던 것을 고쳤다. **placeholder 잘림을 회귀 게이트로 고정** — 실제 글꼴로 텍스트 폭을 재서 칸 안에 들어오는지 확인한다(input의 `scrollWidth`는 value 기준이라 placeholder 잘림을 못 잡는다).
  - 회귀 게이트가 **행사 이름이 chip으로 올라오지 않는 것**까지 강제한다 — fixture에 `2026 로보월드` 기록이 있어도 chip 목록에는 연도·행사명 패턴이 하나도 없어야 한다.

### 인터뷰 003 실사용 후속: 만남 맥락 가독성 · AI 조사 요청 범위 확대 (branch `agent/int-000015-followup`, Kairen-Ref: INT-000015, ISS-000082, ISS-000084, TSK-000218, TSK-000220)

founder가 배포본을 실제로 열어 본 뒤 나온 다섯 가지 지시를 반영했다.

- **`AI 조사 요청`이 진짜 필요한 판단까지 맡는다**: 예시가 `최근 경력·회사 소식`처럼 무난한 것들뿐이라 무엇을 시킬 수 있는지가 좁게 읽혔다. 이제 **실력·전문성 추정 / 의사결정 권한 / 평판·레퍼런스 / 최근 성과와 실패 / 이해관계·경쟁 관계**를 먼저 꺼내 놓는다. 묻기 껄끄럽지만 만나기 전에 알아야 하는 것들이다.
  - **권한을 넓힌 게 아니다.** 기존 `public-research-v1`의 mode는 원래 `public_professional_background`이고, 금지되는 `민감 특성`은 정치성향·종교·성적 지향·건강·질병·인종이다. 공개된 결과물을 근거로 한 직업적 판단은 처음부터 계약 안이었다 — 화면이 그걸 좁게 보여 주고 있었을 뿐이다.
  - 대신 조건을 화면에 못 박았다: `공개된 결과물(경력·논문·특허·발표·제품·기사·평가)을 근거로 판단까지 합니다. 근거와 확신도를 함께 적고, 모르는 건 모른다고 남겨요.` 그리고 하지 않는 일을 시각적으로 분리해 함께 표시한다.
- **만남 맥락이 처음부터 펼쳐진다**: 촬영 직후 바로 쓰는 칸이라 접어 둘 이유가 없었다. 접기 버튼은 그대로 남는다.
- **입력 칸이 입력 칸으로 보인다**: 카드 배경(#f8fafd)과 입력 배경(#f5f7fb)이 사실상 같은 색인데 테두리마저 없어 "글쓰기 박스인지 구별이 안 되는" 상태였다. 카드는 흰색, 입력은 눌린 회색 + 또렷한 테두리 + 50px 높이 + 14.5px 글자로 분리했다. 라벨도 Ionic 기본 라벨(placeholder와 같은 회색조)에서 진한 잉크색 12.5px 굵은 글씨로 꺼냈다.
- **만난 자리 예시가 실무 동선으로 바뀐다**: 기록이 없어도 `전시회 부스 / 고객사 방문 / 우리 사무실 / IR·투자 미팅 / 세미나·학회 / 채용 면접`이 먼저 뜬다. 실제로 입력한 적 있는 만난 곳이 있으면 그게 항상 앞이다.
- **음성 입력 안내가 처음부터 보인다**: `키보드 마이크로 말해도 돼요`가 메모 placeholder 안에 묻혀 있었다. 이제 `만남 맥락` 제목 옆 배지로 올렸다 — 가장 빠른 입력 수단이 가장 늦게 발견되던 문제다.
- **접수 버튼이 화면 밖으로 밀리지 않는다**: 예시 chip이 8개로 늘자 조사 요청 시트에서 `조사 요청 접수` 버튼이 화면 아래로 밀려 **누를 수 없게 됐다**(회귀 게이트가 잡았다 — 844px 화면에서 버튼 하단이 986px). 버튼을 시트 바닥에 고정하고 조사 요청 시트만 더 높게 연다.
- **날짜가 바뀌면 빨개지던 게이트 수정**: `지난주` 회귀 fixture가 "8일 전"이라 일→월 요일이 넘어가는 순간 창 밖으로 밀렸다. 주 시작(월요일) 기준으로 계산해 어느 요일에 돌려도 같은 구간에 들어오게 했다.

### 개선 인터뷰 003 반영: 만남 맥락 정리 · AI 조사 요청 표면 · 즉시 응답 사람 찾기 · 다중 후보 (branch `agent/int-000015-ai-surfaces`, Kairen-Ref: INT-000015, ISS-000082, ISS-000084, ISS-000103, TSK-000218, TSK-000220, TSK-000249)

- **`기억할 맥락`이 `만남 맥락` 카드가 됐다**: 네 칸이 같은 무게로 쌓이고 칸마다 `(선택, 2시간 유지)`가 반복돼, 무엇을 왜 적는지가 안 읽히고 촬영 버튼이 화면 아래로 밀렸다. 이제 **기본은 접힘**이고, 열면 `어디서 만났나요?`가 첫 칸이며 안내는 영역 위에 한 번만 나온다. 최근에 실제로 입력한 만난 곳과 자주 쓰는 관계(`잠재 고객`, `오늘 처음` 등)를 **chip으로 먼저 제안**한다. 입력이 생기면 접힌 줄에 `2026 로보월드 · Kairen: 잠재 고객 · 나: 오늘 처음`처럼 **저장될 내용 그대로** 요약된다.
- **`조사 지시` → `AI 조사 요청`**: 일반 메모와 똑같이 생긴 입력 박스라 AI에게 일을 맡기는 기능임이 드러나지 않았다. 이제 전용 표면(구분되는 배경·테두리 + sparkle 표식 + `소유자 전용` 배지)에 놓이고, 접수 뒤 거치는 단계를 **미리** 보여 준다 — `작성 중 › 접수됨 › 공개 자료 조사 중 › 출처 정리 중 › 완료`. 예시 chip은 이미 쓴 문장을 지우지 않고 덧붙인다. **권한은 넓히지 않았다** — owner 전용, 공개·합법 출처만, 로그인 자료·민감 특성 추론·외부 전송 없음이 화면에 그대로 적혀 있다. 캡처 화면과 인물 카드가 같은 표면을 쓴다.
- **AI 사람 찾기가 네트워크를 기다리지 않는다**: 예전에는 문장을 넣으면 **먼저 서버에서 기록 100건을 받아 온 뒤에** 대조를 시작했다 — 체감 지연의 전부가 그 왕복이었다. 이제 이 기기에 이미 있는 기록으로 **즉시** 후보를 내고(실측 0.0초대), 그다음 배경에서 최신 기록을 받아 다시 대조한다. 문장이 밖으로 나가지 않는 계약은 그대로다.
- **기다리는 동안 무엇을 하는지 보인다**: 불투명한 로딩 대신 실제 단계를 보여 준다 — `말한 단서를 해석하고 있어요` → `이 기기에 있는 기록 86건을 대조하고 있어요`(실제 건수) → `가능성 높은 후보부터 정리하고 있어요`. **남은 시간은 지어내지 않고** 경과 시간과 `중단` 버튼을 준다. 끝나면 `기록 4건 대조 · 0.2초`처럼 실측값으로 말한다.
- **결과가 한 명으로 수렴하지 않는다**: `한화`처럼 여러 명이 걸리는 질의에서 후보를 전부 남기고 `후보 3명`과 적용된 단서 chip(`지난주`, `한화`)을 함께 보여 준다. 근거가 둘 이상 맞으면 `강하게 일치`, 하나뿐이거나 시점이 살짝 벗어나면 `가능성 있음`으로 나눈다. 후보가 많으면 소속·만난 곳 chip으로 즉시 좁힌다. 후보 상한도 12명 → 60명으로 올렸다.
- **못 찾은 이유를 구분한다**: `조건에 맞는 기록이 없어요`와 `이 기기에 아직 기록이 없어요`를 다르게 말한다.
- **OCR이 이름 줄을 쪼개도 살린다**: 실제 명함 렌더가 흐릴 때 `Jane Kim`이 `Jane |` + `Kim` 두 박스로 나뉘고 신뢰도가 함께 떨어져 이름을 통째로 놓쳤다(CI 실측, 벤치 9/10). 같은 줄 판정을 중심 거리 대신 **세로 구간 겹침**으로 바꾸고, 직함 줄이 붙어 있거나 이메일이 뒷받침하면 낮은 신뢰도 줄도 버리지 않으며, 이메일 아이디(`jane.kim`)로 쪼개진 낱말을 되붙인다. `@` 앞 공백도 이메일로 읽는다.
- **회귀 게이트 신설**: 맥락 카드가 접힌 채 시작해 저장될 내용만 요약하는지, AI 조사 요청이 권한을 넓히지 않고 단계·경계를 보여 주는지, 사람 찾기가 서버 응답 전에 후보를 내고 진행 단계를 말하는지, 회사 질의가 후보를 전부 남기고 좁혀지는지를 실제 앱 경로로 검증한다. 수정 전 빌드에서 4건 모두 FAIL을 확인했다.

### 개선 인터뷰 002 반영: 자연어 사람 찾기·상단 정리·갤러리 정책·프로필 UX (branch `agent/int-000013-card-capture`, Kairen-Ref: INT-000013, ISS-000058, ISS-000066, ISS-000084, ISS-000102, ISS-000103, TSK-000220)

- **기억나는 대로 말해서 사람을 찾는다**: 검색 화면에 `AI 사람 찾기`가 생겼다. "지난주쯤 만났던 사람인데 한화 다니던 구매팀장이었어"처럼 문장으로 말하면 시점(`지난주`→7월 13일~19일)·소속·만난 곳·직함으로 나눠 기록과 대조하고, 후보마다 **왜 그 사람인지**를 근거 줄로 보여 준다. 기존 `빠른 검색`(이름·회사·만난 곳)은 그대로 남아 기본값이다.
  - **문장은 기기 밖으로 나가지 않는다**: 해석과 대조를 전부 휴대폰 안에서 한다. 외부 AI를 호출하지 않고, 기기 기록에서 못 찾았을 때만 기존 검색에 **단어 하나**(예: `한화`)를 넘긴다.
  - **기록에 없는 건 지어내지 않는다**: "여자였어", "안경 썼어" 같은 조건은 명함 기록에 없으므로 조용히 무시하지 않고 *"성별은 명함 기록에 남기지 않아 조건으로 쓰지 않았어요"* 라고 알려 준다. 못 찾은 단어도 그대로 되돌려 말한다.
- **명함 사진이 휴대폰 갤러리를 더럽히지 않는다**: 카이렌 카메라로 찍은 사진은 원래 갤러리에 저장되지 않는다 — 갤러리에 사본을 만드는 건 `기본 카메라 앱으로 찍기` 경로뿐이었다. 이제 그 버튼은 기본으로 숨기고(설정 `기본 카메라 앱 쓰지 않기`), 카메라가 열리지 않는 기기에서만 *"갤러리에 남아요"* 를 붙여 예외로 보여 준다. 캡처 수정 화면의 다시 찍기도 앱 카메라를 쓴다.
  - **전송이 끝난 원본을 기기에서 지운다**: 예전에는 최근 50건의 명함 원본이 전송 뒤에도 기기에 계속 남았다. 이제 서버가 성공을 확인하고 10분이 지나면 원본을 지우고 목록용 작은 썸네일만 남긴다. 대기·실패 항목은 재전송을 위해 그대로 둔다.
  - **지울 수 없는 건 지운다고 하지 않는다**: 이미 갤러리에 쌓인 사진은 웹 앱이 삭제할 수 없다 — 설정에서 그 사실을 그대로 알린다.
- **상단이 제품 이름 대신 지금 상태를 보여 준다**: 최상단 `Kairen Card Capture 모바일 메모리` + 떠 있는 새로고침 조합을 없앴다. 상단 바가 **현재 화면 이름 + 한 줄 상태**(`3건 처리 중`, `모두 최신`, `연결 필요`)와 새로고침 하나를 소유하고, 화면마다 또 그리던 큰 제목을 걷어냈다 — 폰에서 내용이 약 90px 위로 올라온다.
- **사람을 열면 `지금 알아둘 것`이 먼저 보인다**: 인물 전문 맨 위에 최근 변화·다음 대화 단서·Kairen 접점을 최대 3줄로 보여 준다. 요약을 새로 쓰지 않고 기록의 문장을 **그대로 인용**하며, 어느 섹션에서 왔는지와 언제 확인된 기록인지를 함께 표시한다. 해당 내용이 기록에 없으면 아무것도 만들지 않는다.
- **프로필의 웹 주소를 바로 연다**: 회사 홈페이지·LinkedIn·기사처럼 브리핑 본문, 표, 프로필 메타데이터에 있는 `http`·`https` 주소를 링크로 표시하고 새 탭에서 안전하게 연다. 실행형 주소나 앱 내부 상대 경로는 링크로 만들지 않는다.
- **액션을 목적에 따라 묶는다**: 전화·문자·메일·연락처 저장은 `연락`, 메모·조사 지시는 `기록`, 전체 프로필·수정 요청·캡처 수정은 `관리`로 나눴다. 메모 추가만 파란색으로 강조하던 근거 없는 우선순위도 제거했다.
- **브라우저 기본 팝업을 앱 입력창으로 교체한다**: 메모 추가·조사 지시·수정 요청은 맥락 설명, 입력 예시, 글자 수, 취소·저장 동작이 있는 앱 내부 입력창에서 작성한다. 주소나 기술 용어가 노출되던 브라우저 문구는 더 이상 표시되지 않는다.
- **설정 문구를 사용자 관점으로 바꾼다**: `연결과 경계` 같은 내부 용어 대신 `내 앱 설정`, `이 기기`, `사용자·연결 정보 편집`처럼 실제로 무엇을 확인하고 바꾸는지 설명한다. API 주소와 토큰은 `고급 설정` 안에 유지한다.
- **회귀 게이트 신설**: 상단 바가 화면 이름·상태·새로고침을 소유하는지, 회상 검색이 근거를 붙이고 기록에 없는 속성을 조건으로 쓰지 않는지(문장이 네트워크로 나가지 않는지 포함), 기본 카메라 경로가 기본으로 숨는지, 인용 하이라이트가 출처를 밝히는지를 실제 앱 경로로 검증한다. 수정 전 빌드에서 4건 모두 FAIL을 확인했다.

### 이름 인식 캐시 오염 제거 · 진행 단계 표시 · 자동 새로고침 안내 (branch `agent/tsk-000249-name-and-progress`, Kairen-Ref: ISS-000096, TSK-000249)

- **다른 사람 이름이 나오던 원인 제거**: OCR 라이브러리가 결과를 캐시하는데, 캐시 키가 **이미지 앞 1024바이트(좌상단 256픽셀) 해시 + 길이**였다. 명함은 대개 같은 비율로 크롭되고 좌상단이 여백이라 키가 충돌해서 **직전에 처리한 사람의 인식 결과가 그대로 나올 수 있었다.** 캐시를 껐다(캡처당 한 번만 호출하므로 손해도 없다).
- **성이 떨어져 나가지 않는다**: 자간이 넓은 이름을 OCR이 `이 강 윤`처럼 쪼개 놓으면 `강윤`만 잡히거나 아무것도 못 잡았다. 한글 토큰만으로 이루어진 줄은 합쳐서 한 이름으로 되돌리고, 고른 이름이 다른 후보의 끝부분이면 더 긴 쪽을 택한다.
- **이름 인식 벤치 신설**: 실제 PP-OCR 엔진이 뱉은 줄을 실제 픽커에 그대로 먹여 10개 명함 레이아웃에서 정확도를 잰다 — **2/10 → 10/10**.
- **처리 진행이 한 사다리로 보인다**: 기기 쪽(전송 대기·전송됨)과 서버 쪽(접수됨·처리 중·완료)이 서로 다른 문구로 흩어져 있던 것을 **4단계 하나의 진행**으로 합쳤다 — 사진 전송 → 서버 접수 → 이름·정보 인식 → 웹 조사·기록 정리. 카드마다 `4단계 중 3단계 · 이름·정보 인식 중`과 `8분 경과 · 약 7분 남음 · 보통 6~20분`, 그리고 단계 막대를 보여 준다. 오래 걸릴 때는 남은 시간을 약속하는 대신 재요청·문의 버튼을 띄운다.
- **자동 새로고침이 언제 도는지 보인다**: 상단 한 줄 상태와 기록 섹션에 `방금 업데이트 · 12초 뒤 자동 새로고침`처럼 남은 시간이 표시된다(주기 20초, 앱 복귀 시 즉시 갱신).

### 본 대로 잘리는 촬영 결과 · 뒷면 확인 단계 · 목록 한 줄 요약 (branch `agent/tsk-000246-wysiwyg-crop`, Kairen-Ref: ISS-000100, TSK-000246)

- **화면에서 본 사각형 그대로 잘린다**: 라이브 미리보기는 명함을 잡아 놓고, 촬영 시점에는 그 사각형을 버리고 전체 프레임에서 **처음부터 다시 감지**하고 있었다. 조건이 달라 재감지가 실패하면 결과물이 프레임 전체로 나왔다 — "기껏 인식해 놓고 왜 안 자르냐"의 정체다. 이제 촬영 직전 1.5초 안에 화면에 그려진 사각형을 그대로 써서 자른다. 그게 없을 때만 재감지 → 가이드 크롭 → 전체 프레임 순으로 내려간다.
- **뒷면도 "이대로 괜찮나요?"를 묻는다**: 앞면만 확인 단계가 있고 뒷면은 촬영 즉시 닫혔다. 자동 촬영이면 결과를 볼 기회조차 없었다. 이제 뒷면도 `완료` / `뒷면 다시 찍기`를 고를 수 있다.
- **명함 기록이 "이름 — 한 줄 요약"으로 보인다**: 목록이 브리핑 제목을 그대로 써서 모든 항목이 `이런 분이에요`였다. 이제 브리핑 본문의 첫 문장을 한 줄 요약으로 보여 준다(머리글·불릿·표는 건너뛰고 64자에서 자름). 요약이 아직 없으면 진행 상태를 보여 준다. 브리핑 파일 형식과 파싱 규칙은 그대로다.

### 명함 감지 알고리즘 재설계와 이름 오탐 수정 (branch `agent/tsk-000244-detect-v2`, Kairen-Ref: ISS-000101, TSK-000244, ISS-000096, TSK-000245)

- **수직으로 내려다봐도 명함을 잡는다**: 기존 감지기는 카드 윤곽이 한 군데라도 끊기면 후보 자체를 못 만들었다. 사선 촬영이 잘 되던 건 드롭 섀도가 윤곽을 그려 줬기 때문이고, 수직에서는 폰이 광원을 가려 그 그림자가 사라진다 — 즉 **그림자에 의존하는 알고리즘**이었다. 이제 조명을 먼저 평탄화하고, edge map 3종(Canny·모폴로지 그래디언트·adaptive)에서 **Hough 직선 교점 + 컨투어 + 최소 외접 사각형**을 모두 후보로 만든 뒤 고른다. 실측(감지 벤치): 수직·그림자 없음 검출률 0.50 → **1.00**(IoU 0.48 → 0.98), 수직·저대비 0.75 → **1.00**(IoU 0.17 → 0.84).
- **박스가 촐싹대지 않는다**: 후보 선택 기준을 "면적 최대"에서 **실제 경계 근거(edge support) + 종횡비 + 직각도 + 면적** 합산 점수로 바꿨다. 여기에 직전 프레임 사각형에 대한 근접 보너스·교체 비용·데드밴드를 더하고, 화면 보간을 적응형으로(조금 움직이면 천천히, 크게 움직이면 빠르게) 만들었다.
- **엉뚱한 박스 대신 "못 찾음"**: 점수가 최소 품질선에 못 미치면 사각형을 그리지 않는다. 배경의 큰 사각형을 명함이라고 우기지 않는다.
- **이름 인식이 로고를 사람 이름으로 착각하지 않는다**: 이름 선택 규칙이 "카드에서 가장 큰 글자"였는데, 로고를 크게 넣는 명함에서는 정반대로 작동했다. 이제 **한국 성씨 prior**(2~4자 한글은 실제 성씨로 시작해야 함 — `카이렌`·`로보틱스` 제거), **직함 인접 가점**, **이메일 아이디 대조**, **전부 대문자 영문 감점**을 함께 본다. 확신 가는 사람 이름이 없으면 회사명을 채우는 대신 **비워 둔다**.
- **더 정확해졌는데 더 빨라졌다**: 프레임당 비용이 깊은 프레임 18~31ms, 가벼운 프레임 7~9ms다(데스크톱, 320px 축소 프레임). 조명 추정을 1/4 축소본에서 계산하고, 가벼운 프레임은 직전 사각형이 아직 경계 위에 있으면 재탐색 없이 그대로 추적한다. 깊은 프레임은 항상 다시 찾으므로 잘못 잡힌 박스가 굳지 않는다.
- **감지 벤치 신설**: 빌드된 감지 워커를 그대로 불러 실패 조건 장면(그림자 없음·폰 그림자·저대비·반사광·어두운 책상·나무 책상)에 대해 검출률·IoU·프레임 간 떨림·프레임당 지연을 수치로 낸다. 앞으로 감지 알고리즘 변경은 이 수치로 판정한다.

### 뒷면이 안 찍히던 문제와 잘려 보이던 결과물 수정 (branch `agent/tsk-000243-capture-flow`, Kairen-Ref: ISS-000100, TSK-000243)

- **뒷면이 실제로 찍힌다**: `뒷면도 찍기`를 누르면 명함을 뒤집기도 전에 **1.5초 만에** 자동 촬영이 다시 발사돼, 앞면이 뒷면으로 저장되고 있었다(실측). 이제 명함이 화면에서 한 번 벗어난 뒤에만 자동 촬영이 재개된다 — 뒤집는 동안은 `명함을 뒤집어 뒷면을 대주세요`로 안내하고, 오래 걸리면 수동 버튼을 안내한다. 수동 촬영은 언제나 가능하다.
- **촬영 결과가 잘리지 않는다**: 촬영 직후 확인 화면의 썸네일이 `cover`라 가로로 긴 명함이 세로에 맞춰 확대되고 양옆이 잘려 "잘못 찍힌 것처럼" 보였다. 확인 화면·대기열 목록·편집 화면의 명함 이미지를 전부 잘리지 않게(`contain`) 바꿨다.
- **대기열에서 앞·뒷면 여부가 보인다**: 목록 행에 `앞면` / `앞·뒷면` 표시를 추가했다 — 예전에는 편집 화면을 열어야만 뒷면이 담겼는지 알 수 있었다.
- **촬영 흐름 회귀 게이트 신설**: 장면을 바꿀 수 있는 합성 카메라로 앞면 자동 촬영 → 같은 장이 남아 있는 6초 동안 재발사 없음 → 명함 교체 후 뒷면 자동 촬영 → 두 장 저장까지를 실제 앱 경로로 검증한다.

### 감지 사각형이 명함 위로 뜨던 오프셋 수정 (branch `agent/tsk-000241-overlay-geometry`, Kairen-Ref: ISS-000098, TSK-000241)

- **감지 사각형이 명함에 정확히 앉는다**: 인식은 되는데 박스만 위로 떠 있던 증상을 고쳤다. 미리보기의 video가 grid 아이템이라 `height:100%`가 auto로 풀려 비디오가 스테이지보다 길어져 있었고(폰 화면 계측: 오버레이 341x455 위에 비디오 341x606), 화면에는 카메라 프레임의 **위쪽**이 보이는데 앱은 가운데가 보인다고 계산해 박스를 76px 위에 그렸다. legacy(v1.0)에 있던 `position:absolute; inset:0`을 복원하고, 좌표 변환을 **비디오·오버레이의 실제 렌더 상자를 재서** 만들도록 바꿨다 — 레이아웃이 다시 바뀌어도 같은 오차가 재발하지 않는다.
- **촬영 결과물도 함께 교정**: 감지 실패 시의 가이드 영역 크롭이 같은 좌표 변환을 쓰므로, 잘려 나오던 위치가 함께 맞춰졌다.
- **미리보기가 프레임 가운데를 보여 준다**: 이전에는 카메라 프레임의 위쪽 3/4만 보였다. 이제 legacy처럼 가운데를 보여 준다.
- **회귀 게이트를 진실값 기준으로 교체**: 기존 게이트는 기대 위치를 앱과 **똑같은 공식으로 다시 계산**해, 좌표 변환이 통째로 틀려도 항상 통과했다(실폰 보고가 반복돼도 CI는 초록이었다). 이제 오버레이를 숨기고 화면을 찍어 **실제로 보이는 명함**을 픽셀에서 찾아 비교한다. 수정 전 빌드에서 FAIL(세로 오차 73px) → 수정 후 PASS(3px)를 확인했다.

### 명함 감지 즉시 동작 복원 (branch `agent/tsk-000237-live-detect-debug`, Kairen-Ref: ISS-000098, TSK-000239)

- **카메라를 열면 곧바로 명함을 잡는다**: 감지 엔진을 앱 시작 직후(유휴 2.5초)에 워커에서 미리 기동하도록 되돌렸다. 직전 버전은 프리즈를 막으려고 엔진 실행을 카메라 진입 시점으로 미뤘는데, 실기기에서는 10MB 엔진 컴파일이 수십 초 걸려 **그동안 감지가 아예 죽고 화면 42% 지점의 고정 가이드 사각형만 떠 있었다**(갤럭시 폴드 7 재현 보고: "명함 위로 올라오는 사각형 인식이 완전 안 됨", "legacy는 잘 됨"). 엔진이 워커에서 돌기 때문에 미리 기동해도 화면은 멈추지 않는다.
- **대기 상태를 오해 없이 표시**: 아직 명함을 못 찾은 대기 프레임은 **점선**으로 그리고 코너 브래킷을 숨겨 감지된 박스와 구분한다. 엔진이 준비 중이면 "명함 감지 엔진 준비 중 N초… 지금도 촬영할 수 있어요"로 경과를 알린다.
- **느린 기기에서 박스가 끊기지 않게**: 감지 박스 유지 시간을 0.45초 → 1.1초로 늘려 왕복이 느린 기기에서도 사각형이 카드에 붙어 있는다.
- **감지 회귀 게이트 신설**: 카메라 스트림을 주입해 실제 앱 경로를 구동하는 e2e 두 종 — (1) **엔진 준비를 기다리지 않고** 카메라 진입 후 박스가 카드에 붙기까지의 시간을 재는 게이트(이번 회귀가 재발하면 실패), (2) 실사 시나리오 5종(기울기·반사광·저대비·복잡한 배경·둥근 모서리)에서 박스가 카드 위에 앉는지 검증하는 매트릭스.

### 빠른 이름 인식 엔진 교체: PP-OCRv5 한국어 (branch `agent/tsk-000236-ppocr-quickname`, Kairen-Ref: ISS-000096, TSK-000236)

- **인식 엔진 교체**: 촬영 직후 "이름 먼저 확인"이 Tesseract에서 **PaddleOCR PP-OCRv5 모바일(한국어 인식 모델)**로 바뀌었다. 공개 벤치마크에서 한국어 포함 scene text 정확도가 Tesseract를 크게 앞서는 최신 신경망 OCR이다. 게이트 실측(합성 명함): 이름·직함·회사·연락처 4줄 전부 정확 인식, 추론 0.7초.
- **이름 선택 로직 교체**: "카드에서 가장 큰 글씨의 사람 이름" — 단어 박스 크기(글꼴 크기)·신뢰도 기반 픽커로 바꿔 회사명·직함을 이름으로 뽑는 오탐을 줄였다. 직함이 같은 줄에 붙은 경우("홍길동 대표이사")도 이름만 분리한다.
- **온디바이스·자체 호스팅 유지**: 모델(det 4.7MB + 한국어 rec 13.4MB + 사전)과 ONNX Runtime WASM(13.5MB)을 전부 자체 호스팅하고 전용 Web Worker에서 실행한다 — 명함 이미지는 외부로 나가지 않고, 메인 스레드 프리즈도 없다(게이트 실측 long task 0ms). 자산은 pinned hash로 validate에 고정.
- **폴백 유지**: PP-OCR 초기화 실패 기기는 기존 TextDetector → Tesseract 경로로 그대로 동작한다. 준비가 늦으면(7초 상한) 해당 캡처만 폴백을 쓰고 다음 캡처부터 새 엔진을 쓴다.
- **품질 게이트 신설**: 실제 모델을 워커에 로드해 합성 한국어 명함의 이름 정확 추출 + 메인 스레드 long task 한도를 CI에서 강제한다.

### 카메라 프리즈 근본 수정: 감지 엔진 워커 격리 (branch `agent/tsk-000230-opencv-worker`, Kairen-Ref: ISS-000091, TSK-000230)

- **감지 엔진을 Web Worker로 완전 이전**: OpenCV(WASM ~10MB)의 로드·컴파일·명함 감지·perspective 보정·흐림 점수가 전부 별도 스레드에서 돌아, 카메라를 여는 순간을 포함해 **어떤 시점에도 메인 스레드가 잠기지 않는다.** 직전 수정(prefetch+지연 실행)은 다운로드 대기만 없앴을 뿐 컴파일 블로킹이 카메라 진입 시점에 남아 폰에서 "준비 중… + 전 버튼 무반응"이 재현됐다 — 이번이 근본 수정이다.
- **프리즈 회귀 게이트 신설**: 실제 vendor 엔진을 워커에 로드·감지시키며 메인 스레드 long task를 계측하는 Playwright 게이트 추가 — 측정: 엔진 로드 603ms 동안 메인 스레드 최대 점유 66ms(이전 방식은 404ms+ 블로킹, 폰에서는 수 배).
- **새 배포 자동 반영**: 새 버전이 활성화되면 페이지를 한 번 자동 새로고침한다 — 홈 화면 앱이 "한 번 전 실행" 빌드를 계속 보여주던 혼선 제거(최초 설치 시에는 새로고침하지 않음).
- **빌드 표시**: 설정 화면 하단에 빌드 시각을 표시해 "지금 무슨 버전을 보고 있나"를 바로 확인할 수 있다.

### 실폰 1차 피드백 반영 (branch `agent/tsk-000230-phone-feedback`, Kairen-Ref: ISS-000091, ISS-000094, ISS-000095, TSK-000230~232)

- **카메라 프리즈 해소**: 명함 감지 엔진(OpenCV WASM)을 앱 유휴 시점에 **내려받기만** 하고, 실행·컴파일은 카메라 미리보기가 뜬 뒤로 옮겼다. 촬영 버튼을 누른 직후 메인 스레드가 잠겨 아무 버튼도 반응하지 않던 문제가 사라졌다(측정: 실행 시 메인 스레드 점유 약 0.4초/데스크톱). 엔진 준비 전에도 수동 촬영·가이드 크롭은 그대로 동작하며 안내 문구도 그렇게 바뀌었다.
- **2시간 유지 실동작**: 만난 곳·관계·조사 지시를 입력 즉시 저장한다. 완료를 누르지 못하고 앱을 벗어나도 값이 유지되며, 과거 캡처를 수정해도 현재 맥락이 덮어써지지 않는다. 조사 지시도 2시간 유지 대상에 포함했다.
- **명함 기록 통합**: 최근 캡처와 받은 명함 브리핑을 하나의 목록으로 합쳤다. 같은 명함이 두 곳에 나뉘어 보이지 않고, 브리핑 카드 안에서 내가 적은 맥락과 캡처 수정 진입을 함께 볼 수 있다. 전송 대기·실패 항목은 대기 행으로 남는다.
- **화면 간결화**: 캡처 첫 화면의 큰 제목·설명 문단을 한 줄로 줄이고, 캡처 화면의 사람 검색 바로가기를 없앴다(하단 검색 탭 유지).
- **조사 지시 예시**: 자주 쓰는 형태를 입력창 예시로 넣었다 — 최근 경력·이직, 회사 투자·뉴스, 인터뷰·발표, 공통 접점.
- **새로고침 반응**: 우측 상단·목록 새로고침이 진행("새로고침 중…")과 완료를 즉시 알린다.
- **전방위 인물 조사**: 처리 절차의 웹 보강 규칙을 LinkedIn 편중에서 소스군 체크리스트로 바꿨다 — 사람 6군(전문 프로필·뉴스/인터뷰·발표/컨퍼런스·논문/특허/GitHub·협회/수상·최근 90일 활동) 중 4군 이상, 회사 5군 중 3군 이상을 실제로 조회하고, 한글·영문·이니셜 질의 변형과 동일인 2개 이상 근거 일치, 출처별 신뢰도·확인일·충돌 기록을 남긴다. 브리핑에는 "만나기 전에 알면 좋은 것" 대화 포인트 3~5개가 들어간다. 공개·합법 출처 한정과 민감 정보 금지 경계는 그대로다.

### 캡처 작업 화면·온보딩 복원 (branch `agent/tsk-000228-capture-surface`, Kairen-Ref: ISS-000091, TSK-000228)

- **한 화면 촬영 흐름 복원**: 캡처 탭이 랜딩형 hero에서 legacy 작업 화면으로 돌아왔다 — 촬영 버튼·기기 내 이름 확인·기억할 맥락 4필드(스티키 값이 촬영 전에 보임)·완료가 한 화면에 있고, 최근 캡처·받은 명함 브리핑이 같은 스크롤에서 접기 상태(legacy와 같은 저장 키)로 이어진다. 완료 후에는 만난 곳·관계를 유지한 채 즉시 다음 명함을 찍을 수 있다.
- **카메라 모달은 촬영 전용**: 앞면 촬영 직후 카메라를 벗어나지 않는 선택지(뒷면도 찍기 / 뒷면 없이 완료 / 앞면 다시 찍기)를 복원했고, 스트림을 유지해 뒷면 촬영이 즉시 시작된다. 맥락 입력·이름 확인·완료는 메인 화면이 소유한다.
- **링크 우선 온보딩 복원**: 첫 실행은 legacy처럼 이름 한 칸("처음 오셨네요 👋")만 묻는다. 주소·토큰 입력은 설정의 "고급 — 직접 연결 설정" 뒤로 숨고, 토큰 라벨이 "개인 링크 토큰 (?k= 값)"과 자동 저장 안내로 바뀌었으며, 토큰 없이 열면 개인 링크 안내 배너가 뜬다.
- **회귀 게이트**: Playwright `parity-restore`에 한 화면 캡처 표면·접기 저장·온보딩 게이트 검증을 추가했다 (총 10/10).

### legacy 파리티 회귀 복원 (branch `agent/tsk-000222-react-parity-regressions`, Kairen-Ref: ISS-000091, TSK-000222, TSK-000223)

- **브리핑·프로필 렌더링 복원**: 브리핑 본문과 전체 프로필이 마크다운 원문 덤프 대신 legacy `mdLite` 규칙(제목·불릿·구분선·화살표·표, escaped pipe 포함)으로 렌더링된다. 전체 프로필에는 미팅 프렙 카드(이름·직함·회사·마지막 확인·바로 연락)와 frontmatter 요약 박스가 돌아왔고, 검색 결과 프로필에서도 메모 추가·조사 지시를 바로 실행할 수 있다.
- **연락처 추출 폴백**: 서버 contact 요약이 없는 기존 브리핑에서도 본문 텍스트에서 전화·메일을 추출해 전화·문자·메일·연락처 저장 버튼을 복원한다.
- **최근 캡처 맥락 복원**: 처리 완료된 브리핑의 이름이 로컬 캡처 목록에 매핑되고, 만난 곳·관계 맥락 줄·바로 메모 버튼·"예전 캡처 더 보기" 페이지네이션이 돌아왔다. 새 캡처는 104px 썸네일을 저장해 전송 후 원본이 정리돼도 목록 이미지가 유지된다.
- **진행 문구·제목 규칙 복원**: 처리 대기 3단계 문구(통상 소요·완료까지 약 N분 남음)와 captureId 폴백 경과 계산, note/조사 지시 receipt의 "메모 → 대상" 제목, skipped 항목의 만난 곳 폴백 제목을 복원했다.
- **오프라인 신뢰성**: 배경 20초 자동 새로고침 실패가 더 이상 토스트를 띄우지 않는다(수동 새로고침만 안내). 서버 오류 코드는 legacy처럼 한글 안내로 매핑되고, owner 게이트는 localStorage에 캐시되어 전파가 약해도 전체 프로필·검색 UI가 유지된다. 최근 검색 칩은 누르면 즉시 검색한다.
- **카메라 라이브 오버레이 복원**: 감지된 명함 사각형을 주변 스크림·코너 브래킷·프레임 보간·잠금 상태로 실시간 표시하고(자동 촬영이 꺼져 있어도 표시), 자동 촬영 진행 링을 스테이지에 그린다. 감지 실패 시 가이드 영역 크롭 폴백으로 배경 전체 업로드를 막고, 촬영 직후 흐림 경고를 복원했다.
- **빠른 이름 보호**: 사용자가 이미 입력·수정한 이름을 OCR 완료가 덮어쓰지 않으며, Tesseract 모델을 유휴 시간에 미리 로드해 첫 촬영의 이름 인식 대기를 줄인다.
- **회귀 게이트**: 위 파리티를 단위 테스트 36건과 Playwright `parity-restore` e2e 3건으로 고정했다.

## v2.0.0 — 2026-07-26 — `c9036b3` (Kairen-Ref: TSK-000221)

- **운영 승격**: 기본 Pages URL이 token·view query를 보존해 React 앱을 열고, 기존 static 앱은 `legacy.html` 복구 경로로 보존한다. root와 React service worker가 서로의 cache를 지우지 않도록 경계를 분리했다.
- **검색 진입점**: 하단 내비게이션의 모호한 `사람` 탭을 `검색`으로 바꾸고 캡처 첫 화면에 `사람 검색` 바로가기를 추가해 이름·회사 검색을 즉시 발견할 수 있게 했다.
- **운영 app shell**: React·TypeScript·Vite와 Ionic React로 `docs/next/` 앱을 만들고 legacy 앱은 `docs/legacy.html` rollback baseline으로 유지한다.
- **style ownership**: Ionic은 mobile shell·safe area·modal·toast를 소유하고, Tailwind는 Preflight 없이 Kairen-owned layout·content에만 사용한다.
- **contract adapter**: 기존 GAS `list`·`search`·`doc`·`persondoc`·`requeue`·`addnote`·`researchinstruction`·`correction`·upload payload와 IndexedDB `cardcapture/q`를 typed boundary로 고정했다.
- **설정·offline queue parity**: 기존 `?api`·`?k` 링크와 `cc_*` 저장 key를 그대로 받고, IndexedDB reopen·captureId 순차 전송·local `sent` 비중복·online/visibility 재전송·실패 data/tries·수동 재시도·50건 이후 sent 원본 정리를 복원했다.
- **offline shell**: 취약한 Workbox dependency를 채택하지 않고 작은 build-time generator로 `/next/` scope 전용 service worker와 web manifest를 생성한다. root와 legacy cache는 React cache와 분리한다.
- **server-off recovery gate**: Playwright가 실제 정적 서버를 종료한 뒤 Chrome reload를 수행해 cached shell·navigation이 복구되는지 검증한다.
- **촬영 parity**: 후면 camera permission·resolution·failure mapping·track cleanup, 앞·뒷면, retake, torch, 기본 카메라 fallback, stable auto-capture, 맥락·메모·owner 조사 지시를 React 앱에 연결했다.
- **captured-image queue gate**: camera frame을 legacy와 같은 최대 2000px long edge·JPEG 0.85로 만들고 기존 IndexedDB queue에 보관한 뒤 설정된 기존 GAS로 자동 전송한다. 미설정·offline에서는 POST 0을 유지하고 연결 복귀 뒤 같은 captureId로 보낸다.
- **quick-name OCR boundary**: legacy `nameCandidate`와 parity를 고정하고, 기기 `TextDetector`를 우선한 뒤 pinned self-hosted Tesseract로 fallback해 `quickName`을 같은 queue payload에 보존한다. OCR 실패는 촬영·로컬 보관을 막지 않는다.
- **OpenCV geometry boundary**: 기존 Otsu/Canny·adaptive threshold·명함 비율 scoring·perspective warp를 typed service로 분리하고, 엔진 지연·검출 실패 때 전체 프레임으로 비차단 fallback한다. 실제 명함 검출 품질 판정은 phone gate에 남긴다.
- **read/action parity**: 최근 캡처 상세·정보/사진 수정·같은 captureId 재전송, 20초 briefing refresh·지연 재처리·pagination·offline cache, Person profile, 전화·문자·메일·vCard, 사후 메모·조사 지시·수정 요청, 최근 검색과 PWA shortcut을 복원했다.
- **운영 전환**: 기존 사용자 기능을 React 앱에 직접 연결하고 legacy app은 rollback link로 유지한다. 계약·브라우저·CI 검증과 founder 실사용 확인 뒤 PR #8을 병합하고 Pages·GAS·watcher를 운영 배포했다.

## [Unreleased] — capture experience wave (branch `agent/card-capture-interview-wave`, Kairen-Ref: TSK-000178, TSK-000217, TSK-000218, TSK-000219, TSK-000220)

- **이름 먼저 확인**: 촬영 직후 브라우저 내장 OCR을 먼저 시도하고, 미지원 기기에서는 자체 호스팅 Tesseract.js 한국어+영어 모델을 사용한다. 이름은 즉시 확인·수정할 수 있으며 기기 OCR 결과와 provenance를 `quickName`으로 보존한다. 명함 이미지는 제3자 OCR 서비스로 전송하지 않는다.
- **안정 감지 자동 촬영**: 명함 사각형, 프레임 간 흔들림, 선명도, 심한 과노출을 함께 확인해 안정되면 자동 촬영한다. 사용자는 자동 촬영을 끄거나 언제든 수동 셔터·기본 카메라 폴백을 사용할 수 있다.
- **저대비 감지 보강**: 빠른 Canny 경로가 명함을 놓치면 적응형 임계값 경로를 제한 주기로 실행해 흰 배경·약한 테두리 인식을 보강한다.
- **시각 품질 개선**: 브랜드 헤더, 촬영 우선 계층, 차분한 색·타이포·카드 표면, 접근 가능한 포커스와 자동 촬영 진행 피드백을 적용했다.
- **조사 지시 탭**: owner는 최초 명함 등록과 기존 Person 카드에서 메모와 분리된 조사 지시를 남길 수 있다. GAS는 owner·feature flag·target 일치를 재검증하고 raw/requester/target/time/receipt/policy provenance를 별도 저장한다.
- **조사 지시 안전 경계**: raw 원문을 system prompt에 합치지 않고 public·lawful source bounded plan으로만 처리한다. private/login 자료, credential, 민감 특성 추론, doxxing, 외부 send/write, paid API, protected write와 human gate 우회는 실행하지 않는다.
- **회귀 검증**: 자동 촬영의 정상·오발·흔들림·흐림·과노출을 독립 순수 함수와 결정적 테스트로 고정했다.
- **조사 지시 회귀 검증**: owner/guest, 최초/기존 Person, target mismatch, note 혼입, prompt injection, private·sensitive·doxxing·credential·external·paid effect, source conflict를 합성 fixture로 고정했다.
- **처리 상태 정합성**: 브리핑 목록 조회는 cache-busting과 `no-store`로 최신 서버 상태를 읽고, 재처리 요청은 POST에서 terminal 상태를 재검증해 이미 완료·건너뜀인 카드를 `received`로 되돌리지 않는다. 재처리 경과 시간은 최신 `receivedAt`부터 다시 계산한다. (Kairen-Ref: TSK-000161)

## [Unreleased] — watcher v3 (branch `agent/watcher-v3`)

- **워처 v3 카드별 처리**: 대량 연속 캡처를 한 번의 긴 실행이 아니라 가장 이른 캡처부터 **한 건씩** 처리 → 카드마다 폰에 하나씩 도착하고, 사이마다 하트비트·backlog가 갱신됨. 무한 루프 방지 상한(25장/실행)·무진행 가드 포함 (ISS-000065, fixture 31/31 PASS). **라이브 워처 교체는 별도 단계**(로컬 pull+재시작, Setup 참조).

## v1.x UI (merged: PR #2 `agent/ux-retake`, PR #3 `agent/ux-round3`, PR #4 `agent/ux-round4`)

- **앞면 다시 찍기**: 앞면 촬영 직후 선택지([뒷면도 찍기 / 뒷면 없이 완료])에 "앞면 다시 찍기" 추가 — 카메라를 벗어나지 않고 앞면 교체 (2026-07-24 실사용 피드백), sw v13.

## [Unreleased] — v1.1 후보 (branch `agent/product-baseline-v1`, Kairen-Ref: TSK-000140~164)

추가 (배포 전 — GAS 재배포·사람 승인 필요):
- **바로 연락**: 브리핑·프렙 카드에서 전화·문자·메일·연락처 저장(vCard) — brief 텍스트 추출 폴백으로 기존 캡처도 동작, 처리 계약의 `contact` 요약 필드로 정확도 향상.
- **미팅 프렙 카드**: 검색 결과·전체 프로필 상단에 이름·직함·회사·마지막 확인·연락 버튼 요약 먼저 표시.
- **검색 상단 이동 + 최근 검색 3건** — 미팅 직전 10초 회상 동선 단축. 홈화면 **shortcuts**(인맥 검색/브리핑, `?view=`) 추가.
- **다시 처리 요청**: 30분 이상 지연된 캡처에서 사용자가 직접 재큐잉(`action=requeue`) + 문의 메일 버튼.
- **연속 촬영 동선**: 전송 즉시 상단 복귀 — 행사장에서 다음 명함을 끊김 없이.
- **검색**: owner 토큰 한정 Person 검색(`action=search`) + 웹앱 "검색" 탭 — 이름·회사로 축적된 인맥을 폰에서 회상.
- **사후 메모**: 브리핑·검색 결과에서 "메모 추가"(`action=addnote`) — 회의 끝나고, 또는 나중에 기억났을 때 그 사람 기록에 병합(-note 캡처로 파이프라인 재사용).
- **더 보기**: 브리핑(`action=list` limit/offset·hasMore)과 최근 캡처 목록에서 예전 것까지 조회.
- **처리 진행 표시**: 대기 중 브리핑에 경과 시간·통상 소요(6~20분)·30분 초과 경고 표시 — "깜깜무소식" 해소 (2026-07-24 실사용 피드백).
- **촬영 확인 토스트 상단 이동**: 하단 토스트가 "뒷면 추가" 버튼을 가리던 문제 수정 (2026-07-24 실사용 피드백).
- **처리 완료 알림**: 워처가 처리 완료 시 GAS(`action=notify`)를 통해 소유자 메일로 알림(0원, 옵트인 설정 파일).
- **수정 요청**: 브리핑에서 "수정 요청" 전송(`action=correction`) → 다음 처리 때 사용자 정정으로 반영.
- **워처 v2**: health 파일(`watcher-health.json`)·상태 점검 스크립트·연속 실패 추적·백로그 나이 노출 + 처리 프롬프트에 쓰기 allowlist·untrusted 입력 방어 삽입.
- **저장소 거버넌스**: AGENTS.md, SECURITY.md(위협 모델·토큰 runbook), RELEASE.md(release evidence 계약), CHANGELOG.md, `scripts/validate.ps1`(secret scan 포함), `eval/`(합성 회귀 fixture 16종 + 채점기).

## v1.0 — 2026-07-23 — `2d4704c`
- 어디서·관계 유지 필드에 2시간 만료 도입(오래된 행사명이 다음 캡처에 잘못 남는 문제 방지).

## v0.9 — 2026-07-23 — `6103230`
- 브리핑 자동 새로고침이 펼쳐 읽던 항목을 접지 않음(내용 무변경 시 재렌더 생략).
- 관계 필드 순서를 "Kairen과의 관계" 먼저로 변경, 최근 캡처 목록 표시 개편(이름·어디서 중심, 메모 숨김).

## v0.8 — 2026-07-23 — `01bc624`
- 브리핑 20초 자동 새로고침.
- 워처 인코딩 수정(UTF-8 BOM — 한글 경로 FATAL 해결), GAS가 Drive 중복 파일 중 최신 capture.json을 읽도록 수정(폰 '처리 대기중' 오표시 해결).

## v0.7 — 2026-07-23 — `c4554b1`
- 카메라 화면 개선(스크림+코너 브래킷+보간), 처리 엔진을 Codex로 전환(로그인 단계 제거).

## v0.6 — 2026-07-23 — `9177196`
- 브리핑에서 "전체 프로필 보기" — Person `.md` 전문 뷰어(`action=persondoc`, owner 한정).
- 즉시 처리 워처 도입(파일 이벤트+60초 폴링+시작 스윕, processing.lock).

## v0.5 — 2026-07-23 — `e496d02`
- "받은 명함 브리핑" 탭 + GAS `action=list`(토큰 scope, OWNER_NAMES 전체 열람).

## v0.4 — 2026-07-23 — `e914e1b`
- 뒷면 선택 명확화, 최근 캡처 상세(다시 찍기·수정·재전송).

## v0.3 — 2026-07-23 — `ff00186`
- 캡처 컨텍스트 4필드: 어디서 만났는지(유지형)·나와의 관계·Kairen과의 관계·메모.

## v0.2 — 2026-07-23 — `9433d19`
- 인페이지 카메라 + OpenCV.js 명함 사각형 자동 인식·크롭·원근 보정(self-hosted).

## v0.1 — 2026-07-23 — `7f9d007`
- 캡처 PWA가 기본 GAS 배포 URL을 내장.

## v0 (G0) — 2026-07-23 — `d85f248`
- 업로드 API(Code.gs: ping/whoami/POST)와 캡처 PWA 최초 공개(토큰 검증, Drive inbox 저장).
